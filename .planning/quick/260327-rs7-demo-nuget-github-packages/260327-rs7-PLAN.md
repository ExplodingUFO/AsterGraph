---
mode: quick
slug: 260327-rs7-demo-nuget-github-packages
objective: Align package/docs story to one main entry package plus protocol entry, then publish non-demo libraries to private GitHub Packages.
files_modified:
  - README.md
  - docs/host-integration.md
  - NuGet.config.sample
  - src/AsterGraph.Abstractions/AsterGraph.Abstractions.csproj
  - src/AsterGraph.Core/AsterGraph.Core.csproj
  - src/AsterGraph.Editor/AsterGraph.Editor.csproj
  - src/AsterGraph.Avalonia/AsterGraph.Avalonia.csproj
constraints:
  - Locked publish scope: `AsterGraph.Abstractions`, `AsterGraph.Core`, `AsterGraph.Editor`, `AsterGraph.Avalonia` only; exclude Demo.
  - Locked entry guidance: `AsterGraph.Avalonia` is the main integration entry; `AsterGraph.Abstractions` is the protocol/contract entry.
  - Repo docs are the primary place for package and usage guidance.
  - Must attempt real GitHub Packages publish; if credentials fail, stop cleanly with exact next steps.
---

<objective>
Deliver one atomic quick plan that updates package positioning + docs and performs real private package release for the four non-demo libraries.

Purpose: make host consumption unambiguous and immediately usable through GitHub Packages.
Output: aligned docs/metadata and either successful publish evidence or a clean blocked state with explicit credential remediation steps.
</objective>

<context>
@F:/CodeProjects/DotnetCore/avalonia-node-map/.planning/STATE.md
@F:/CodeProjects/DotnetCore/avalonia-node-map/.planning/quick/260327-rs7-demo-nuget-github-packages/260327-rs7-CONTEXT.md
@F:/CodeProjects/DotnetCore/avalonia-node-map/README.md
@F:/CodeProjects/DotnetCore/avalonia-node-map/docs/host-integration.md
@F:/CodeProjects/DotnetCore/avalonia-node-map/NuGet.config.sample
@F:/CodeProjects/DotnetCore/avalonia-node-map/Directory.Build.props
@F:/CodeProjects/DotnetCore/avalonia-node-map/src/AsterGraph.Abstractions/AsterGraph.Abstractions.csproj
@F:/CodeProjects/DotnetCore/avalonia-node-map/src/AsterGraph.Core/AsterGraph.Core.csproj
@F:/CodeProjects/DotnetCore/avalonia-node-map/src/AsterGraph.Editor/AsterGraph.Editor.csproj
@F:/CodeProjects/DotnetCore/avalonia-node-map/src/AsterGraph.Avalonia/AsterGraph.Avalonia.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align package positioning and metadata with the locked entry strategy</name>
  <files>README.md, docs/host-integration.md, src/AsterGraph.Abstractions/AsterGraph.Abstractions.csproj, src/AsterGraph.Core/AsterGraph.Core.csproj, src/AsterGraph.Editor/AsterGraph.Editor.csproj, src/AsterGraph.Avalonia/AsterGraph.Avalonia.csproj</files>
  <action>Update repo documentation and package metadata to express one clear consumption story: default hosts start from `AsterGraph.Avalonia` (main entry), contract-first/runtime-only hosts start from `AsterGraph.Abstractions` (protocol entry), and `Core`/`Editor` are referenced explicitly only for advanced scenarios. Ensure descriptions/readme pointers/tags remain consistent with this story and do not position Demo as a consumer package.</action>
  <verify>
    <automated>dotnet build "F:/CodeProjects/DotnetCore/avalonia-node-map/avalonia-node-map.sln" -v minimal</automated>
  </verify>
  <done>Docs and package metadata consistently communicate the same entry-point strategy and non-demo package boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare GitHub Packages feed guidance and publish commands for non-demo libraries</name>
  <files>README.md, docs/host-integration.md, NuGet.config.sample</files>
  <action>Add/adjust private-feed instructions for GitHub Packages consumption and release: source URL template, authenticated source setup pattern, and exact pack+push command flow for `AsterGraph.Abstractions`, `AsterGraph.Core`, `AsterGraph.Editor`, and `AsterGraph.Avalonia` only. Keep secrets out of committed config and preserve local-feed sample behavior.</action>
  <verify>
    <automated>dotnet pack "F:/CodeProjects/DotnetCore/avalonia-node-map/src/AsterGraph.Abstractions/AsterGraph.Abstractions.csproj" -c Release -o "F:/CodeProjects/DotnetCore/avalonia-node-map/artifacts/packages" && dotnet pack "F:/CodeProjects/DotnetCore/avalonia-node-map/src/AsterGraph.Core/AsterGraph.Core.csproj" -c Release -o "F:/CodeProjects/DotnetCore/avalonia-node-map/artifacts/packages" && dotnet pack "F:/CodeProjects/DotnetCore/avalonia-node-map/src/AsterGraph.Editor/AsterGraph.Editor.csproj" -c Release -o "F:/CodeProjects/DotnetCore/avalonia-node-map/artifacts/packages" && dotnet pack "F:/CodeProjects/DotnetCore/avalonia-node-map/src/AsterGraph.Avalonia/AsterGraph.Avalonia.csproj" -c Release -o "F:/CodeProjects/DotnetCore/avalonia-node-map/artifacts/packages"</automated>
  </verify>
  <done>Repository contains actionable feed configuration and command-level publishing instructions aligned to the locked package list.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <decision>Credential gate for real GitHub Packages publish</decision>
  <context>Execution must attempt `dotnet nuget push` to GitHub Packages. If credentials are valid, publish completes; if not, execution must stop with exact remediation steps, not partial success claims.</context>
  <how-to-verify>
    1. Attempt push of generated `.nupkg` files to GitHub Packages source (exclude Demo artifacts).
    2. On success: capture published package IDs + version and finish.
    3. On auth failure (401/403): fail cleanly and provide exact next steps:
       - Create/update a PAT with at least `write:packages` (and `read:packages` for restore).
       - Add/update source credentials:
         `dotnet nuget add source "https://nuget.pkg.github.com/<OWNER>/index.json" --name github --username <GITHUB_USER> --password <PAT> --store-password-in-clear-text`
       - Retry push:
         `dotnet nuget push "artifacts/packages/*.nupkg" --source github --skip-duplicate`
    4. Resume only after user confirms credentials are configured.
  </how-to-verify>
  <resume-signal>Reply with either "publish-ok" (if push succeeded) or "credentials-ready" after completing the remediation steps.</resume-signal>
</task>

</tasks>

<success_criteria>
- Repo docs and package metadata reflect the locked entry model (`AsterGraph.Avalonia` main entry, `AsterGraph.Abstractions` protocol entry).
- GitHub Packages private-feed setup and publish instructions are explicit and secret-safe.
- Non-demo libraries are packed and published, or execution fails cleanly with exact credential next steps and no ambiguity.
- Demo remains excluded from publish scope and consumption guidance.
</success_criteria>
