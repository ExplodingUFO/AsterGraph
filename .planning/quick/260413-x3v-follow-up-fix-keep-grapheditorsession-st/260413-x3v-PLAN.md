---
mode: quick
slug: 260413-x3v-follow-up-fix-keep-grapheditorsession-st
objective: Follow up the stock menu builder extraction so canvas menu descriptors continue reading live node catalog definitions after session creation.
files_modified:
  - src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
  - src/AsterGraph.Editor/Runtime/Internal/GraphEditorSessionStockMenuDescriptorBuilder.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
constraints:
  - Keep the extracted helper structure in place.
  - Do not freeze node definitions at session construction time.
  - Add one focused regression proving post-session `RegisterDefinition(...)` updates canvas menu descriptors.
  - Do not widen the change into unrelated runtime or retained menu work.
---

<tasks>

<task type="auto">
  <name>Task 1: Add a failing live-catalog regression test</name>
  <files>tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs</files>
  <action>Add a focused test that creates a session, registers another node definition on the same `NodeCatalog`, and asserts `BuildContextMenuDescriptors(Canvas)` exposes the new add-node item.</action>
  <verify>
    <automated>Run the focused temporary harness for the new test and confirm it fails before the fix.</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Switch stock builder definitions dependency from snapshot to live provider</name>
  <files>src/AsterGraph.Editor/Runtime/GraphEditorSession.cs, src/AsterGraph.Editor/Runtime/Internal/GraphEditorSessionStockMenuDescriptorBuilder.cs</files>
  <action>Replace the helper's frozen definitions list with a live provider delegate and update `GraphEditorSession` to pass the descriptor-support definitions through that provider.</action>
  <verify>
    <automated>Re-run the focused regression test and ensure it passes.</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Re-run focused session verification and record the quick fix</name>
  <files>.planning/quick/260413-x3v-follow-up-fix-keep-grapheditorsession-st/260413-x3v-SUMMARY.md, .planning/STATE.md</files>
  <action>Run the focused `GraphEditorSessionTests` harness plus `AsterGraph.Editor` build, then record the commands and results for this follow-up fix.</action>
  <verify>
    <automated>Focused harness green; `dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj -v minimal` green.</automated>
  </verify>
</task>

</tasks>
