---
mode: quick
slug: 260327-q6x-demo
objective: Fix the Demo inspector parameter editor so inputs are obvious and usable, then finish with one clean commit.
files_modified:
  - src/AsterGraph.Avalonia/Controls/GraphInspectorView.axaml
  - src/AsterGraph.Avalonia/Themes/AsterGraphTheme.axaml
  - tests/AsterGraph.Editor.Tests/GraphInspectorStandaloneTests.cs
constraints:
  - Honor D-01, D-02, D-03: keep the single-page three-column shell and one primary live editor.
  - Honor D-08 and D-13: make runtime/editor capability seams clearer through UI evidence, not placeholder text.
  - Honor D-09, D-10, D-11: keep user-facing copy Chinese-first while preserving English API/type identifiers.
  - Do not introduce a second runtime model, new UI stack, or unrelated feature work.
  - Produce one clean commit for this quick fix (stage only relevant files).
---

<objective>
Make the right-side parameter editing surface inside the Demo experience visually obvious and practically usable.

Purpose: remove ambiguity around whether parameter fields are editable and reduce “looks disabled / hard to notice” friction.
Output: clearer parameter editor affordances in the inspector, matching theme tokens, focused regression coverage, and one clean commit.
</objective>

<context>
@F:/CodeProjects/DotnetCore/avalonia-node-map/.planning/STATE.md
@F:/CodeProjects/DotnetCore/avalonia-node-map/.planning/phases/06-demo/06-CONTEXT.md
@F:/CodeProjects/DotnetCore/avalonia-node-map/.planning/phases/06-demo/06-UI-SPEC.md
@F:/CodeProjects/DotnetCore/avalonia-node-map/.planning/quick/260327-pas-phase-6-demo/260327-pas-CONTEXT.md
@F:/CodeProjects/DotnetCore/avalonia-node-map/src/AsterGraph.Avalonia/Controls/GraphInspectorView.axaml
@F:/CodeProjects/DotnetCore/avalonia-node-map/src/AsterGraph.Avalonia/Themes/AsterGraphTheme.axaml
@F:/CodeProjects/DotnetCore/avalonia-node-map/src/AsterGraph.Editor/ViewModels/NodeParameterViewModel.cs
@F:/CodeProjects/DotnetCore/avalonia-node-map/tests/AsterGraph.Editor.Tests/GraphInspectorStandaloneTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen parameter editor affordance and clarity in inspector cards</name>
  <files>src/AsterGraph.Avalonia/Controls/GraphInspectorView.axaml</files>
  <action>Refine the parameter section UI so editable controls are unmistakable per D-08 and D-13. Keep the existing parameter binding model, but update the parameter card structure/copy/visual cues to clearly distinguish: editable vs read-only, mixed values, and validation errors. Use Chinese-first UX labels (per D-09/D-11), keep technical identifiers in English only where they are API/type names (per D-10), and avoid adding new behavior paths or runtime state sources. The result should make text/enum/bool editors look interactive instead of blending into surrounding explanation text.</action>
  <verify>
    <automated>dotnet test "F:/CodeProjects/DotnetCore/avalonia-node-map/tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj" --filter "GraphInspectorStandaloneTests" -v minimal</automated>
  </verify>
  <done>When a node is selected, parameter cards read as actionable editor controls (not passive copy), and key states (mixed/invalid/read-only) are visually understandable at a glance.</done>
</task>

<task type="auto">
  <name>Task 2: Align theme tokens and input styles for stronger edit-state contrast</name>
  <files>src/AsterGraph.Avalonia/Themes/AsterGraphTheme.axaml, src/AsterGraph.Avalonia/Controls/GraphInspectorView.axaml</files>
  <action>Adjust inspector/input-related tokens and control styles to increase contrast and discoverability without breaking the phase’s dark technical shell (D-12). Tighten corner radius and border emphasis only where it improves editability cues; ensure focused/editable controls are visually stronger than surrounding descriptive text blocks; keep style changes scoped to inspector parameter editing surfaces so the three-column architecture showcase (D-01/D-03) remains intact.</action>
  <verify>
    <automated>dotnet test "F:/CodeProjects/DotnetCore/avalonia-node-map/tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj" --filter "GraphInspectorStandaloneTests|GraphEditorViewTests" -v minimal</automated>
  </verify>
  <done>Parameter input controls have clear visual hierarchy and usable contrast in the Demo/GraphEditor inspector context, while existing shell structure and behavior remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock the fix with focused regression checks and create one clean commit</name>
  <files>tests/AsterGraph.Editor.Tests/GraphInspectorStandaloneTests.cs</files>
  <behavior>
    - Test 1: Inspector parameter section exposes explicit editable controls for selected node parameters.
    - Test 2: Parameter editing cues remain present after style refactor (no regression to “looks like plain text”).
    - Test 3: Existing standalone-inspector boundary still holds (no workspace/fragment shell leakage).
  </behavior>
  <action>Update focused headless tests to assert the new parameter-editor affordance structure using stable control/text/visibility assertions. Then run the filtered suite and create one clean commit containing only this quick fix’s files. Do not include unrelated pre-existing workspace edits in the commit.</action>
  <verify>
    <automated>dotnet test "F:/CodeProjects/DotnetCore/avalonia-node-map/tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj" --filter "GraphInspectorStandaloneTests|GraphEditorViewTests|DemoMainWindowTests" -v minimal</automated>
  </verify>
  <done>Focused regressions pass and exactly one clean commit is produced for the inspector-parameter usability fix.</done>
</task>

</tasks>

<success_criteria>
- Demo inspector parameter editors are clearly discoverable and usable in the right-side editing flow.
- Editable/read-only/mixed/validation states are visually distinct and understandable.
- Phase 6 three-column, single-editor architecture contract remains intact.
- Focused tests guard the new affordance contract.
- Work is finalized as one clean commit with only relevant files.
</success_criteria>
