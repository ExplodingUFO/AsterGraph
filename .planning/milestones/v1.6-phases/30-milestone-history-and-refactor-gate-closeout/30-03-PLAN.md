---
phase: 30-milestone-history-and-refactor-gate-closeout
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - README.md
  - docs/host-integration.md
requirements:
  - CLOSE-02
  - GUARD-01
autonomous: true
---

<objective>
Synchronize the live planning and contributor-facing docs around the reconstructed `v1.4` archive and the new maintenance lane so the current proof ring, carried concerns, and next action are obvious from current files instead of stale phase-directory context.
</objective>

<scope>
- Update top-level planning artifacts to point at the new `v1.4` archive and current milestone posture.
- Add the new `maintenance` lane to the contributor-facing verification story while keeping `release` as the publish-facing full gate.
- Keep the phase narrow: do not introduce new proof tools, new samples, or Phase 31 semantic fixes here.
</scope>

<must_haves>
- `.planning/ROADMAP.md` no longer describes `v1.4` as waiting for archive and instead links to the new archive.
- `.planning/PROJECT.md` and `.planning/STATE.md` identify the live proof ring, the carried history/save concern, and the next milestone action without assuming missing phase directories still exist.
- `README.md` and `docs/host-integration.md` describe `eng/ci.ps1 -Lane maintenance` as the targeted hotspot-refactor gate and keep `-Lane release` as the publish-facing gate.
- Current planning/docs tell one consistent story about proof surfaces: focused regression lanes, `PackageSmoke`, and `ScaleSmoke`.
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Sync the live planning artifacts to the new archive and next action</name>
    <files>
      .planning/PROJECT.md
      .planning/ROADMAP.md
      .planning/STATE.md
    </files>
    <read_first>
      .planning/PROJECT.md
      .planning/ROADMAP.md
      .planning/STATE.md
      .planning/MILESTONES.md
      .planning/phases/30-milestone-history-and-refactor-gate-closeout/30-RESEARCH.md
    </read_first>
    <action>
      Update the live planning files so `v1.4` is treated as archived history, the carried `STATE_HISTORY_OK` concern remains visible for Phase 31, and the current next action points at Phase 30 execution or Phase 31 planning rather than missing older phase artifacts.
    </action>
    <acceptance_criteria>
      `.planning/ROADMAP.md` links the archived `v1.4` milestone
      `.planning/PROJECT.md` no longer lists missing `v1.4` archive files as current debt after Plan 01 lands
      `.planning/STATE.md` preserves the carried history/save concern and surfaces the new maintenance-gate context
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Task 2: Sync contributor-facing verification docs to the maintenance lane</name>
    <files>
      README.md
      docs/host-integration.md
    </files>
    <read_first>
      README.md
      docs/host-integration.md
      eng/ci.ps1
      .planning/phases/30-milestone-history-and-refactor-gate-closeout/30-RESEARCH.md
    </read_first>
    <action>
      Add the new `maintenance` lane to the documented verification flow as the targeted hotspot-refactor gate, keep `release` as the publish gate, and preserve the current proof-ring description around `PackageSmoke`, `ScaleSmoke`, and the split test lanes.
    </action>
    <acceptance_criteria>
      `README.md` documents `eng/ci.ps1 -Lane maintenance`
      `docs/host-integration.md` documents `eng/ci.ps1 -Lane maintenance`
      both docs keep `eng/ci.ps1 -Lane release` as the publish-facing gate
      the docs do not invent a second proof-ring tool or second script path
    </acceptance_criteria>
  </task>
</tasks>

<threat_model>
- Risk: top-level docs drift again by describing a maintenance command different from the script implementation.
- Mitigation: update docs only after Plan 02 lands, and point every contributor-facing reference at the same `eng/ci.ps1 -Lane maintenance` command.
- Risk: planning files hide the carried history/save concern while trying to clean up archive debt.
- Mitigation: keep `STATE_HISTORY_OK` and the Phase 31 follow-on explicit in `.planning/PROJECT.md` and `.planning/STATE.md`.
- Risk: archive sync work accidentally widens into release-gate or semantic-fix scope.
- Mitigation: keep changes limited to archive links, proof-ring wording, maintenance-lane guidance, and next-step normalization.
</threat_model>

<verification>
- pwsh -NoProfile -Command "$hits = @((Select-String -Path '.planning/ROADMAP.md' -Pattern 'v1.4-ROADMAP.md' -Quiet),(Select-String -Path '.planning/PROJECT.md' -Pattern 'maintenance gate' -Quiet),(Select-String -Path '.planning/STATE.md' -Pattern 'maintenance|Phase 31|execute-phase 30' -Quiet),(Select-String -Path 'README.md' -Pattern 'maintenance gate|Lane maintenance' -Quiet),(Select-String -Path 'docs/host-integration.md' -Pattern 'maintenance gate|Lane maintenance' -Quiet)); if ($hits -contains $false) { exit 1 }"
- pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane maintenance -Framework all -Configuration Release
</verification>

<output>
After completion, create `.planning/phases/30-milestone-history-and-refactor-gate-closeout/30-03-SUMMARY.md`
</output>
