---
phase: 31-history-and-save-semantic-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AsterGraph.Editor/ViewModels/Facade/GraphEditorViewModel.InteractionSurface.cs
  - src/AsterGraph.Editor/ViewModels/Facade/GraphEditorViewModel.PersistenceSurface.cs
  - src/AsterGraph.Editor/ViewModels/Facade/GraphEditorViewModel.StateProjection.cs
  - src/AsterGraph.Editor/ViewModels/Internal/GraphEditorViewModelKernelAdapter.cs
  - src/AsterGraph.Editor/Kernel/GraphEditorKernel.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorHistorySemanticTests.cs
requirements:
  - STATE-01
autonomous: true
---

<objective>
Converge retained local mutations onto one undo/redo/dirty/save contract so retained drag and other facade-owned mutations no longer fork away from kernel-owned history when earlier runtime commands have already populated the kernel stack.
</objective>

<scope>
- Add one focused mixed-path regression that reproduces the current retained-vs-kernel history conflict.
- Introduce one internal retained-mutation commit path so local retained completion points can reuse the same history/save authority that `Undo`, `Redo`, and dirty-state projection actually read.
- Keep public APIs, constructor signatures, and user-facing host entry points unchanged.
</scope>

<must_haves>
- Retained drag after earlier kernel/session mutations still undoes and redoes the just-committed positions instead of silently routing to a different history owner.
- Save-boundary dirty semantics are explicit and stable after mixed kernel-plus-retained mutation sequences.
- `CanUndo` and `CanRedo` reflect the same authority that `Undo` and `Redo` will execute.
- The phase does not introduce a new public command, public event, or public migration burden.
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Add a focused mixed-path retained history/save regression</name>
    <files>
      tests/AsterGraph.Editor.Tests/GraphEditorHistorySemanticTests.cs
    </files>
    <read_first>
      .planning/phases/31-history-and-save-semantic-closure/31-RESEARCH.md
      tests/AsterGraph.Editor.Tests/GraphEditorTransactionTests.cs
      tools/AsterGraph.ScaleSmoke/Program.cs
    </read_first>
    <action>
      Add a focused editor test that first seeds kernel history through runtime/session mutations, then performs retained drag/save/undo/redo so the current mixed-owner semantic path is covered directly rather than only through `ScaleSmoke`.
    </action>
    <acceptance_criteria>
      a dedicated focused history semantic test file exists
      the new regression covers kernel-history-seeded retained drag/save behavior
      the new regression asserts the intended dirty/save contract instead of preserving the carried mismatch
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Task 2: Implement one retained-mutation commit path shared by history interaction and retained dirty finishers</name>
    <files>
      src/AsterGraph.Editor/ViewModels/Facade/GraphEditorViewModel.InteractionSurface.cs
      src/AsterGraph.Editor/ViewModels/Facade/GraphEditorViewModel.PersistenceSurface.cs
      src/AsterGraph.Editor/ViewModels/Facade/GraphEditorViewModel.StateProjection.cs
      src/AsterGraph.Editor/ViewModels/Internal/GraphEditorViewModelKernelAdapter.cs
      src/AsterGraph.Editor/Kernel/GraphEditorKernel.cs
    </files>
    <read_first>
      src/AsterGraph.Editor/ViewModels/Facade/GraphEditorViewModel.InteractionSurface.cs
      src/AsterGraph.Editor/ViewModels/Facade/GraphEditorViewModel.PersistenceSurface.cs
      src/AsterGraph.Editor/ViewModels/Facade/GraphEditorViewModel.StateProjection.cs
      src/AsterGraph.Editor/ViewModels/Internal/GraphEditorViewModelKernelAdapter.cs
      src/AsterGraph.Editor/Services/History/GraphEditorHistoryStateCoordinator.cs
      src/AsterGraph.Editor/Kernel/GraphEditorKernel.cs
      .planning/phases/31-history-and-save-semantic-closure/31-RESEARCH.md
    </read_first>
    <action>
      Implement one internal path that commits retained local mutations back through kernel-owned history/save state, then route retained history completion and shared retained `MarkDirty(...)` finishers through it so the facade stops exposing a mixed undo/redo authority.
    </action>
    <acceptance_criteria>
      retained `CompleteHistoryInteraction(...)` no longer depends on a history owner that `Undo()` / `Redo()` might ignore
      retained dirty/save semantics remain correct even when kernel history already exists
      `CanUndo` and `CanRedo` stay aligned with the actual undo/redo path
      public entry points and public runtime contracts remain unchanged
    </acceptance_criteria>
  </task>
</tasks>

<threat_model>
- Risk: the fix only patches drag while other retained local mutation finishers still fork history/save semantics.
- Mitigation: route both retained history interaction completion and shared retained `MarkDirty(...)` flows through the same internal commit path.
- Risk: live drag responsiveness regresses because the kernel is updated on every pointer move.
- Mitigation: keep live drag preview local during movement, then commit once at retained interaction completion.
- Risk: the phase widens into general facade extraction.
- Mitigation: constrain the change to semantic ownership and finishers; leave broader collaborator movement to Phase 32.
</threat_model>

<verification>
- dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorHistorySemanticTests|FullyQualifiedName~GraphEditorTransactionTests|FullyQualifiedName~GraphEditorFacadeRefactorTests|FullyQualifiedName~GraphEditorSessionTests" -v minimal
</verification>

<output>
After completion, create `.planning/phases/31-history-and-save-semantic-closure/31-01-SUMMARY.md`
</output>
