---
phase: 31-history-and-save-semantic-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - tools/AsterGraph.ScaleSmoke/Program.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorProofRingTests.cs
requirements:
  - STATE-03
autonomous: true
---

<objective>
Replace the carried `STATE_HISTORY_OK` mismatch with aligned, machine-checkable proof output so `ScaleSmoke` and the proof ring report the same retained history/save contract enforced by the focused tests.
</objective>

<scope>
- Update the `ScaleSmoke` history proof from a raw ambiguous tuple into an explicit contract check.
- Add automated proof coverage for the same semantic contract so the proof ring does not rely on stale planning notes or manual interpretation.
- Keep existing proof-ring markers for readiness and automation intact unless the history proof needs a supplemental detail field.
</scope>

<must_haves>
- `ScaleSmoke` no longer emits a "known mismatch" history marker that contradicts the focused tests.
- The history proof is explicit about pass/fail semantics and can still emit supplemental detail when useful for debugging.
- Automated proof coverage protects the same contract that `ScaleSmoke` reports.
- The maintenance lane remains green after the proof surface changes.
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Align `ScaleSmoke` history output to the new semantic contract</name>
    <files>
      tools/AsterGraph.ScaleSmoke/Program.cs
    </files>
    <read_first>
      tools/AsterGraph.ScaleSmoke/Program.cs
      tests/AsterGraph.Editor.Tests/GraphEditorHistorySemanticTests.cs
      tests/AsterGraph.Editor.Tests/GraphEditorSaveBoundaryTests.cs
      .planning/phases/31-history-and-save-semantic-closure/31-RESEARCH.md
    </read_first>
    <action>
      Update the scale-smoke history proof so it evaluates the retained history/save contract directly and emits an explicit success marker, with supplemental detail only after the pass/fail result.
    </action>
    <acceptance_criteria>
      `ScaleSmoke` no longer relies on the old ambiguous `STATE_HISTORY_OK` mismatch tuple
      the history proof output clearly indicates whether the semantic contract passed
      the history proof still leaves enough detail to debug failures when it does not pass
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Task 2: Add proof-ring coverage for the aligned history/save contract</name>
    <files>
      tests/AsterGraph.Editor.Tests/GraphEditorProofRingTests.cs
    </files>
    <read_first>
      tests/AsterGraph.Editor.Tests/GraphEditorProofRingTests.cs
      tools/AsterGraph.ScaleSmoke/Program.cs
      .planning/phases/31-history-and-save-semantic-closure/31-RESEARCH.md
    </read_first>
    <action>
      Add focused proof-ring coverage that exercises the same retained history/save contract used by `ScaleSmoke`, so contributors can verify the proof surface through tests without parsing a stale known-mismatch note.
    </action>
    <acceptance_criteria>
      proof-ring coverage asserts the aligned history/save contract
      the proof-ring test does not rely on a stale known-mismatch expectation
      the focused proof tests and `ScaleSmoke` tell one consistent story
    </acceptance_criteria>
  </task>
</tasks>

<threat_model>
- Risk: `ScaleSmoke` still emits a tuple that humans have to interpret manually.
- Mitigation: emit an explicit pass/fail history marker and keep raw details secondary.
- Risk: proof-ring coverage drifts from the smoke proof after the marker change.
- Mitigation: base the proof-ring assertion on the same semantic contract being enforced by the focused history/save suites.
- Risk: changing proof output accidentally breaks unrelated readiness or automation markers.
- Mitigation: isolate the change to the history marker and keep the rest of the proof surface untouched.
</threat_model>

<verification>
- dotnet run --project tools/AsterGraph.ScaleSmoke/AsterGraph.ScaleSmoke.csproj -c Release --framework net8.0 --nologo
- dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorProofRingTests|FullyQualifiedName~GraphEditorHistoryInteractionTests|FullyQualifiedName~GraphEditorSaveBoundaryTests" -v minimal
- pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane maintenance -Framework all -Configuration Release
</verification>

<output>
After completion, create `.planning/phases/31-history-and-save-semantic-closure/31-03-SUMMARY.md`
</output>
