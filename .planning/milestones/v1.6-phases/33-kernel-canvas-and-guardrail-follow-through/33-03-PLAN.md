---
phase: 33-kernel-canvas-and-guardrail-follow-through
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - Directory.Build.props
  - src/AsterGraph.Editor/AsterGraph.Editor.csproj
  - src/AsterGraph.Avalonia/AsterGraph.Avalonia.csproj
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
requirements:
  - GUARD-02
  - FACADE-03
  - FACADE-04
autonomous: true
---

<objective>
Replace the repo-wide `CS1591` blanket with scoped XML-doc debt handling, then close Phase 33 with verification and planning-state sync.
</objective>

<scope>
- Remove repo-wide `CS1591` suppression from `Directory.Build.props`.
- Apply scoped suppression only where current public API debt still exists.
- Prove `AsterGraph.Abstractions` and `AsterGraph.Core` remain clean under `warnaserror:CS1591`.
- Finish the phase with summaries, review, verification, and live planning updates.
</scope>

<must_haves>
- repo root no longer hides `CS1591` for every project
- clean publishable packages prove they stay clean under `warnaserror:CS1591`
- remaining doc debt is explicit at project scope instead of repo scope
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Scope XML-doc debt to the projects that still need it</name>
    <files>
      Directory.Build.props
      src/AsterGraph.Editor/AsterGraph.Editor.csproj
      src/AsterGraph.Avalonia/AsterGraph.Avalonia.csproj
    </files>
    <read_first>
      Directory.Build.props
      src/AsterGraph.Editor/AsterGraph.Editor.csproj
      src/AsterGraph.Avalonia/AsterGraph.Avalonia.csproj
      .planning/phases/33-kernel-canvas-and-guardrail-follow-through/33-RESEARCH.md
    </read_first>
    <action>
      Move XML-doc suppression out of the repo root and into explicit project scopes, preserving successful builds while making the remaining debt boundary honest.
    </action>
    <acceptance_criteria>
      repo-wide blanket suppression is removed
      only projects with live debt keep scoped suppression
      build stays green
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Task 2: Verify doc guardrails and close the phase</name>
    <files>
      .planning/ROADMAP.md
      .planning/STATE.md
      .planning/REQUIREMENTS.md
      eng/ci.ps1
    </files>
    <read_first>
      .planning/phases/33-kernel-canvas-and-guardrail-follow-through/33-VALIDATION.md
      eng/ci.ps1
    </read_first>
    <action>
      Run the doc-focused build checks, rerun maintenance, then close the phase with summaries, review, verification, and milestone-state updates.
    </action>
    <acceptance_criteria>
      doc-focused build checks pass
      maintenance passes
      phase closeout artifacts exist
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- dotnet build src/AsterGraph.Abstractions/AsterGraph.Abstractions.csproj -c Release /warnaserror:CS1591 -v minimal
- dotnet build src/AsterGraph.Core/AsterGraph.Core.csproj -c Release /warnaserror:CS1591 -v minimal
- dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj -c Release -v minimal
- dotnet build src/AsterGraph.Avalonia/AsterGraph.Avalonia.csproj -c Release -v minimal
- pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane maintenance -Framework all -Configuration Release
</verification>

<output>
After completion, create `.planning/phases/33-kernel-canvas-and-guardrail-follow-through/33-03-SUMMARY.md`, `.planning/phases/33-kernel-canvas-and-guardrail-follow-through/33-REVIEW.md`, and `.planning/phases/33-kernel-canvas-and-guardrail-follow-through/33-VERIFICATION.md`
</output>
