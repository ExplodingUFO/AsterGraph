# Plan 43-03: Add one checked-in .NET 10 consumer proof

## Goal

Verify that a `.NET 10` consumer can still load the packed AsterGraph surface through the minimal host path.

## Steps

1. Extend `HostSample` so it can build under `net10.0` for packed-package consumption without disturbing the existing `net8.0` project-reference proof.
2. Extend `eng/ci.ps1` release validation to run and capture one packed `HostSample` proof marker under `net10.0`.
3. Surface the `.NET 10` marker in release artifacts and summaries so maintainers can see the proof result directly.

## Verification

- `dotnet build tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj -c Release -f net10.0 /p:UsePackedAsterGraphPackages=true`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane release -Framework all -Configuration Release`
