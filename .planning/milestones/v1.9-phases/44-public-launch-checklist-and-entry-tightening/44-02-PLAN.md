# Plan 44-02: Refresh alpha-status and quick-start

## Goal

Bring the public guides in `docs/en` up to the current operational reality after workflow stabilization.

## Steps

1. Rewrite `docs/en/alpha-status.md` known limitations to focus on remaining operational launch steps.
2. Update `docs/en/quick-start.md` package-source and verification sections to point to the maintained launch checklist and the `.NET 10` HostSample proof.
3. Preserve the current canonical-entry guidance and the `HostSample` vs `Demo` roles.

## Verification

- `rg -n "launch checklist|NuGet.org|HostSample|AsterGraph.Demo|net10" docs/en/alpha-status.md docs/en/quick-start.md`
