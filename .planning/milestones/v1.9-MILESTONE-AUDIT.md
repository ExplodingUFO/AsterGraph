---
milestone: v1.9
audited: 2026-04-16T11:25:20Z
status: passed
scores:
  requirements: 10/10
  phases: 3/3
  integration: 4/4
  flows: 4/4
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: milestone-close
    items:
      - "GitHub-hosted workflows are now green, but Actions still emit Node.js 20 deprecation warnings for checkout/setup-dotnet/upload-artifact and should be updated before the 2026 runner cutovers."
nyquist:
  compliant_phases: []
  partial_phases: [42, 43, 44]
  missing_phases: []
  overall: partial
---

# Milestone v1.9 Audit

## Verdict

Passed. v1.9 closed the last operational blockers between the shipped `0.2.0-alpha.1` surface and a repo that can be opened confidently: clean-runner CI now matches local proof assumptions, the prerelease workflow schedules correctly, the consumer proof story now includes `.NET 10`, and the remaining public-opening steps are published as one short checklist.

## Scope

- Milestone: `v1.9 Public Launch Gate and CI Stabilization`
- Phases: 42-44
- Plans: 9
- Audited sources:
  - `.planning/ROADMAP.md`
  - `.planning/REQUIREMENTS.md`
  - `.planning/PROJECT.md`
  - `.planning/STATE.md`
  - phase 42-44 research, validation, review, and verification artifacts
  - GitHub Actions run `24506891943` (`ci.yml`)
  - GitHub Actions run `24507210737` (`release.yml`, manual dispatch)

## Requirements Coverage

| Requirement | Assigned Phase | Final Status | Evidence |
|-------------|----------------|--------------|----------|
| CI-01 | 42 | satisfied | Hosted `ci.yml` run `24506891943` passed on Windows and Linux after the clean-runner proof fixes landed. |
| CI-02 | 42 | satisfied | Plugin proof tests now resolve staged payloads from active build outputs, and path-bearing assertions compare canonicalized paths instead of Windows-only literals. |
| CI-03 | 43 | satisfied | Both workflows now prepare and use a workspace-local `NUGET_PACKAGES` path, eliminating the failing post-job cache cleanup assumption about the default global package folder. |
| CI-04 | 43 | satisfied | `eng/ci.ps1 -Lane release -Framework all -Configuration Release` now emits `HOST_SAMPLE_NET10_OK:True` from the packed consumer path. |
| REL-01 | 43 | satisfied | Manual `release.yml` dispatch `24507210737` scheduled validation jobs correctly instead of failing with zero jobs. |
| REL-02 | 43 | satisfied | The manual dispatch ran validation jobs and cleanly skipped `publish-nuget-prerelease` plus `create-github-prerelease` outside a real tagged publish context. |
| REL-03 | 43 | satisfied | Branch CI and prerelease release automation now have one clear operational split: `ci.yml` proves the branch surface, `release.yml` validates and only publishes when tag/secret gates are satisfied. |
| OSS-01 | 44 | satisfied | README and `docs/alpha-status.md` now describe only the real remaining launch blockers after workflow stabilization instead of replaying v1.8 work. |
| OSS-02 | 44 | satisfied | `.planning/public-launch-checklist.md` now gives one short operational checklist for visibility, checks, first tag, and artifact review. |
| OSS-03 | 44 | satisfied | README, quick start, and launch guidance still keep `HostSample` as the minimal consumer proof and `AsterGraph.Demo` as the showcase host. |

## Phase Verification Summary

| Phase | Status | Requirements | Evidence |
|-------|--------|--------------|----------|
| 42 | passed | CI-01, CI-02 | focused plugin contract suite `16/16`, hosted `ci.yml` run `24506891943`, updated `42-VERIFICATION.md` |
| 43 | passed | CI-03, CI-04, REL-01, REL-02, REL-03 | local `release` lane passing with `HOST_SAMPLE_NET10_OK:True`, manual `release.yml` dispatch `24507210737`, workflow review |
| 44 | passed | OSS-01, OSS-02, OSS-03 | doc alignment review, launch checklist presence, and phase 44 verification artifacts |

## Cross-Phase Integration

### Passed wiring checks

- Phase 42 removed the last hidden local-output assumption from plugin proof tests, which made the Phase 43 workflow stabilization meaningful on hosted Linux instead of only on the local Windows workspace.
- Phase 43 folded the new `.NET 10` packed consumer proof into the same release lane that already runs `HostSample`, `PackageSmoke`, `ScaleSmoke`, and coverage, so launch readiness stayed on one command path.
- Phase 44 tightened public docs only after the workflows were actually green, so launch guidance now points at the real maintained proof surface instead of aspirational YAML.
- The hosted `ci.yml` and manually dispatched `release.yml` together now prove the intended branch-vs-release split rather than leaving one of those entry points unexecuted.

## End-to-End Flows

| Flow | Status | Evidence |
|------|--------|----------|
| Branch CI on hosted runners | passed | `ci.yml` run `24506891943` concluded `success` across Windows, Linux, contract-proof, and release-validation jobs. |
| Manual prerelease workflow dispatch | passed | `release.yml` run `24507210737` concluded `success`; validation jobs ran and publish/release jobs skipped cleanly. |
| Packed consumer proof including `.NET 10` | passed | local `release` lane passed with `HOST_SAMPLE_OK:True`, `HOST_SAMPLE_NET10_OK:True`, `PACKAGE_SMOKE_OK:True`, and `SCALE_HISTORY_CONTRACT_OK:True:True:False:True:False:22`. |
| Public launch guidance flow | passed | README, alpha-status, quick start, and the launch checklist all describe the same remaining operational steps and keep `HostSample` / `Demo` roles explicit. |

## Nyquist Coverage

| Phase | VALIDATION.md | Status | Notes |
|-------|---------------|--------|-------|
| 42 | present | partial | validation contract present, older Nyquist frontmatter keys not normalized |
| 43 | present | partial | validation contract present, older Nyquist frontmatter keys not normalized |
| 44 | present | partial | validation contract present, older Nyquist frontmatter keys not normalized |

## Non-Critical Tech Debt

- Hosted workflows are green, but GitHub still emits Node.js 20 deprecation warnings for the current `actions/checkout@v4`, `actions/setup-dotnet@v4`, and `actions/upload-artifact@v4` pins. That should be cleaned up before the 2026 hosted-runner cutovers.

## Conclusion

v1.9 achieved its definition of done. The repo now has hosted-runner parity, a valid prerelease workflow, an explicit `.NET 10` consumer proof, and one short public-launch checklist. What remains after this milestone is new milestone framing, not more closeout on the public-alpha launch gate.
