---
phase: 35-release-gate-and-matrix-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eng/ci.ps1
requirements:
  - REL-01
  - REL-03
autonomous: true
---

<objective>
Extend the repo-local CI script so it exposes an explicit contract lane and makes the release lane cover the full official verification surface, including the minimal host sample.
</objective>

<scope>
- add a `contract` lane
- run `AsterGraph.HostSample` as part of the maintained proof system
- keep package validation on the existing SDK-integrated pack path
</scope>

<must_haves>
- `eng/ci.ps1 -Lane contract` runs focused contract proof
- `eng/ci.ps1 -Lane release` remains the single official repo verification entry point
- release validation includes packed-package `HostSample`, `PackageSmoke`, and `ScaleSmoke`
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Add focused contract proof and HostSample execution to eng/ci.ps1</name>
    <files>
      eng/ci.ps1
    </files>
    <read_first>
      eng/ci.ps1
      .planning/phases/35-release-gate-and-matrix-automation/35-RESEARCH.md
    </read_first>
    <action>
      Introduce an explicit contract lane, wire the host sample into the scripted proof surface, and keep release validation centered on one command path.
    </action>
    <acceptance_criteria>
      contract lane exists
      release lane includes host sample, smoke tools, coverage, and package validation
      maintenance lane remains narrow
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane contract -Framework all -Configuration Release
- pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane release -Framework all -Configuration Release
</verification>

<output>
After completion, create `.planning/phases/35-release-gate-and-matrix-automation/35-01-SUMMARY.md`
</output>
