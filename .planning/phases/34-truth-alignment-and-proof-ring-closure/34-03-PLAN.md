---
phase: 34-truth-alignment-and-proof-ring-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 34-01
  - 34-02
files_modified:
  - tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj
  - tools/AsterGraph.HostSample/Program.cs
  - avalonia-node-map.sln
  - README.md
  - docs/quick-start.md
  - docs/host-integration.md
requirements:
  - PROOF-01
autonomous: true
---

<objective>
Restore a real minimal consumer host sample, expose it through the solution, and make the official proof ring point at it as the canonical minimal host path.
</objective>

<scope>
- add a narrow `AsterGraph.HostSample` project back under `tools/`
- support both project-reference and packed-package consumption modes
- keep the sample minimal and consumer-oriented instead of rebuilding the old giant proof app
</scope>

<must_haves>
- `tools/AsterGraph.HostSample` contains a runnable project and program
- the sample proves the canonical consumer route without depending on the demo shell
- the solution and docs expose the sample as a real maintained entry point
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Restore a narrow minimal host sample</name>
    <files>
      tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj
      tools/AsterGraph.HostSample/Program.cs
    </files>
    <read_first>
      tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj
      tools/AsterGraph.PackageSmoke/Program.cs
      .planning/phases/34-truth-alignment-and-proof-ring-closure/34-RESEARCH.md
    </read_first>
    <action>
      Reintroduce `AsterGraph.HostSample` as a small consumer-facing sample that uses the canonical factory-based route and can switch between project references and packed-package restore.
    </action>
    <acceptance_criteria>
      host sample builds and runs
      sample emits stable success output
      sample code is materially smaller and easier to read than the deleted proof-heavy host sample
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Task 2: Make the sample discoverable in solution and docs</name>
    <files>
      avalonia-node-map.sln
      README.md
      docs/quick-start.md
      docs/host-integration.md
    </files>
    <read_first>
      avalonia-node-map.sln
      README.md
      docs/quick-start.md
      docs/host-integration.md
    </read_first>
    <action>
      Add the sample to the canonical solution and wire the docs so the official proof ring points to it deliberately instead of through stale historical references.
    </action>
    <acceptance_criteria>
      solution membership lists `AsterGraph.HostSample`
      docs describe `HostSample` as the minimal consumer sample rather than as a hidden or implied artifact
      proof-ring wording stays aligned with `PackageSmoke`, `ScaleSmoke`, and the scripted release gate
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- dotnet build tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj -c Release --nologo -v minimal
- dotnet run --project tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj -c Release --nologo
- dotnet sln .\avalonia-node-map.sln list
</verification>

<output>
After completion, create `.planning/phases/34-truth-alignment-and-proof-ring-closure/34-03-SUMMARY.md`
</output>
