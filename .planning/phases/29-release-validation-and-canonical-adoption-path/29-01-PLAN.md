---
phase: 29-release-validation-and-canonical-adoption-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eng/ci.ps1
  - .github/workflows/ci.yml
  - tests/**/*.csproj
  - tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj
  - tools/AsterGraph.ScaleSmoke/AsterGraph.ScaleSmoke.csproj
  - tests/*.runsettings
  - eng/**
requirements:
  - QUAL-03
autonomous: true
---

<objective>
Add a real release-validation lane to the tracked repo-local command path so the repo can execute packed-package smoke, runtime/readiness smoke, coverage/reporting, and public API or package-compatibility checks automatically instead of relying on README-only command bundles.
</objective>

<scope>
- Extend `eng/ci.ps1` with a dedicated release-validation lane rooted in the existing script-first posture.
- Add any checked-in coverage/reporting and API/package-compatibility config the release lane needs.
- Keep `.github/workflows/ci.yml` reusing the same repo-local command path rather than duplicating logic in YAML.
</scope>

<must_haves>
- A release-validation lane exists in `eng/ci.ps1`.
- The release lane executes `PackageSmoke` and `ScaleSmoke` explicitly rather than only building their projects.
- Coverage/reporting and public API or package-compatibility checks are checked in and run automatically for the publishable packages.
- CI can call the same release lane without creating a second command story.
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Add a dedicated release-validation lane to `eng/ci.ps1`</name>
    <files>
      eng/ci.ps1
      .github/workflows/ci.yml
      tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj
      tools/AsterGraph.ScaleSmoke/AsterGraph.ScaleSmoke.csproj
    </files>
    <read_first>
      eng/ci.ps1
      .github/workflows/ci.yml
      tools/AsterGraph.PackageSmoke/Program.cs
      tools/AsterGraph.ScaleSmoke/Program.cs
      .planning/phases/29-release-validation-and-canonical-adoption-path/29-CONTEXT.md
    </read_first>
    <action>
      Extend `eng/ci.ps1` with a release-validation lane that packs the four publishable packages, runs `tools/AsterGraph.PackageSmoke` against packed packages, runs `tools/AsterGraph.ScaleSmoke`, and keeps the current split regression lanes intact. Update `.github/workflows/ci.yml` only enough to reuse that release lane once it exists.
    </action>
    <acceptance_criteria>
      `eng/ci.ps1` contains a release-validation lane entry
      `eng/ci.ps1` executes `AsterGraph.PackageSmoke`
      `eng/ci.ps1` executes `AsterGraph.ScaleSmoke`
      `pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane release -Framework all -Configuration Release` exits 0
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Task 2: Add checked-in coverage and API/package-compatibility checks to the release lane</name>
    <files>
      eng/ci.ps1
      tests/*.runsettings
      eng/**
      tests/**/*.csproj
    </files>
    <read_first>
      eng/ci.ps1
      .planning/REQUIREMENTS.md
      .planning/phases/29-release-validation-and-canonical-adoption-path/29-CONTEXT.md
      .planning/phases/29-release-validation-and-canonical-adoption-path/29-RESEARCH.md
    </read_first>
    <action>
      Add checked-in coverage/reporting configuration and public API or package-compatibility checks for the four publishable packages, then integrate them into the Phase 29 release lane so they run automatically alongside smoke execution.
    </action>
    <acceptance_criteria>
      Checked-in coverage/reporting config exists in the repo
      Checked-in API or package-compatibility config exists for the publishable packages
      `eng/ci.ps1 -Lane release` runs both check types automatically
      `AsterGraph.Abstractions` is covered by the compatibility check surface
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane release -Framework all -Configuration Release
</verification>

<output>
After completion, create `.planning/phases/29-release-validation-and-canonical-adoption-path/29-01-SUMMARY.md`
</output>
