---
phase: 06-demo
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml
  - tests/AsterGraph.Editor.Tests/GraphEditorLocalizationTests.cs
autonomous: true
requirements: [DEMO-I18N-01]
must_haves:
  truths:
    - "The stock full-shell experience reads as Simplified Chinese for user-facing Demo copy instead of mixing a Chinese outer shell with English GraphEditorView chrome."
    - "Runtime-owned captions continue to flow through the existing localization seam rather than a Demo-only string replacement path."
    - "Technical API/type identifiers remain English where they explain the actual host integration seam."
  artifacts:
    - path: "src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml"
      provides: "Chinese user-facing stock shell copy for toolbar, library, workspace, fragments, mini map, and shortcuts."
      contains: "保存快照"
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorLocalizationTests.cs"
      provides: "Focused coverage for translated shell literals and preserved API identifiers."
      contains: "保存快照"
  key_links:
    - from: "src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml"
      to: "src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs"
      via: "Runtime-owned captions and bound Chinese shell labels already exposed by plan 06-01 metadata"
      pattern: "Workspace|Fragments|Mini Map|Shortcuts"
    - from: "src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml"
      to: "src/AsterGraph.Editor/Localization/IGraphLocalizationProvider.cs"
      via: "Localized stock shell plus runtime-owned captions"
      pattern: "GetString|Localize"
---

<objective>
Complete the Chinese-first shell story by translating stock `GraphEditorView` user-facing copy and validating that the Demo continues to present runtime-owned captions through the canonical localization path established in the showcase shell.

Purpose: satisfy D-09, D-10, and D-11 without inventing a second localization system or translating away host-facing API identity.
Output: translated stock shell XAML and focused localization regression coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo/06-CONTEXT.md
@.planning/phases/06-demo/06-RESEARCH.md
@.planning/phases/06-demo/06-UI-SPEC.md
@.planning/phases/06-demo/06-01-PLAN.md
@src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml
@src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
@src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
@src/AsterGraph.Editor/Localization/IGraphLocalizationProvider.cs

<interfaces>
From `src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs`:
```csharp
private sealed class DemoGraphLocalizationProvider : IGraphLocalizationProvider
{
    private static readonly IReadOnlyDictionary<string, string> Values = new Dictionary<string, string>(StringComparer.Ordinal)
    {
        ["editor.menu.canvas.addNode"] = "添加节点",
        ["editor.inspector.title.none"] = "请选择一个节点",
    };

    public string GetString(string key, string fallback)
        => Values.TryGetValue(key, out var localized) ? localized : fallback;
}
```

From `src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs` research references:
```csharp
StatsCaption => LocalizeFormat("editor.stats.caption", "{0} nodes  ·  {1} links  ·  {2:0}% zoom", ...)
InspectorTitle => LocalizeText(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused localization regression coverage</name>
  <read_first>src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml, src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs, .planning/phases/06-demo/06-UI-SPEC.md, .planning/phases/06-demo/06-01-PLAN.md</read_first>
  <files>tests/AsterGraph.Editor.Tests/GraphEditorLocalizationTests.cs</files>
  <action>Create `GraphEditorLocalizationTests.cs` with focused assertions that the stock shell renders Chinese user-facing literals and the Demo localization provider still supplies runtime-owned captions through `IGraphLocalizationProvider`. Include exact expected strings `保存快照`, `加载快照`, `删除所选节点`, `节点库`, `工作区`, `片段`, `迷你地图`, `快捷键`, and `请选择一个节点`. Also assert that technical identifiers used in the Phase 6 showcase such as `AsterGraphCanvasViewFactory` remain English instead of being translated into pseudo-API names.</action>
  <verify>
    <automated>dotnet test "F:/CodeProjects/DotnetCore/avalonia-node-map/tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj" --filter GraphEditorLocalizationTests</automated>
  </verify>
  <acceptance_criteria>`GraphEditorLocalizationTests.cs` exists and contains assertions for the exact Chinese shell literals and for preserved English API identifiers.</acceptance_criteria>
  <done>The Phase 6 localization work now has a focused regression entry point that protects both Chinese UX copy and English technical seam names.</done>
</task>

<task type="auto">
  <name>Task 2: Translate stock GraphEditorView user-facing copy to Chinese</name>
  <read_first>src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml, src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs, .planning/phases/06-demo/06-CONTEXT.md, .planning/phases/06-demo/06-UI-SPEC.md</read_first>
  <files>src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml</files>
  <action>Translate the current English shell literals in `GraphEditorView.axaml` into simplified Chinese per D-09 and D-11 while keeping host-facing type names out of the translatable UX layer. Use concrete strings: `节点图 SDK 演示台`, `保存快照`, `加载快照`, `撤销`, `重做`, `适配视图`, `重置视图`, `删除所选节点`, `节点库`, `单击卡片可在当前视口中心附近插入节点。`, `工作区`, `片段`, `导出片段`, `导入片段`, `清空片段`, `另存为模板`, `刷新模板`, `导入模板`, `删除模板`, `迷你地图`, and `快捷键`. Rewrite the shortcut helper text into natural Chinese while preserving the exact behaviors already implemented. Do not rename `GraphEditorView`, `GraphEditorViewModel`, or any public factory/presenter types inside explanatory host-seam copy elsewhere.</action>
  <verify>
    <automated>dotnet test "F:/CodeProjects/DotnetCore/avalonia-node-map/tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj" --filter GraphEditorLocalizationTests</automated>
  </verify>
  <acceptance_criteria>`GraphEditorView.axaml` contains the listed Chinese literals and no longer contains the old English toolbar/library/workspace/fragment/shortcut labels.</acceptance_criteria>
  <done>The stock shell now presents a Chinese-first UX instead of leaking English shell chrome into the Demo.</done>
</task>

<task type="auto">
  <name>Task 3: Validate runtime-owned captions still flow through the canonical provider</name>
  <read_first>src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs, src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs, src/AsterGraph.Editor/Localization/IGraphLocalizationProvider.cs, .planning/phases/06-demo/06-RESEARCH.md</read_first>
  <files>tests/AsterGraph.Editor.Tests/GraphEditorLocalizationTests.cs</files>
  <action>Extend the focused localization assertions to verify that runtime-owned captions already surfaced by the Demo host in plan 06-01 still resolve through `IGraphLocalizationProvider.GetString(key, fallback)` and not through a new Demo-only translation path. Treat the `MainWindowViewModel` provider from plan 06-01 as the source of truth and do not modify `src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs` in this plan, which preserves file ownership and resolves the checker's parallel-safety concern.</action>
  <verify>
    <automated>dotnet test "F:/CodeProjects/DotnetCore/avalonia-node-map/tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj" --filter GraphEditorLocalizationTests</automated>
  </verify>
  <acceptance_criteria>`GraphEditorLocalizationTests.cs` contains assertions that runtime-owned captions remain sourced through `IGraphLocalizationProvider.GetString`, and `files_modified` for this plan do not include `src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs`.</acceptance_criteria>
  <done>Phase 6 localization remains anchored to the existing editor localization seam while this plan stays file-isolated from 06-01.</done>
</task>

</tasks>

<verification>
- `dotnet test "F:/CodeProjects/DotnetCore/avalonia-node-map/tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj" --filter GraphEditorLocalizationTests`
</verification>

<success_criteria>
- The stock shell is Chinese-first for end-user Demo copy.
- Runtime-owned captions remain localized through the canonical `IGraphLocalizationProvider` seam.
- Technical host-seam identifiers stay English where they help users recognize the real API path.
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo/06-02-SUMMARY.md`
</output>
