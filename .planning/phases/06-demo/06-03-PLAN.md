---
phase: 06-demo
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/AsterGraph.Demo/Views/MainWindow.axaml
  - src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
  - tests/AsterGraph.Editor.Tests/DemoDiagnosticsProjectionTests.cs
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03]
must_haves:
  truths:
    - "The Demo right rail shows machine-readable diagnostics and inspection information from `Editor.Session.Diagnostics` instead of relying on `StatusMessage` alone."
    - "Users can see recent diagnostics, capability/inspection summaries, and current runtime state in the same single-page showcase without opening a separate diagnostics workbench."
    - "The Demo explicitly teaches the canonical host seam by naming `IGraphEditorSession` and `Editor.Session.Diagnostics` in the runtime evidence area."
  artifacts:
    - path: "src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs"
      provides: "Thin runtime/diagnostics projection properties over the retained editor session."
      contains: "CaptureInspectionSnapshot"
    - path: "src/AsterGraph.Demo/Views/MainWindow.axaml"
      provides: "Right-rail runtime and diagnostics evidence cards with Chinese labels and severity-aware recent history."
      contains: "以下信息直接来自 Editor.Session.Diagnostics，而不是状态栏文案。"
    - path: "tests/AsterGraph.Editor.Tests/DemoDiagnosticsProjectionTests.cs"
      provides: "Focused regression coverage for diagnostics projection and separation from StatusMessage."
      contains: "GetRecentDiagnostics"
  key_links:
    - from: "src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs"
      to: "src/AsterGraph.Editor/Diagnostics/IGraphEditorDiagnostics.cs"
      via: "Inspection snapshot and recent-diagnostics projection"
      pattern: "CaptureInspectionSnapshot|GetRecentDiagnostics"
    - from: "src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs"
      to: "src/AsterGraph.Demo/Views/MainWindow.axaml"
      via: "Right-rail bindings for runtime state and recent diagnostic rows"
      pattern: "运行时与诊断|能力证明"
---

<objective>
Project the canonical diagnostics and inspection APIs into the Demo right rail so the Phase 6 showcase makes runtime state, capability snapshots, and recent diagnostics visible from the retained editor session.

Purpose: satisfy D-08 while preserving the Phase 5 contract story that diagnostics live in `AsterGraph.Editor`, not in a Demo-only status panel or separate diagnostics workbench.
Output: session-backed diagnostics projection properties, right-rail evidence cards, and focused regression coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo/06-CONTEXT.md
@.planning/phases/06-demo/06-RESEARCH.md
@.planning/phases/06-demo/06-UI-SPEC.md
@.planning/phases/05-diagnostics-integration-inspection/05-CONTEXT.md
@.planning/phases/05-diagnostics-integration-inspection/05-04-SUMMARY.md
@src/AsterGraph.Demo/Views/MainWindow.axaml
@src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
@src/AsterGraph.Editor/Diagnostics/IGraphEditorDiagnostics.cs
@src/AsterGraph.Editor/Diagnostics/GraphEditorInspectionSnapshot.cs
@tools/AsterGraph.HostSample/Program.cs

<interfaces>
From `tools/AsterGraph.HostSample/Program.cs`:
```csharp
var retainedSession = editor.Session;
var retainedInspection = retainedSession.Diagnostics.CaptureInspectionSnapshot();
var retainedRecentDiagnostics = retainedSession.Diagnostics.GetRecentDiagnostics(10);

Console.WriteLine($"Retained inspection snapshot: nodes={retainedInspection.Document.Nodes.Count}, selected={retainedInspection.Selection.SelectedNodeIds.Count}, pending={retainedInspection.PendingConnection.HasPendingConnection}");
Console.WriteLine($"Retained recent diagnostics count: {retainedRecentDiagnostics.Count}");
Console.WriteLine($"StatusMessage compatibility surface (not canonical diagnostics API): {editor.StatusMessage}");
```

From Phase 5 contract shape:
```csharp
public interface IGraphEditorDiagnostics
{
    GraphEditorInspectionSnapshot CaptureInspectionSnapshot();
    IReadOnlyList<GraphEditorDiagnostic> GetRecentDiagnostics(int maxCount);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused diagnostics projection regression coverage</name>
  <read_first>src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs, tools/AsterGraph.HostSample/Program.cs, .planning/phases/05-diagnostics-integration-inspection/05-CONTEXT.md, .planning/phases/06-demo/06-UI-SPEC.md</read_first>
  <files>tests/AsterGraph.Editor.Tests/DemoDiagnosticsProjectionTests.cs</files>
  <action>Create `DemoDiagnosticsProjectionTests.cs` with focused assertions that the Demo ViewModel reads real data from `Editor.Session.Diagnostics.CaptureInspectionSnapshot()` and `Editor.Session.Diagnostics.GetRecentDiagnostics(...)` instead of synthesizing placeholder diagnostics. Assert the exact helper text `以下信息直接来自 Editor.Session.Diagnostics，而不是状态栏文案。` appears in the bound runtime evidence path, and assert that `StatusMessage` remains separate compatibility UX rather than the sole diagnostics source.</action>
  <verify>
    <automated>dotnet test "F:/CodeProjects/DotnetCore/avalonia-node-map/tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj" --filter DemoDiagnosticsProjectionTests</automated>
  </verify>
  <acceptance_criteria>`DemoDiagnosticsProjectionTests.cs` exists; it mentions `CaptureInspectionSnapshot`, `GetRecentDiagnostics`, and validates separation between diagnostics projection and `StatusMessage`.</acceptance_criteria>
  <done>The Phase 6 diagnostics showcase has focused regression coverage before UI wiring begins.</done>
</task>

<task type="auto">
  <name>Task 2: Expose retained-session inspection and recent diagnostics through thin ViewModel projections</name>
  <read_first>src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs, src/AsterGraph.Editor/Diagnostics/IGraphEditorDiagnostics.cs, tools/AsterGraph.HostSample/Program.cs, .planning/phases/06-demo/06-RESEARCH.md</read_first>
  <files>src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs</files>
  <action>Add thin, runtime-backed projection properties over the existing `Editor.Session` for the right-rail proof area. Pull concrete values from `CaptureInspectionSnapshot()` and `GetRecentDiagnostics(10)` to expose document title, node/link counts, selected node ids, viewport zoom/pan, pending connection state, capabilities, and a bounded recent diagnostics list with severity, code, operation, and message. Add helper properties that name the canonical seam as `IGraphEditorSession` and `Editor.Session.Diagnostics`. Do not add fake DTO stores, mock diagnostics collections, or a second session model; format directly over the retained session and refresh through existing editor/session signals where needed.</action>
  <verify>
    <automated>dotnet test "F:/CodeProjects/DotnetCore/avalonia-node-map/tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj" --filter DemoDiagnosticsProjectionTests</automated>
  </verify>
  <acceptance_criteria>`MainWindowViewModel.cs` contains calls to `Editor.Session.Diagnostics.CaptureInspectionSnapshot()` and `Editor.Session.Diagnostics.GetRecentDiagnostics(10)` or equivalent bounded count access, and contains no hard-coded placeholder diagnostics list.</acceptance_criteria>
  <done>The Demo ViewModel can now surface real runtime and diagnostics evidence from the retained session.</done>
</task>

<task type="auto">
  <name>Task 3: Bind runtime and diagnostics evidence cards into the right rail</name>
  <read_first>src/AsterGraph.Demo/Views/MainWindow.axaml, src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs, .planning/phases/06-demo/06-UI-SPEC.md</read_first>
  <files>src/AsterGraph.Demo/Views/MainWindow.axaml, src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs</files>
  <action>Extend the right rail from plan `06-01` with `运行时与诊断` and `能力证明` sections per D-08. Show the exact helper sentence `以下信息直接来自 Editor.Session.Diagnostics，而不是状态栏文案。`, then render a compact inspection summary card and a recent diagnostics list with severity-aware styling and machine-readable fields visible (`Code`, `Severity`, `Operation`, `Message`). Keep `StatusMessage` visible only as compatibility UX if it remains on screen; do not present it as the proof source. Use Chinese labels for the card rows while preserving English technical identifiers where they name the canonical seam.</action>
  <verify>
    <automated>dotnet test "F:/CodeProjects/DotnetCore/avalonia-node-map/tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj" --filter "DemoDiagnosticsProjectionTests|DemoMainWindowTests"</automated>
  </verify>
  <acceptance_criteria>`MainWindow.axaml` contains `运行时与诊断`, `能力证明`, and the exact helper sentence from the UI-SPEC; the right rail binds a recent diagnostics list that exposes machine-readable fields; the file does not rely on `StatusMessage` alone as the only diagnostics evidence.</acceptance_criteria>
  <done>The Demo now teaches and proves the canonical diagnostics surface through live right-rail evidence instead of hidden code or status-only text.</done>
</task>

</tasks>

<verification>
- `dotnet test "F:/CodeProjects/DotnetCore/avalonia-node-map/tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj" --filter DemoDiagnosticsProjectionTests`
- `dotnet test "F:/CodeProjects/DotnetCore/avalonia-node-map/tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj" --filter "DemoDiagnosticsProjectionTests|DemoMainWindowTests"`
</verification>

<success_criteria>
- The Demo right rail exposes real inspection snapshot and recent diagnostics data from `Editor.Session.Diagnostics`.
- Users can distinguish machine-readable diagnostics from compatibility status text.
- The Demo teaches the canonical runtime seam without creating a separate diagnostics workbench.
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo/06-03-SUMMARY.md`
</output>
