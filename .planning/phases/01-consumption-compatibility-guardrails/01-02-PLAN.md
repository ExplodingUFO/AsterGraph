---
phase: 01-consumption-compatibility-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
  - src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
  - src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewOptions.cs
  - src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewFactory.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs
  - tools/AsterGraph.HostSample/Program.cs
autonomous: true
requirements:
  - PKG-02
must_haves:
  truths:
    - "A host can create a configured `GraphEditorViewModel` through a public AsterGraph-owned options/factory API instead of copying demo composition."
    - "A host can create the default Avalonia `GraphEditorView` through a public factory/options API while keeping the direct `new GraphEditorView { Editor = ... }` route available."
    - "The new initialization surface carries the same style, behavior, workspace, localization, presentation, and menu seams already supported by the constructor path."
  artifacts:
    - path: "src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs"
      provides: "Host-facing editor composition contract."
    - path: "src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs"
      provides: "Canonical editor runtime creation entry point."
    - path: "src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewOptions.cs"
      provides: "Host-facing Avalonia view composition contract."
    - path: "src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewFactory.cs"
      provides: "Canonical default Avalonia view creation entry point."
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs"
      provides: "Regression coverage for the new public initialization APIs."
  key_links:
    - from: "src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs"
      to: "src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs"
      via: "Single composition root delegating to the existing compatibility facade"
      pattern: "new GraphEditorViewModel"
    - from: "src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewFactory.cs"
      to: "src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs"
      via: "Factory-created view wiring `Editor` and `ChromeMode`"
      pattern: "new GraphEditorView"
    - from: "tools/AsterGraph.HostSample/Program.cs"
      to: "new public factories/options"
      via: "Reference host composition"
      pattern: "AsterGraphEditorFactory|AsterGraphAvaloniaViewFactory"
---

<objective>
Introduce the formal public initialization surface for Phase 1 without breaking the current constructor-driven host path.

Purpose: Satisfy D-03 while preserving D-04 and D-05 by adding one canonical composition source that wraps the existing compatibility facades.
Output: Public editor and Avalonia options/factory APIs, host-sample usage of those APIs, and targeted initialization regression coverage.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-consumption-compatibility-guardrails/01-CONTEXT.md
@.planning/phases/01-consumption-compatibility-guardrails/01-RESEARCH.md
@src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
@src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs
@tools/AsterGraph.HostSample/Program.cs
@tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs

<interfaces>
From `src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs`:
```csharp
public GraphEditorViewModel(
    GraphDocument document,
    INodeCatalog nodeCatalog,
    IPortCompatibilityService compatibilityService,
    GraphWorkspaceService? workspaceService = null,
    GraphFragmentWorkspaceService? fragmentWorkspaceService = null,
    GraphEditorStyleOptions? styleOptions = null,
    GraphEditorBehaviorOptions? behaviorOptions = null,
    GraphFragmentLibraryService? fragmentLibraryService = null,
    IGraphContextMenuAugmentor? contextMenuAugmentor = null,
    INodePresentationProvider? nodePresentationProvider = null,
    IGraphLocalizationProvider? localizationProvider = null)
```

From `src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs`:
```csharp
public GraphEditorViewModel? Editor { get; set; }
public GraphEditorViewChromeMode ChromeMode { get; set; }
public GraphEditorView()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define host-facing initialization contracts for editor and Avalonia composition</name>
  <read_first>
    .planning/phases/01-consumption-compatibility-guardrails/01-CONTEXT.md
    .planning/phases/01-consumption-compatibility-guardrails/01-RESEARCH.md
    src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
    src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs
  </read_first>
  <files>src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs, src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs, src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewOptions.cs, src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewFactory.cs</files>
  <action>Create a factory-first public initialization contract per D-03. Put the new APIs under existing `Hosting` namespaces. `AsterGraphEditorOptions` must carry the current host-composition inputs from the `GraphEditorViewModel` constructor: `GraphDocument`, `INodeCatalog`, `IPortCompatibilityService`, optional workspace/fragment services, style options, behavior options, fragment library service, context-menu augmentor, node-presentation provider, and localization provider. `AsterGraphEditorFactory.Create(...)` must validate required inputs and delegate to the existing constructor without duplicating default logic. `AsterGraphAvaloniaViewOptions` must at least expose `GraphEditorViewChromeMode`, and `AsterGraphAvaloniaViewFactory.Create(...)` must return a `GraphEditorView` with `Editor` and `ChromeMode` applied. Keep both factories free of demo-specific assumptions and note in XML docs that direct construction remains supported per D-04 and D-05.</action>
  <verify>
    <automated>dotnet build avalonia-node-map.sln -v minimal</automated>
  </verify>
  <acceptance_criteria>The new options and factory types compile, expose the existing host seams as first-class inputs, and route all creation through the current `GraphEditorViewModel`/`GraphEditorView` compatibility facades rather than introducing a second behavior path.</acceptance_criteria>
  <done>Phase 1 has a canonical public construction API in `AsterGraph.Editor` and `AsterGraph.Avalonia`.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the new initialization surface through tests and the host sample</name>
  <read_first>
    tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs
    tools/AsterGraph.HostSample/Program.cs
    src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
    src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewFactory.cs
  </read_first>
  <files>tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs, tools/AsterGraph.HostSample/Program.cs</files>
  <action>Fill `GraphEditorInitializationTests.cs` with real regression coverage for PKG-02. Cover at minimum: required inputs are validated, style/behavior/provider seams are forwarded unchanged through `AsterGraphEditorFactory.Create(...)`, and `AsterGraphAvaloniaViewFactory.Create(...)` returns a `GraphEditorView` wired to the supplied editor with the requested `ChromeMode`. Update `tools/AsterGraph.HostSample/Program.cs` to use the new factories/options as the canonical composition example while preserving a concise comment or local note that the direct constructor route remains supported for migration per D-04.</action>
  <verify>
    <automated>dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorInitializationTests" -v minimal</automated>
  </verify>
  <acceptance_criteria>The targeted initialization tests pass, and the host sample demonstrates the new official composition path without losing existing host seams such as localization, node presentation, menu augmentation, style options, and behavior options.</acceptance_criteria>
  <done>Hosts have a public, tested, non-demo initialization path for both the editor runtime and the default Avalonia view.</done>
</task>

</tasks>

<verification>
- `dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorInitializationTests" -v minimal`
- `dotnet run --project tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj`
</verification>

<success_criteria>
- New host-facing factories/options exist in the publishable libraries.
- The initialization surface forwards all currently supported host seams.
- The host sample demonstrates the new canonical path while the old constructor path remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/01-consumption-compatibility-guardrails/01-02-SUMMARY.md`
</output>
