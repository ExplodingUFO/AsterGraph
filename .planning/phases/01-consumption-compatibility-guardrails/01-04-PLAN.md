---
phase: 01-consumption-compatibility-guardrails
plan: 04
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
files_modified:
  - README.md
  - docs/host-integration.md
  - src/AsterGraph.Abstractions/README.md
  - src/AsterGraph.Core/README.md
  - src/AsterGraph.Editor/README.md
  - src/AsterGraph.Avalonia/README.md
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
must_haves:
  truths:
    - "Hosts can identify the supported AsterGraph package boundary and target frameworks from one consistent documentation story."
    - "Hosts can see the canonical factory-based initialization path and the still-supported constructor-based compatibility path in the same migration guide."
    - "Packed-package and sample-host verification prove the documented story matches the shipped packages."
  artifacts:
    - path: "README.md"
      provides: "Top-level supported package boundary, target-framework support story, quick-start, and migration framing."
    - path: "docs/host-integration.md"
      provides: "Detailed canonical initialization and staged migration guide for external hosts."
    - path: "src/AsterGraph.Abstractions/README.md"
      provides: "Contract-package positioning inside the supported package set."
    - path: "src/AsterGraph.Core/README.md"
      provides: "Model/serialization package positioning inside the supported package set."
    - path: "src/AsterGraph.Editor/README.md"
      provides: "Standard host-facing runtime package positioning per D-01."
    - path: "src/AsterGraph.Avalonia/README.md"
      provides: "Default UI package positioning relative to the new editor initialization surface."
  key_links:
    - from: "README.md"
      to: "src/AsterGraph.Editor/README.md"
      via: "Consistent package-boundary narrative"
      pattern: "AsterGraph.Editor"
    - from: "docs/host-integration.md"
      to: "src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs"
      via: "Canonical code snippet and migration guidance"
      pattern: "AsterGraphEditorFactory"
    - from: "packed packages"
      to: "tools/AsterGraph.PackageSmoke"
      via: "Published boundary verification"
      pattern: "UsePackedAsterGraphPackages=true"
---

<objective>
Publish one consistent external-consumption story for Phase 1 and close the phase with package-boundary verification.

Purpose: Satisfy PKG-01 and make the new init/migration story discoverable and trustworthy to external hosts.
Output: Aligned top-level and package READMEs, updated host guide, and final automated verification across tests, host sample, and packed-package smoke.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-consumption-compatibility-guardrails/01-CONTEXT.md
@.planning/phases/01-consumption-compatibility-guardrails/01-RESEARCH.md
@README.md
@docs/host-integration.md
@src/AsterGraph.Abstractions/README.md
@src/AsterGraph.Core/README.md
@src/AsterGraph.Editor/README.md
@src/AsterGraph.Avalonia/README.md
@tools/AsterGraph.HostSample/Program.cs
@tools/AsterGraph.PackageSmoke/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the package boundary and migration story across all consumer docs</name>
  <read_first>
    .planning/phases/01-consumption-compatibility-guardrails/01-CONTEXT.md
    README.md
    docs/host-integration.md
    src/AsterGraph.Abstractions/README.md
    src/AsterGraph.Core/README.md
    src/AsterGraph.Editor/README.md
    src/AsterGraph.Avalonia/README.md
  </read_first>
  <files>README.md, docs/host-integration.md, src/AsterGraph.Abstractions/README.md, src/AsterGraph.Core/README.md, src/AsterGraph.Editor/README.md, src/AsterGraph.Avalonia/README.md</files>
  <action>Update every consumer-facing doc so the package story is internally consistent per D-01 and D-02. The root README must name the four publishable packages, supported `net8.0`/`net9.0` target story, and the non-consumable status of `AsterGraph.Demo`. The host guide must present the new factory/options path as canonical while explicitly documenting the retained constructor path as a staged migration route per D-04 through D-06. Package READMEs must agree on when `AsterGraph.Editor` is a standard host-facing dependency rather than a merely optional deep-integration detail.</action>
  <verify>
    <automated>rg -n "AsterGraph.Abstractions|AsterGraph.Core|AsterGraph.Editor|AsterGraph.Avalonia|factory|migration|net8.0|net9.0|AsterGraph.Demo" README.md docs/host-integration.md src/AsterGraph.Abstractions/README.md src/AsterGraph.Core/README.md src/AsterGraph.Editor/README.md src/AsterGraph.Avalonia/README.md</automated>
  </verify>
  <acceptance_criteria>The root README, host guide, and package READMEs no longer disagree about the supported package set, the role of `AsterGraph.Editor`, or the staged migration path.</acceptance_criteria>
  <done>External hosts can read any first-party AsterGraph doc and get the same package-boundary and migration guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full Phase 1 verification ring against source and packed packages</name>
  <read_first>
    .planning/phases/01-consumption-compatibility-guardrails/01-VALIDATION.md
    tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj
    tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj
    README.md
  </read_first>
  <files>README.md, docs/host-integration.md, src/AsterGraph.Abstractions/README.md, src/AsterGraph.Core/README.md, src/AsterGraph.Editor/README.md, src/AsterGraph.Avalonia/README.md</files>
  <action>Use the final documentation state to drive the Phase 1 verification ring for PKG-01 through PKG-03, but keep the long multi-command ring in plan-level verification rather than as a single task-level check. For this task, first run the targeted editor regression command to confirm the documented init and migration story still matches the source surface. Then complete the full plan verification ring: run the full solution tests, pack all four publishable libraries to `artifacts/packages`, execute the packed-package smoke with `UsePackedAsterGraphPackages=true`, and finally run the host sample. If any command fails because the docs or package story still drift from the shipped surface, fix the documented artifacts in this plan before closing it.</action>
  <verify>
    <automated>dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorInitializationTests|FullyQualifiedName~GraphEditorMigrationCompatibilityTests" -v minimal</automated>
  </verify>
  <acceptance_criteria>The full solution passes tests, all four packages pack successfully, the packed-package smoke runs against the local feed, and the host sample still runs with the documented public entry story.</acceptance_criteria>
  <done>Phase 1 is externally verifiable: documentation, samples, and packed packages all agree on the supported consumption and migration path.</done>
</task>

</tasks>

<verification>
- `dotnet test avalonia-node-map.sln -v minimal`
- `dotnet pack src/AsterGraph.Abstractions/AsterGraph.Abstractions.csproj -c Release -o artifacts/packages`
- `dotnet pack src/AsterGraph.Core/AsterGraph.Core.csproj -c Release -o artifacts/packages`
- `dotnet pack src/AsterGraph.Editor/AsterGraph.Editor.csproj -c Release -o artifacts/packages`
- `dotnet pack src/AsterGraph.Avalonia/AsterGraph.Avalonia.csproj -c Release -o artifacts/packages`
- `dotnet run --project tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj -p:UsePackedAsterGraphPackages=true`
- `dotnet run --project tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj`
</verification>

<success_criteria>
- The four publishable packages have one documented and verified consumption boundary.
- The new factory-based initialization path and retained compatibility path are both documented.
- Source tests, packed-package smoke, and host-sample verification all pass against the same story.
</success_criteria>

<output>
After completion, create `.planning/phases/01-consumption-compatibility-guardrails/01-04-SUMMARY.md`
</output>
