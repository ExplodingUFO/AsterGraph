---
phase: 01-consumption-compatibility-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Directory.Build.props
  - tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
must_haves:
  truths:
    - "Phase verification commands run without duplicate assembly-attribute failures from committed audit artifacts."
    - "Initialization and migration regression suites exist as concrete test entry points instead of remaining implicit validation gaps."
  artifacts:
    - path: "Directory.Build.props"
      provides: "Repository-wide compile-item guardrail that excludes generated audit outputs under src/**/artifacts/**."
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs"
      provides: "Named initialization regression suite for the new public construction APIs."
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs"
      provides: "Named staged-migration regression suite covering old and new host entry paths."
  key_links:
    - from: "Directory.Build.props"
      to: "src/**/artifacts/audit/**"
      via: "MSBuild compile exclusion"
      pattern: "artifacts/**/*.cs"
    - from: "tests/AsterGraph.Editor.Tests/*.cs"
      to: "01-VALIDATION.md"
      via: "Named test suites referenced by phase verification commands"
      pattern: "GraphEditorInitializationTests|GraphEditorMigrationCompatibilityTests"
---

<objective>
Stabilize the Phase 1 execution baseline before any public API changes land.

Purpose: Remove the known build blocker and create the missing targeted test entry points that later plans will implement against.
Output: A compile-safe solution, plus explicit initialization and migration test files wired into the existing editor test project.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-consumption-compatibility-guardrails/01-CONTEXT.md
@.planning/phases/01-consumption-compatibility-guardrails/01-RESEARCH.md
@.planning/phases/01-consumption-compatibility-guardrails/01-VALIDATION.md
@Directory.Build.props
@tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj
@tests/AsterGraph.Editor.Tests/GraphEditorFacadeRefactorTests.cs
@tests/AsterGraph.Editor.Tests/GraphEditorViewTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove generated audit outputs from default compile items</name>
  <read_first>
    .planning/phases/01-consumption-compatibility-guardrails/01-RESEARCH.md
    .planning/phases/01-consumption-compatibility-guardrails/01-VALIDATION.md
    Directory.Build.props
  </read_first>
  <files>Directory.Build.props</files>
  <action>Add a repository-wide MSBuild exclusion for generated C# files under `src/**/artifacts/**` so the committed audit snapshots no longer participate in SDK default compile globs. Limit the exclusion to generated build outputs per the Wave 0 blocker in research, keep package README packing untouched, and document the intent with one short comment because this guardrail is product-critical for PKG-01 verification.</action>
  <verify>
    <automated>dotnet test avalonia-node-map.sln -v minimal</automated>
  </verify>
  <acceptance_criteria>The solution test command reaches normal compilation/test execution without `CS0579` duplicate assembly attribute errors from files under `src/**/artifacts/audit/**`.</acceptance_criteria>
  <done>Build hygiene is restored and Phase 1 verification commands are no longer blocked by committed audit artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Create explicit initialization and migration regression suites</name>
  <read_first>
    .planning/phases/01-consumption-compatibility-guardrails/01-VALIDATION.md
    tests/AsterGraph.Editor.Tests/GraphEditorFacadeRefactorTests.cs
    tests/AsterGraph.Editor.Tests/GraphEditorViewTests.cs
  </read_first>
  <files>tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs, tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs</files>
  <action>Create two new xUnit files in `tests/AsterGraph.Editor.Tests` with compile-safe suite skeletons and concrete method names matching the phase validation contract. `GraphEditorInitializationTests` must name the factory/options and default Avalonia-view behaviors that Plan 01-02 will implement for D-03. `GraphEditorMigrationCompatibilityTests` must name staged old-vs-new parity scenarios for D-04 through D-06. Use the repo's local helper/factory style, keep the files compiling immediately, and mark any not-yet-implemented cases with explicit skip messages instead of placeholder assertions so later plans can fill them in deterministically.</action>
  <verify>
    <automated>dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorInitializationTests|FullyQualifiedName~GraphEditorMigrationCompatibilityTests" -v minimal</automated>
  </verify>
  <acceptance_criteria>Both files exist, compile in the existing editor test project, and expose named test cases for the exact PKG-02 and PKG-03 behaviors referenced by `01-VALIDATION.md`.</acceptance_criteria>
  <done>Wave 0 no longer has missing test-file references; later plans can implement against explicit test entry points instead of creating them ad hoc.</done>
</task>

</tasks>

<verification>
Run the Wave 0 validation ring before moving to dependent plans:
- `dotnet test avalonia-node-map.sln -v minimal`
- `dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorInitializationTests|FullyQualifiedName~GraphEditorMigrationCompatibilityTests" -v minimal`
</verification>

<success_criteria>
- The repository no longer compiles generated audit C# files from committed `artifacts` folders.
- The phase has explicit initialization and migration regression files matching the validation contract.
- Dependent plans can assume a clean build baseline and concrete test targets.
</success_criteria>

<output>
After completion, create `.planning/phases/01-consumption-compatibility-guardrails/01-01-SUMMARY.md`
</output>
