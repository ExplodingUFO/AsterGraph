---
phase: 01-consumption-compatibility-guardrails
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
  - src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
  - tools/AsterGraph.PackageSmoke/Program.cs
autonomous: true
requirements:
  - PKG-03
must_haves:
  truths:
    - "Existing hosts can stay on `new GraphEditorViewModel(...)` plus `new GraphEditorView { Editor = ... }` without source breakage."
    - "New factory-based entry paths and the legacy constructor-based paths exhibit equivalent observable behavior for the shared host seams."
    - "The migration story is enforceable by tests and smoke consumers, not only by prose."
  artifacts:
    - path: "src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs"
      provides: "Compatibility-facade documentation for the retained constructor path."
    - path: "src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs"
      provides: "Compatibility-facade documentation for the retained view path."
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs"
      provides: "Parity regression suite for legacy and new host composition paths."
    - path: "tools/AsterGraph.PackageSmoke/Program.cs"
      provides: "Compile/runtime smoke covering both migration stages."
  key_links:
    - from: "tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs"
      to: "src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs"
      via: "New composition path parity assertions"
      pattern: "AsterGraphEditorFactory"
    - from: "tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs"
      to: "src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs"
      via: "Legacy composition path parity assertions"
      pattern: "new GraphEditorViewModel"
    - from: "tools/AsterGraph.PackageSmoke/Program.cs"
      to: "packed publishable packages"
      via: "Consumer smoke for staged migration"
      pattern: "constructor|factory"
---

<objective>
Turn the Phase 1 migration promise into a tested compatibility contract.

Purpose: Satisfy D-04 through D-06 by keeping the old host path alive, documenting it as the compatibility facade, and proving parity against the new factory-based path.
Output: Compatibility-facing XML docs on the retained entry points, migration regression tests, and smoke validation for both adoption stages.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-consumption-compatibility-guardrails/01-CONTEXT.md
@.planning/phases/01-consumption-compatibility-guardrails/01-RESEARCH.md
@src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
@src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs
@src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
@src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewFactory.cs
@tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
@tools/AsterGraph.PackageSmoke/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the retained constructor and view paths as the Phase 1 compatibility facade</name>
  <read_first>
    .planning/phases/01-consumption-compatibility-guardrails/01-CONTEXT.md
    src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
    src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs
    src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
  </read_first>
  <files>src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs, src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs</files>
  <action>Update XML docs and remarks on the existing public constructor/view members to make the Phase 1 migration contract explicit. State that `GraphEditorViewModel` construction and `GraphEditorView` with `Editor` assignment remain supported compatibility facades per D-04 and D-05, while new integrations should prefer the factory/options APIs introduced in Plan 01-02. Do not obsolete or remove the current APIs in this phase; the goal is staged guidance, not forced rewrites.</action>
  <verify>
    <automated>dotnet build avalonia-node-map.sln -v minimal</automated>
  </verify>
  <acceptance_criteria>The retained public entry points have clear IDE-visible migration guidance and continue compiling without signature changes or warning spam that would contradict Phase 1's additive strategy.</acceptance_criteria>
  <done>Existing hosts have explicit source-level guidance that their current path remains supported during the migration window.</done>
</task>

<task type="auto">
  <name>Task 2: Add parity regression and smoke coverage for staged migration</name>
  <read_first>
    tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
    tools/AsterGraph.PackageSmoke/Program.cs
    src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
    src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewFactory.cs
  </read_first>
  <files>tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs, tools/AsterGraph.PackageSmoke/Program.cs</files>
  <action>Implement `GraphEditorMigrationCompatibilityTests.cs` so it proves old and new entry paths are behaviorally aligned for the shared host seams. Cover at minimum: constructor path still builds the editor with style/behavior/provider inputs, factory path yields equivalent observable state, and Avalonia view creation still binds `Editor`/`ChromeMode` correctly across both routes. Extend `tools/AsterGraph.PackageSmoke/Program.cs` so the smoke consumer touches both the retained constructor facade and the new factory surface, printing unambiguous markers for each path. Keep the smoke project limited to the publishable package set per D-02.</action>
  <verify>
    <automated>dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorMigrationCompatibilityTests" -v minimal</automated>
  </verify>
  <acceptance_criteria>The migration regression suite passes, and the package smoke executable compiles against both migration stages without requiring `AsterGraph.Demo` or other non-publishable assets.</acceptance_criteria>
  <done>PKG-03 is enforced by automated parity coverage and package-consumption smoke instead of relying only on documentation.</done>
</task>

</tasks>

<verification>
- `dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorMigrationCompatibilityTests" -v minimal`
- `dotnet run --project tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj`
</verification>

<success_criteria>
- The legacy constructor/view path remains source-compatible and explicitly documented as supported.
- Automated tests prove parity between legacy and new entry paths.
- Package smoke validates both migration stages against the publishable package set.
</success_criteria>

<output>
After completion, create `.planning/phases/01-consumption-compatibility-guardrails/01-03-SUMMARY.md`
</output>
