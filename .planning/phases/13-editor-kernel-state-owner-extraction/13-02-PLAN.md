---
phase: 13-editor-kernel-state-owner-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
  - src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
  - src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorTransactionTests.cs
requirements:
  - KERN-01
  - KERN-02
autonomous: true
---

<objective>
Make session/runtime composition kernel-first so `CreateSession(...)` no longer constructs `GraphEditorViewModel` as the canonical runtime state owner.
</objective>

<scope>
- Rebuild `GraphEditorSession` around kernel-owned state and operations rather than direct `GraphEditorViewModel` ownership.
- Update `AsterGraphEditorFactory.CreateSession(...)` to compose the runtime from the kernel path.
- Preserve public `IGraphEditorSession` behavior and compatibility shims while ownership changes under the hood.
</scope>

<verification>
- `dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorSessionTests|FullyQualifiedName~GraphEditorTransactionTests|FullyQualifiedName~GraphEditorDiagnosticsInspectionTests" -v minimal`
</verification>

<output>
After completion, create `.planning/phases/13-editor-kernel-state-owner-extraction/13-02-SUMMARY.md`
</output>

