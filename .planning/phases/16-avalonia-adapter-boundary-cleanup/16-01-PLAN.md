---
phase: 16-avalonia-adapter-boundary-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs
  - src/AsterGraph.Avalonia/Controls/NodeCanvas.axaml.cs
  - src/AsterGraph.Avalonia/Controls/Internal/
  - src/AsterGraph.Avalonia/Menus/GraphContextMenuPresenter.cs
  - src/AsterGraph.Avalonia/Presentation/IGraphContextMenuPresenter.cs
  - tests/AsterGraph.Editor.Tests/GraphContextMenuPresenterTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorViewTests.cs
  - tests/AsterGraph.Editor.Tests/NodeCanvasStandaloneTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs
requirements:
  - ADAPT-01
autonomous: true
---

<objective>
Collapse Avalonia's duplicated default shortcut and stock context-menu policy into shared adapters that consume canonical session command/query seams.
</objective>

<scope>
- Remove duplicated save/load/undo/redo/copy/paste/delete shortcut chains from `GraphEditorView` and `NodeCanvas`.
- Make the stock Avalonia context-menu flow descriptor-first on the session/query path instead of treating `GraphEditorViewModel.BuildContextMenu(...)` as the stock source of truth.
- Preserve current opt-out flags and compatibility entry paths while thinning the internal adapter boundary.
</scope>

<read_first>
- src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs
- src/AsterGraph.Avalonia/Controls/NodeCanvas.axaml.cs
- src/AsterGraph.Avalonia/Menus/GraphContextMenuPresenter.cs
- src/AsterGraph.Avalonia/Presentation/IGraphContextMenuPresenter.cs
- src/AsterGraph.Editor/Runtime/IGraphEditorSession.cs
- src/AsterGraph.Editor/Runtime/IGraphEditorCommands.cs
- src/AsterGraph.Editor/Runtime/IGraphEditorQueries.cs
- src/AsterGraph.Editor/Menus/GraphEditorMenuItemDescriptorSnapshot.cs
- tests/AsterGraph.Editor.Tests/GraphContextMenuPresenterTests.cs
- tests/AsterGraph.Editor.Tests/GraphEditorViewTests.cs
</read_first>

<action>
- Introduce one or more internal Avalonia routing helpers for default keyboard shortcuts and stock context-menu opening so shell and canvas stop carrying near-identical control-plane code.
- Extend the stock menu-presenter path so it can render canonical menu descriptors directly or through a narrow canonical adapter, keeping compatibility `MenuItemDescriptor` handling only where migration still needs it.
- Update `GraphEditorView` and `NodeCanvas` to use shared routing helpers over `Editor.Session` / `ViewModel.Session` instead of directly branching on compatibility `ICommand` properties as the stock behavior path.
- Keep focus checks, opt-out flags, and compatibility `GraphEditorView` entry behavior intact through targeted regression coverage.
</action>

<acceptance_criteria>
- default save/load/undo/redo/copy/paste/delete shortcut routing is no longer duplicated across `GraphEditorView.axaml.cs` and `NodeCanvas.axaml.cs`
- stock Avalonia context menus are opened from canonical session/query descriptor data rather than `GraphEditorViewModel.BuildContextMenu(...)`
- `EnableDefaultContextMenu` and `EnableDefaultCommandShortcuts` still work for full-shell and standalone-canvas composition
- focused Avalonia presenter/view/canvas regressions pass
</acceptance_criteria>

<verification>
- dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphContextMenuPresenterTests|FullyQualifiedName~GraphEditorViewTests|FullyQualifiedName~NodeCanvasStandaloneTests|FullyQualifiedName~GraphEditorInitializationTests" -v minimal
</verification>

<output>
After completion, create `.planning/phases/16-avalonia-adapter-boundary-cleanup/16-01-SUMMARY.md`
</output>
