---
phase: 04-replaceable-presentation-kit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AsterGraph.Avalonia/Presentation/IGraphNodeVisualPresenter.cs
  - src/AsterGraph.Avalonia/Presentation/IGraphContextMenuPresenter.cs
  - src/AsterGraph.Avalonia/Presentation/IGraphInspectorPresenter.cs
  - src/AsterGraph.Avalonia/Presentation/IGraphMiniMapPresenter.cs
  - src/AsterGraph.Avalonia/Presentation/AsterGraphPresentationOptions.cs
  - src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewOptions.cs
  - src/AsterGraph.Avalonia/Hosting/AsterGraphCanvasViewOptions.cs
  - src/AsterGraph.Avalonia/Hosting/AsterGraphInspectorViewOptions.cs
  - src/AsterGraph.Avalonia/Hosting/AsterGraphMiniMapViewOptions.cs
  - src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewFactory.cs
  - src/AsterGraph.Avalonia/Hosting/AsterGraphCanvasViewFactory.cs
  - src/AsterGraph.Avalonia/Hosting/AsterGraphInspectorViewFactory.cs
  - src/AsterGraph.Avalonia/Hosting/AsterGraphMiniMapViewFactory.cs
  - src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml
  - src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs
autonomous: true
requirements: [PRES-01, PRES-02, PRES-03, PRES-04]
must_haves:
  truths:
    - "Host can opt into presenter replacement per Avalonia surface without changing AsterGraph.Editor."
    - "Omitting replacement presenters preserves the existing stock full-shell and standalone-surface behavior."
    - "Full-shell and standalone factory/options entry points stay canonical for presenter configuration."
  artifacts:
    - path: "src/AsterGraph.Avalonia/Presentation/AsterGraphPresentationOptions.cs"
      provides: "Shared per-surface presenter option container in AsterGraph.Avalonia"
      contains: "NodeVisualPresenter"
    - path: "src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewOptions.cs"
      provides: "Full-shell opt-in presenter configuration"
      contains: "Presentation"
    - path: "src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs"
      provides: "Shell-level forwarding of configured presenters into embedded surfaces"
      contains: "Presentation"
  key_links:
    - from: "src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewOptions.cs"
      to: "src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs"
      via: "factory-assigned presentation options"
      pattern: "Presentation"
    - from: "src/AsterGraph.Avalonia/Hosting/AsterGraphCanvasViewFactory.cs"
      to: "src/AsterGraph.Avalonia/Controls/NodeCanvas.axaml.cs"
      via: "presenter property assignment"
      pattern: "Presenter"
---

<objective>
Lock the public Avalonia-side presenter contract surface for Phase 4 and thread opt-in presenter configuration through the existing full-shell and standalone factory/options entry points.

Purpose: establish the API boundary before implementation so node, menu, inspector, and mini-map replacement work can proceed without revisiting public entry points.
Output: public presenter contracts in `AsterGraph.Avalonia`, shared presentation options, and stock-default plumbing through `GraphEditorView` plus the standalone factories.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-replaceable-presentation-kit/04-CONTEXT.md
@.planning/phases/04-replaceable-presentation-kit/04-RESEARCH.md
@src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewOptions.cs
@src/AsterGraph.Avalonia/Hosting/AsterGraphCanvasViewOptions.cs
@src/AsterGraph.Avalonia/Hosting/AsterGraphInspectorViewOptions.cs
@src/AsterGraph.Avalonia/Hosting/AsterGraphMiniMapViewOptions.cs
@src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml
@src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs
@src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
@src/AsterGraph.Editor/Menus/MenuItemDescriptor.cs
@src/AsterGraph.Editor/Presentation/NodePresentationState.cs

<interfaces>
From `src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewOptions.cs`:
```csharp
public sealed record AsterGraphAvaloniaViewOptions
{
    public GraphEditorViewModel? Editor { get; init; }
    public GraphEditorViewChromeMode ChromeMode { get; init; } = GraphEditorViewChromeMode.Default;
}
```

From `src/AsterGraph.Avalonia/Hosting/AsterGraphCanvasViewOptions.cs`:
```csharp
public sealed record AsterGraphCanvasViewOptions
{
    public GraphEditorViewModel? Editor { get; init; }
    public bool EnableDefaultContextMenu { get; init; } = true;
    public bool EnableDefaultCommandShortcuts { get; init; } = true;
}
```

From `src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs`:
```csharp
public ObservableCollection<NodeParameterViewModel> SelectedNodeParameters { get; }
public string InspectorTitle { get; }
public string InspectorCategory { get; }
public string InspectorDescription { get; }
public string InspectorInputs { get; }
public string InspectorOutputs { get; }
public string InspectorConnections { get; }
public string InspectorUpstream { get; }
public string InspectorDownstream { get; }
public string SelectionCaption { get; }
public IReadOnlyList<MenuItemDescriptor> BuildContextMenu(ContextMenuContext context)
```

From `src/AsterGraph.Editor/Menus/MenuItemDescriptor.cs`:
```csharp
public sealed record MenuItemDescriptor
{
    public string Id { get; }
    public string Header { get; }
    public ICommand? Command { get; }
    public object? CommandParameter { get; }
    public IReadOnlyList<MenuItemDescriptor> Children { get; }
    public string? IconKey { get; }
    public string? DisabledReason { get; }
    public bool IsEnabled { get; }
    public bool IsSeparator { get; }
    public bool HasChildren => Children.Count > 0;
}
```

From `src/AsterGraph.Editor/Presentation/NodePresentationState.cs`:
```csharp
public sealed record NodeAdornmentDescriptor(string Text, string AccentHex, string? ToolTip = null);
public sealed record NodeStatusBarDescriptor(string Text, string AccentHex, string? ToolTip = null);
public sealed record NodePresentationState(
    string? SubtitleOverride = null,
    string? DescriptionOverride = null,
    IReadOnlyList<NodeAdornmentDescriptor>? TopRightBadges = null,
    NodeStatusBarDescriptor? StatusBar = null);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the Avalonia presentation contracts and shared option container</name>
  <files>src/AsterGraph.Avalonia/Presentation/IGraphNodeVisualPresenter.cs, src/AsterGraph.Avalonia/Presentation/IGraphContextMenuPresenter.cs, src/AsterGraph.Avalonia/Presentation/IGraphInspectorPresenter.cs, src/AsterGraph.Avalonia/Presentation/IGraphMiniMapPresenter.cs, src/AsterGraph.Avalonia/Presentation/AsterGraphPresentationOptions.cs</files>
  <action>Create the Phase 4 presenter seam contracts inside `AsterGraph.Avalonia` only, per D-01, D-09, and D-11. Keep the seams medium-grain and per-surface: node visuals, context-menu presentation, inspector presentation, and mini-map presentation. Model them so the editor-owned inputs remain `GraphEditorViewModel`, `MenuItemDescriptor`, `NodePresentationState`, and existing selection/parameter projections. Do not invent parallel editor state, do not move any Avalonia-specific contract into `AsterGraph.Editor`, and do not require hosts to replace all presenters together. Add a shared `AsterGraphPresentationOptions`-style container so omission of any presenter explicitly means “use the stock default implementation” per D-02.</action>
  <verify>
    <automated>dotnet build src/AsterGraph.Avalonia/AsterGraph.Avalonia.csproj -v minimal</automated>
  </verify>
  <done>All four public presenter contracts exist in `AsterGraph.Avalonia`, they are documented as optional replacements, and the shared option container can represent partial replacement without forcing a custom shell.</done>
</task>

<task type="auto">
  <name>Task 2: Thread presenter options through the canonical full-shell and standalone entry points</name>
  <files>src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewOptions.cs, src/AsterGraph.Avalonia/Hosting/AsterGraphCanvasViewOptions.cs, src/AsterGraph.Avalonia/Hosting/AsterGraphInspectorViewOptions.cs, src/AsterGraph.Avalonia/Hosting/AsterGraphMiniMapViewOptions.cs, src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewFactory.cs, src/AsterGraph.Avalonia/Hosting/AsterGraphCanvasViewFactory.cs, src/AsterGraph.Avalonia/Hosting/AsterGraphInspectorViewFactory.cs, src/AsterGraph.Avalonia/Hosting/AsterGraphMiniMapViewFactory.cs, src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml, src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml.cs, tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs</files>
  <action>Add opt-in presenter configuration to the existing factory/options entry points per D-02 and D-10. `GraphEditorView` remains the convenience shell, direct `new GraphEditorView { Editor = ... }` remains supported, and stock defaults must still resolve when no presentation options are supplied. Forward the configured presenters into the embedded canvas/inspector/minimap surfaces without introducing a second shell API. Update focused initialization coverage so the canonical factory path validates the new options contract and presenter plumbing, but do not touch the noisy workspace-local `GraphEditorViewTests.cs`. Use a focused temp harness path such as `%TEMP%\\astergraph-phase4-contract-validation\\AsterGraph.Phase4.Contracts.Validation.csproj` if the main test project remains blocked.</action>
  <verify>
    <automated>dotnet test %TEMP%\astergraph-phase4-contract-validation\AsterGraph.Phase4.Contracts.Validation.csproj -v minimal</automated>
  </verify>
  <done>The full-shell and standalone factories accept per-surface presentation options, `GraphEditorView` forwards them into embedded controls, and omitting the new options still creates the current stock shell without regressions.</done>
</task>

</tasks>

<verification>
Confirm the new contracts compile cleanly, the existing factory/direct-construction host story still stands, and the focused contract harness stays isolated from `tests/AsterGraph.Editor.Tests/GraphEditorViewTests.cs`.
</verification>

<success_criteria>
Phase 4 implementation plans can depend on one stable Avalonia-side presentation contract surface, and all presenter replacement remains opt-in through the canonical factory/options entry points.
</success_criteria>

<output>
After completion, create `.planning/phases/04-replaceable-presentation-kit/04-01-SUMMARY.md`
</output>
