---
phase: 04-replaceable-presentation-kit
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
  - tools/AsterGraph.HostSample/Program.cs
  - tools/AsterGraph.PackageSmoke/Program.cs
  - README.md
  - docs/host-integration.md
  - src/AsterGraph.Avalonia/README.md
autonomous: true
requirements: [PRES-01, PRES-02, PRES-03, PRES-04]
must_haves:
  truths:
    - "Stock and custom presenter paths are both proven through a focused validation ring, host sample output, and package smoke markers."
    - "Consumer docs describe presenter replacement as opt-in per surface and keep the `AsterGraph.Editor`/`AsterGraph.Avalonia` boundary explicit."
    - "Factory and migration coverage still proves the retained full-shell/direct-construction path after presenter options are added."
  artifacts:
    - path: "tools/AsterGraph.HostSample/Program.cs"
      provides: "Human-readable proof of per-surface presenter replacement against one editor state"
      contains: "presenter"
    - path: "tools/AsterGraph.PackageSmoke/Program.cs"
      provides: "Machine-checkable presenter replacement markers"
      contains: "PRESENTER_"
    - path: "docs/host-integration.md"
      provides: "Consumer guidance for stock defaults and custom presenter opt-in"
      contains: "presentation"
  key_links:
    - from: "tools/AsterGraph.HostSample/Program.cs"
      to: "src/AsterGraph.Avalonia/Hosting/AsterGraphAvaloniaViewOptions.cs"
      via: "custom presentation options"
      pattern: "Presentation"
    - from: "tools/AsterGraph.PackageSmoke/Program.cs"
      to: "src/AsterGraph.Avalonia/Presentation"
      via: "smoke markers for custom presenters"
      pattern: "PRESENTER_"
---

<objective>
Close Phase 4 with a consumer-facing proof ring: focused validation harnesses, migration/factory regression updates, host sample coverage, package smoke markers, and docs.

Purpose: prove that stock defaults and custom presenters coexist cleanly for all four surfaces and document the public host story.
Output: final Phase 4 verification ring, updated consumer docs, and sample/smoke evidence for the replaceable presentation kit.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-embeddable-avalonia-surfaces/03-04-SUMMARY.md
@tools/AsterGraph.HostSample/Program.cs
@tools/AsterGraph.PackageSmoke/Program.cs
@README.md
@docs/host-integration.md
@src/AsterGraph.Avalonia/README.md
@tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs
@tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs

<interfaces>
From `tools/AsterGraph.PackageSmoke/Program.cs`:
```csharp
Console.WriteLine($"SURFACE_FULLSHELL_OK:{...}");
Console.WriteLine($"SURFACE_CANVAS_OK:{...}");
Console.WriteLine($"SURFACE_INSPECTOR_OK:{...}");
Console.WriteLine($"SURFACE_MINIMAP_OK:{...}");
Console.WriteLine($"MENU_STOCK_PRESENTER_OK:{typeof(GraphContextMenuPresenter).IsPublic}");
```

From `tools/AsterGraph.HostSample/Program.cs`:
```csharp
var view = AsterGraphAvaloniaViewFactory.Create(new AsterGraphAvaloniaViewOptions
{
    Editor = editor,
    ChromeMode = GraphEditorViewChromeMode.Default,
});
var standaloneCanvas = AsterGraphCanvasViewFactory.Create(new AsterGraphCanvasViewOptions { Editor = editor });
var standaloneInspector = AsterGraphInspectorViewFactory.Create(new AsterGraphInspectorViewOptions { Editor = editor });
var standaloneMiniMap = AsterGraphMiniMapViewFactory.Create(new AsterGraphMiniMapViewOptions { Editor = editor });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove stock and custom presenter paths through sample, smoke, and migration-focused regressions</name>
  <files>tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs, tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs, tools/AsterGraph.HostSample/Program.cs, tools/AsterGraph.PackageSmoke/Program.cs</files>
  <action>Extend the consumer proof points around the finished presenter seams. Update initialization/migration regressions so the retained factory/direct-construction host story still works after presenter options were added. Rework `tools/AsterGraph.HostSample` to instantiate at least one custom presenter for each surface across full-shell and standalone composition, and print readable evidence that stock defaults remain opt-in fallbacks. Rework `tools/AsterGraph.PackageSmoke` to emit machine-checkable `PRESENTER_*` markers for node, menu, inspector, and mini-map replacement while preserving the Phase 1-3 markers. Keep the sample/smoke story aligned with the existing runtime/session, stock presenter, and standalone-surface proof patterns established in Phase 2 and Phase 3.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "dotnet run --project tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj; dotnet run --project tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj"</automated>
  </verify>
  <done>The host sample shows readable stock-vs-custom presenter behavior, package smoke emits presenter replacement markers for all four surfaces, and the factory/migration regressions cover the new options contract.</done>
</task>

<task type="auto">
  <name>Task 2: Document the replaceable presentation kit and close the focused validation ring</name>
  <files>README.md, docs/host-integration.md, src/AsterGraph.Avalonia/README.md</files>
  <action>Update the consumer docs so they all tell one coherent Phase 4 story: stock presenters remain default, replacement is opt-in per surface, `AsterGraph.Editor` still owns state/intent/contracts, and Avalonia presenter replacement stays in `AsterGraph.Avalonia` per D-11. Include concrete examples for full-shell and standalone configuration. Build a final temp validation harness under `%TEMP%\\astergraph-phase4-validation\\AsterGraph.Phase4.Validation.csproj` that links the focused Phase 4 regression files only (node/menu, inspector/minimap, initialization/migration as needed) and explicitly excludes the unresolved workspace-local `GraphEditorViewTests.cs` noise file. Use that harness as the final plan-level gate.</action>
  <verify>
    <automated>dotnet test %TEMP%\astergraph-phase4-validation\AsterGraph.Phase4.Validation.csproj -v minimal</automated>
  </verify>
  <done>README, host integration guide, and `AsterGraph.Avalonia` README all describe the same presenter-replacement story, and the final temp harness proves the focused Phase 4 coverage ring without depending on `GraphEditorViewTests.cs`.</done>
</task>

</tasks>

<verification>
Run the final ring after both implementation plans land:
- `dotnet build src/AsterGraph.Avalonia/AsterGraph.Avalonia.csproj -v minimal`
- `dotnet test %TEMP%\astergraph-phase4-validation\AsterGraph.Phase4.Validation.csproj -v minimal`
- `dotnet run --project tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj`
- `dotnet run --project tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj`
- `rg -n "presentation|presenter|mini map|inspector|context menu" README.md docs/host-integration.md src/AsterGraph.Avalonia/README.md`
</verification>

<success_criteria>
All four PRES requirements are proven through focused tests, sample output, smoke markers, and aligned docs, with stock presenters preserved as the zero-configuration default.
</success_criteria>

<output>
After completion, create `.planning/phases/04-replaceable-presentation-kit/04-04-SUMMARY.md`
</output>
