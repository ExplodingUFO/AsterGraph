---
phase: 14-session-and-compatibility-facade-decoupling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
  - src/AsterGraph.Editor/Kernel/GraphEditorKernel.cs
  - src/AsterGraph.Editor/Runtime/
  - tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
requirements:
  - KERN-03
autonomous: true
---

<objective>
Rebuild `GraphEditorViewModel` around the extracted kernel so the VM becomes a compatibility adapter over kernel-owned runtime state rather than a parallel state owner.
</objective>

<scope>
- Introduce the minimum internal adapter/sync layer needed for `GraphEditorViewModel` to compose a `GraphEditorKernel` and project kernel state into existing VM collections/properties.
- Remove the need for `GraphEditorViewModel` to act as the primary `IGraphEditorSessionHost` implementation for graph, selection, viewport, pending-connection, history, and workspace state.
- Keep UI-only concerns such as menus, localization, presentation, clipboard/fragment adapters, and Avalonia-facing command surfaces in the façade layer.
</scope>

<read_first>
- src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
- src/AsterGraph.Editor/Kernel/GraphEditorKernel.cs
- src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
- src/AsterGraph.Editor/Runtime/IGraphEditorSessionHost.cs
- tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
- tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
</read_first>

<action>
- Add an internal kernel-backed synchronization path that initializes VM projections from `GraphEditorKernel` state and refreshes them after kernel-side mutations/events.
- Update `GraphEditorViewModel` construction so its retained `Session` path uses the shared kernel-backed runtime instead of treating VM-owned graph state as canonical.
- Move any remaining session-host-only implementation details out of the large explicit `IGraphEditorSessionHost` block in `GraphEditorViewModel` into a narrower adapter/projection layer.
- Preserve current public `GraphEditorViewModel` members and constructor shape; do not obsolete or remove the compatibility façade in this phase.
</action>

<acceptance_criteria>
- src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs no longer declares `IGraphEditorSessionHost` on the class definition
- src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs composes a `GraphEditorKernel` instance for canonical runtime state
- tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs contains proof that the retained `editor.Session` path stays kernel-backed or adapter-backed rather than VM-owned
- tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs exits 0 with Phase 14 changes
</acceptance_criteria>

<verification>
- dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorSessionTests|FullyQualifiedName~GraphEditorMigrationCompatibilityTests" -v minimal
</verification>

<output>
After completion, create `.planning/phases/14-session-and-compatibility-facade-decoupling/14-01-SUMMARY.md`
</output>
