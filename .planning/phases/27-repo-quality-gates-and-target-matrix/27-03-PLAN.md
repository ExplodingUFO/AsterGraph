---
phase: 27-repo-quality-gates-and-target-matrix
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - .github/workflows/ci.yml
requirements:
  - QUAL-02
autonomous: true
---

<objective>
Add checked-in GitHub Actions CI that runs the shared repo-local validation script across explicit `net8.0` and `net9.0` lanes on the supported package boundary.
</objective>

<scope>
- Add a `.github/workflows/ci.yml` workflow for push, pull request, and manual dispatch.
- Reuse `eng/ci.ps1` instead of duplicating validation logic in YAML.
- Make the framework split visible in the workflow definition.
</scope>

<must_haves>
- CI is checked in and readable from the repo.
- CI reuses the repo-local validation script.
- The workflow makes `net8.0` and `net9.0` coverage explicit.
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Add the GitHub Actions workflow around the shared script</name>
    <files>
      .github/workflows/ci.yml
    </files>
    <read_first>
      eng/ci.ps1
      Directory.Build.props
      README.md
    </read_first>
    <action>
      Create `.github/workflows/ci.yml` with `push`, `pull_request`, and `workflow_dispatch` triggers. Use a Windows runner and `actions/setup-dotnet@v4` with a repo-appropriate SDK entry so the workflow can build `net8.0` and `net9.0`. Encode the framework split as a visible matrix and invoke `pwsh` against `.\eng\ci.ps1 -Lane all -Framework <matrix-value> -Configuration Release`. Keep the workflow small; the script remains the source of truth for the actual validation commands.
    </action>
    <acceptance_criteria>
      `.github/workflows/ci.yml` exists
      `.github/workflows/ci.yml` contains `pull_request:`
      `.github/workflows/ci.yml` contains `workflow_dispatch:`
      `.github/workflows/ci.yml` contains `runs-on: windows-latest`
      `.github/workflows/ci.yml` contains `actions/setup-dotnet@v4`
      `.github/workflows/ci.yml` contains `matrix:`
      `.github/workflows/ci.yml` contains `net8.0`
      `.github/workflows/ci.yml` contains `net9.0`
      `.github/workflows/ci.yml` contains `.\eng\ci.ps1 -Lane all -Framework`
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane all -Framework all -Configuration Release
</verification>

<output>
After completion, create `.planning/phases/27-repo-quality-gates-and-target-matrix/27-03-SUMMARY.md`
</output>
