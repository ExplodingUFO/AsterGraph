---
phase: 27-repo-quality-gates-and-target-matrix
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - eng/ci.ps1
requirements:
  - QUAL-02
autonomous: true
---

<objective>
Add one checked-in repo-local validation entry point that contributors and CI can both run to validate the supported package boundary and current test/tool topology.
</objective>

<scope>
- Create a first-class `pwsh` validation script under `eng/`.
- Make the script encode the supported framework split explicitly instead of relying only on solution-wide implicit behavior.
- Keep the script focused on restore/build/test baseline work for this phase.
</scope>

<must_haves>
- The repo gets one visible scripted command path that CI can call directly.
- The script understands both `net8.0` and `net9.0` lanes.
- The script does not yet absorb Phase 29 release gates such as coverage thresholds, package smoke-from-packed-artifacts, or public API compatibility checks.
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Create the shared CI script with explicit lane and framework switches</name>
    <files>
      eng/ci.ps1
    </files>
    <read_first>
      README.md
      docs/host-integration.md
      Directory.Build.props
      src/AsterGraph.Abstractions/AsterGraph.Abstractions.csproj
      src/AsterGraph.Core/AsterGraph.Core.csproj
      src/AsterGraph.Editor/AsterGraph.Editor.csproj
      src/AsterGraph.Avalonia/AsterGraph.Avalonia.csproj
      tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj
      tests/AsterGraph.Serialization.Tests/AsterGraph.Serialization.Tests.csproj
      tests/AsterGraph.TestPlugins/AsterGraph.TestPlugins.csproj
      tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj
      tools/AsterGraph.ScaleSmoke/AsterGraph.ScaleSmoke.csproj
    </read_first>
    <action>
      Create `eng/ci.ps1` with `param(...)` switches for `-Lane`, `-Framework`, and `-Configuration`. Support at least `restore`, `build`, `test`, and `all` lanes, plus `all`, `net8.0`, and `net9.0` framework modes. The script should restore the repo, build the four publishable packages for the requested framework lane, and run the current focused validation surface for that lane: `Serialization.Tests`, `PackageSmoke`, and `ScaleSmoke` for `net8.0`; `Editor.Tests` and `TestPlugins` for `net9.0`. Keep the script fail-fast and readable, with small helper functions instead of one long inline command block.
    </action>
    <acceptance_criteria>
      `eng/ci.ps1` exists
      `eng/ci.ps1` contains `param(`
      `eng/ci.ps1` contains `ValidateSet('restore', 'build', 'test', 'all')`
      `eng/ci.ps1` contains `ValidateSet('all', 'net8.0', 'net9.0')`
      `eng/ci.ps1` contains `src/AsterGraph.Abstractions/AsterGraph.Abstractions.csproj`
      `eng/ci.ps1` contains `src/AsterGraph.Avalonia/AsterGraph.Avalonia.csproj`
      `eng/ci.ps1` contains `tests/AsterGraph.Serialization.Tests/AsterGraph.Serialization.Tests.csproj`
      `eng/ci.ps1` contains `tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj`
      `eng/ci.ps1` contains `tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj`
      `eng/ci.ps1` contains `tools/AsterGraph.ScaleSmoke/AsterGraph.ScaleSmoke.csproj`
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane all -Framework all -Configuration Release
</verification>

<output>
After completion, create `.planning/phases/27-repo-quality-gates-and-target-matrix/27-02-SUMMARY.md`
</output>
