---
phase: 26-runtime-boundary-canonicalization
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - src/AsterGraph.Editor/Runtime/IGraphEditorQueries.cs
  - src/AsterGraph.Editor/Menus/CompatiblePortTarget.cs
  - src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
  - src/AsterGraph.Editor/README.md
  - tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
requirements:
  - BOUND-03
autonomous: true
---

<objective>
Make the remaining compatibility-only runtime APIs impossible to mistake for the canonical host path by tightening migration guidance, obsolete messaging, and proof coverage.
</objective>

<scope>
- Strengthen compiler-visible and IDE-visible migration guidance.
- Document the staged exit path for compatibility-only runtime APIs without removing them in this phase.
- Back the guidance with focused tests so the deprecation posture does not drift quietly later.
</scope>

<must_haves>
- Hosts can still compile against retained compatibility APIs in this phase.
- The code and docs state plainly that `GetCompatiblePortTargets(...)` is the canonical runtime path and that MVVM-shaped targets are retained-only shims.
- The staged removal plan is explicit enough that a later major-version cleanup can build on it without rediscovery.
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Tighten obsolete messages and API remarks around the compatibility shim</name>
    <files>
      src/AsterGraph.Editor/Runtime/IGraphEditorQueries.cs
      src/AsterGraph.Editor/Menus/CompatiblePortTarget.cs
      src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
    </files>
    <read_first>
      src/AsterGraph.Editor/Runtime/IGraphEditorQueries.cs
      src/AsterGraph.Editor/Menus/CompatiblePortTarget.cs
      src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
      src/AsterGraph.Editor/README.md
      .planning/PROJECT.md
      .planning/REQUIREMENTS.md
    </read_first>
    <action>
      Update XML remarks, obsolete messages, and retained-facade comments so they explicitly say: `IGraphEditorQueries.GetCompatiblePortTargets(string, string)` is the canonical runtime query, `CompatiblePortTarget` is a retained compatibility shim for MVVM-oriented hosts, and the shim is scheduled for staged retirement rather than indefinite first-class support. Use exact wording consistently across the interface, the shim type, and `GraphEditorViewModel`.
    </action>
    <acceptance_criteria>
      `src/AsterGraph.Editor/Runtime/IGraphEditorQueries.cs` contains `canonical runtime queries`
      `src/AsterGraph.Editor/Menus/CompatiblePortTarget.cs` contains `retained compatibility shim`
      `src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs` contains `compatibility facade`
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Task 2: Document the staged migration and lock it with focused tests</name>
    <files>
      src/AsterGraph.Editor/README.md
      tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
      tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
    </files>
    <read_first>
      src/AsterGraph.Editor/README.md
      tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
      tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
      src/AsterGraph.Editor/Runtime/IGraphEditorQueries.cs
      src/AsterGraph.Editor/Menus/CompatiblePortTarget.cs
      src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
    </read_first>
    <action>
      Add a short migration section to `src/AsterGraph.Editor/README.md` that names the canonical runtime query, the retained compatibility shim, and the staged retirement posture: current milestone keeps the shim with strong guidance, later minor releases may add stronger warnings, and a future major release may remove it. Extend focused tests so they assert the intended obsolete guidance and retained compatibility posture instead of only checking that the attributes still exist.
    </action>
    <acceptance_criteria>
      `src/AsterGraph.Editor/README.md` contains `GetCompatiblePortTargets(...)`
      `src/AsterGraph.Editor/README.md` contains `future major release may remove`
      `tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs` contains assertions for obsolete guidance text or staged migration messaging
      `dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorSessionTests|FullyQualifiedName~GraphEditorMigrationCompatibilityTests" --nologo -v minimal` exits 0
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorSessionTests|FullyQualifiedName~GraphEditorMigrationCompatibilityTests" --nologo -v minimal
- dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj --nologo -v minimal
</verification>

<output>
After completion, create `.planning/phases/26-runtime-boundary-canonicalization/26-03-SUMMARY.md`
</output>
