# Plan 01 - Release Proof Artifacts

## Scope

- capture HostSample, PackageSmoke, ScaleSmoke, and coverage outputs into stable files under `artifacts/`
- keep `eng/ci.ps1` as the single repo-local validation entrypoint

## Files

- `eng/ci.ps1`
- `eng/coverage-report.ps1`
