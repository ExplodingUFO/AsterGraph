---
phase: 20-host-menu-control-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AsterGraph.Demo/Views/MainWindow.axaml
  - src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
requirements:
  - CTRL-01
autonomous: true
---

<objective>
Turn the `视图` host menu group into a real live shell/chrome control surface that updates the current `GraphEditorView` without replacing the demo session.
</objective>

<scope>
- Add direct view/chrome quick toggles to the host menu.
- Replace the summary-only `视图` pane content with compact live view control rows.
- Keep the graph-first shell intact and preserve the single `Editor` instance.
</scope>

<read_first>
- .planning/phases/20-host-menu-control-consolidation/20-CONTEXT.md
- .planning/phases/20-host-menu-control-consolidation/20-RESEARCH.md
- .planning/phases/20-host-menu-control-consolidation/20-UI-SPEC.md
- .planning/phases/19-graph-first-demo-shell/19-01-SUMMARY.md
- .planning/phases/19-graph-first-demo-shell/19-02-SUMMARY.md
- src/AsterGraph.Demo/Views/MainWindow.axaml
- src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
- tests/AsterGraph.Editor.Tests/DemoMainWindowTests.cs
</read_first>

<action>
- Update `src/AsterGraph.Demo/Views/MainWindow.axaml` so `PART_ViewMenu` contains direct checkable menu items using Avalonia `ToggleType="CheckBox"` with `IsChecked` bound to the existing chrome booleans. Use these exact quick-toggle headers: `显示顶栏`, `显示节点库`, `显示检查器`, and `显示状态栏`.
- Keep one explicit menu action such as `打开视图控制` under `PART_ViewMenu` that focuses the right-side pane on the `视图` group. Use separators so the dropdown stays readable rather than becoming a dense ribbon replacement.
- Replace the current summary-only `视图` drawer content with compact live view control rows bound to the same `IsHeaderChromeVisible`, `IsLibraryChromeVisible`, `IsInspectorChromeVisible`, and `IsStatusChromeVisible` properties. The drawer rows may use `CheckBox` or another built-in compact toggle control, but they must bind to the same properties as the menu items.
- If `MainWindowViewModel` needs additional computed labels or commands for view grouping, add them there without creating alternate copies of the chrome booleans.
- Keep `MainGraphEditorView` bound to the same `Editor` instance and to the same chrome visibility properties. Do not add another `new GraphEditorViewModel(` path or another live graph surface.
</action>

<acceptance_criteria>
- `src/AsterGraph.Demo/Views/MainWindow.axaml` contains `ToggleType="CheckBox"` view menu items with headers `显示顶栏`, `显示节点库`, `显示检查器`, and `显示状态栏`
- `src/AsterGraph.Demo/Views/MainWindow.axaml` contains a `PART_ViewMenu` entry with header `打开视图控制`
- `src/AsterGraph.Demo/Views/MainWindow.axaml` still contains exactly one `controls:GraphEditorView x:Name="MainGraphEditorView"` bound to `Editor="{Binding Editor}"`
- `src/AsterGraph.Demo/Views/MainWindow.axaml` continues to bind `IsHeaderChromeVisible`, `IsLibraryChromeVisible`, `IsInspectorChromeVisible`, and `IsStatusChromeVisible` on `MainGraphEditorView`
- `src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs` contains only the existing constructor-time `new GraphEditorViewModel(` initialization and no second editor creation path
- `dotnet build src/AsterGraph.Demo/AsterGraph.Demo.csproj --nologo -v minimal` exits 0
</acceptance_criteria>

<verification>
- dotnet build src/AsterGraph.Demo/AsterGraph.Demo.csproj --nologo -v minimal
</verification>

<output>
After completion, create `.planning/phases/20-host-menu-control-consolidation/20-01-SUMMARY.md`
</output>
