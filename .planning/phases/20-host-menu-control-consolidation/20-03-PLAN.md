---
phase: 20-host-menu-control-consolidation
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/AsterGraph.Demo/Views/MainWindow.axaml
  - src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
  - tests/AsterGraph.Editor.Tests/DemoMainWindowTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorDemoShellTests.cs
  - tests/AsterGraph.Editor.Tests/DemoDiagnosticsProjectionTests.cs
  - tests/AsterGraph.Editor.Tests/DemoHostMenuControlTests.cs
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03
autonomous: true
---

<objective>
Complete the `运行时` host menu group with compact live readouts from the canonical session path and lock the full host-menu control flow with targeted headless regressions.
</objective>

<scope>
- Expose runtime-facing demo readouts from the same host menu structure.
- Keep runtime content compact and explicitly tied to the current `Editor.Session`.
- Add regression coverage for view, behavior, and runtime host menu groups on the single live session.
</scope>

<read_first>
- .planning/phases/20-host-menu-control-consolidation/20-CONTEXT.md
- .planning/phases/20-host-menu-control-consolidation/20-RESEARCH.md
- .planning/phases/20-host-menu-control-consolidation/20-UI-SPEC.md
- .planning/phases/20-host-menu-control-consolidation/20-01-PLAN.md
- .planning/phases/20-host-menu-control-consolidation/20-02-PLAN.md
- src/AsterGraph.Demo/Views/MainWindow.axaml
- src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
- tests/AsterGraph.Editor.Tests/DemoMainWindowTests.cs
- tests/AsterGraph.Editor.Tests/GraphEditorDemoShellTests.cs
- tests/AsterGraph.Editor.Tests/DemoDiagnosticsProjectionTests.cs
</read_first>

<action>
- Update `src/AsterGraph.Demo/Views/MainWindow.axaml` so `PART_RuntimeMenu` provides explicit runtime access entries with these exact headers: `打开运行时摘要` and `查看最近诊断`. Both must route into the existing right-side pane rather than creating a second dashboard or a new editor surface.
- Replace the summary-only `运行时` drawer content with compact, machine-checkable runtime sections bound to the existing projection properties in `MainWindowViewModel`. The drawer must render rows or cards for `文档标题`, `节点数量`, `连线数量`, `当前选择`, `视口缩放`, and `最近诊断`.
- Keep runtime values sourced from the current canonical session projection path (`Editor.Session.Diagnostics` plus inspection snapshot). Do not introduce a second runtime-summary cache that can drift from the real session.
- Add or update headless tests so the demo shell proves all three control groups operate on the same live session. Prefer a focused test file such as `tests/AsterGraph.Editor.Tests/DemoHostMenuControlTests.cs`, and update `DemoMainWindowTests`, `GraphEditorDemoShellTests`, or `DemoDiagnosticsProjectionTests` where needed for named parts and readout assertions.
- Make the tests assert that opening `视图`, `行为`, or `运行时` groups never replaces `MainWindowViewModel.Editor`, and that runtime readouts continue to match the current `Editor.Session` state and diagnostics path.
</action>

<acceptance_criteria>
- `src/AsterGraph.Demo/Views/MainWindow.axaml` contains `PART_RuntimeMenu` entries with headers `打开运行时摘要` and `查看最近诊断`
- `src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs` continues to expose runtime projection properties including `RuntimeDocumentTitle`, `RuntimeNodeCount`, `RuntimeConnectionCount`, `RuntimeSelectedNodeCount`, `RuntimeViewportZoom`, and `RecentDiagnostics`
- `tests/AsterGraph.Editor.Tests/DemoHostMenuControlTests.cs` exists and contains focused host-menu control assertions
- the test suite contains assertions that `MainWindowViewModel.Editor` is the same instance before and after opening `视图`, `行为`, and `运行时` groups
- the test suite contains assertions that runtime readouts still come from `Editor.Session.Diagnostics` and inspection snapshot projections rather than a separate cache
- `dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~DemoHostMenuControlTests|FullyQualifiedName~DemoMainWindowTests|FullyQualifiedName~GraphEditorDemoShellTests|FullyQualifiedName~DemoDiagnosticsProjectionTests" -v minimal` exits 0
- `dotnet build src/AsterGraph.Demo/AsterGraph.Demo.csproj --nologo -v minimal` exits 0
</acceptance_criteria>

<verification>
- dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~DemoHostMenuControlTests|FullyQualifiedName~DemoMainWindowTests|FullyQualifiedName~GraphEditorDemoShellTests|FullyQualifiedName~DemoDiagnosticsProjectionTests" -v minimal
- dotnet build src/AsterGraph.Demo/AsterGraph.Demo.csproj --nologo -v minimal
</verification>

<output>
After completion, create `.planning/phases/20-host-menu-control-consolidation/20-03-SUMMARY.md`
</output>
