---
phase: 20-host-menu-control-consolidation
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/AsterGraph.Demo/Views/MainWindow.axaml
  - src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
requirements:
  - CTRL-02
autonomous: true
---

<objective>
Consolidate the demo's editing-behavior and command-permission toggles into the `行为` host menu group and its compact drawer section without adding a second behavior-authoring path.
</objective>

<scope>
- Expose the existing behavior toggles directly through the host menu.
- Replace summary-only behavior text with compact live controls in the drawer.
- Keep `ApplyHostOptions()` and `BuildCommandPermissions()` as the single behavior update path.
</scope>

<read_first>
- .planning/phases/20-host-menu-control-consolidation/20-CONTEXT.md
- .planning/phases/20-host-menu-control-consolidation/20-RESEARCH.md
- .planning/phases/20-host-menu-control-consolidation/20-UI-SPEC.md
- .planning/phases/20-host-menu-control-consolidation/20-01-PLAN.md
- src/AsterGraph.Demo/Views/MainWindow.axaml
- src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
- tests/AsterGraph.Editor.Tests/GraphEditorDemoShellTests.cs
</read_first>

<action>
- Update `src/AsterGraph.Demo/Views/MainWindow.axaml` so `PART_BehaviorMenu` contains direct checkable menu items with `ToggleType="CheckBox"` and `IsChecked` bound to these existing booleans: `IsReadOnlyEnabled`, `IsGridSnappingEnabled`, `IsAlignmentGuidesEnabled`, `AreWorkspaceCommandsEnabled`, `AreFragmentCommandsEnabled`, and `AreHostMenuExtensionsEnabled`. Use these exact headers: `只读模式`, `网格吸附`, `对齐辅助线`, `工作区命令`, `片段命令`, and `宿主菜单扩展`.
- Keep one explicit menu entry such as `打开行为控制` that focuses the right-side pane on the `行为` group.
- Replace the current summary-only `行为` drawer content with compact bound control rows for the same six booleans. Reuse the existing properties instead of introducing alternate backing fields or a separate behavior options model.
- Keep behavior propagation concrete inside `MainWindowViewModel`: property changes must continue to flow through `ApplyHostOptions()` and `BuildCommandPermissions()`. If commands or helper labels are added, they should only orchestrate the existing property-driven path.
- Ensure the behavior controls still act on the current `Editor` and do not instantiate or swap another editor/session object when toggled from either the menu or the drawer.
</action>

<acceptance_criteria>
- `src/AsterGraph.Demo/Views/MainWindow.axaml` contains `ToggleType="CheckBox"` behavior menu items with headers `只读模式`, `网格吸附`, `对齐辅助线`, `工作区命令`, `片段命令`, and `宿主菜单扩展`
- `src/AsterGraph.Demo/Views/MainWindow.axaml` contains a `PART_BehaviorMenu` entry with header `打开行为控制`
- `src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs` still contains `ApplyHostOptions()` and `BuildCommandPermissions()` and those methods continue to reference the six existing behavior booleans
- `src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs` contains only the existing constructor-time `new GraphEditorViewModel(` initialization and no secondary editor creation path
- toggling behavior controls is driven by the existing boolean properties rather than a second behavior state model
- `dotnet build src/AsterGraph.Demo/AsterGraph.Demo.csproj --nologo -v minimal` exits 0
</acceptance_criteria>

<verification>
- dotnet build src/AsterGraph.Demo/AsterGraph.Demo.csproj --nologo -v minimal
</verification>

<output>
After completion, create `.planning/phases/20-host-menu-control-consolidation/20-02-SUMMARY.md`
</output>
