---
phase: 15-capability-and-descriptor-contract-normalization
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/AsterGraph.Editor/Menus/
  - src/AsterGraph.Editor/Runtime/
  - src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
  - src/AsterGraph.Editor/Services/GraphEditorCommandStateNotifier.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs
  - tests/AsterGraph.Editor.Tests/GraphContextMenuPresenterTests.cs
requirements:
  - CAP-02
autonomous: true
---

<objective>
Make command and menu contracts canonical around stable descriptors and runtime-owned invocation instead of `RelayCommand`, `ICommand`, and VM projection types.
</objective>

<scope>
- Introduce canonical command/menu descriptor contracts that identify actions by stable IDs and payloads instead of direct MVVM command objects.
- Refactor editor-layer menu construction to produce canonical descriptors first, with compatibility adapters translating them back into current `MenuItemDescriptor` / `ICommand` flows where required.
- Remove `NodeTemplateViewModel` and other VM projection types from the canonical command/menu contract path.
</scope>

<read_first>
- src/AsterGraph.Editor/Menus/MenuItemDescriptor.cs
- src/AsterGraph.Editor/Menus/IGraphContextMenuHost.cs
- src/AsterGraph.Editor/Menus/GraphContextMenuBuilder.cs
- src/AsterGraph.Editor/Menus/GraphContextMenuAugmentationContext.cs
- src/AsterGraph.Editor/ViewModels/NodeTemplateViewModel.cs
- src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
- tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs
- tests/AsterGraph.Editor.Tests/GraphContextMenuPresenterTests.cs
</read_first>

<action>
- Add canonical editor-layer descriptor types for command discovery/invocation and menu item/action composition.
- Rework `GraphContextMenuBuilder` and related host seams so canonical menu construction no longer requires `RelayCommand`, `ICommand`, `ObservableCollection`, or VM-only template objects as the editor-facing model.
- Preserve existing menu presenter and augmentor behavior by adding adapter layers that materialize compatibility `MenuItemDescriptor` objects from the canonical descriptors.
- Extend tests to prove the canonical descriptor path and to ensure existing augmentor/shell integrations still function through the compatibility bridge.
</action>

<acceptance_criteria>
- the editor-layer canonical command/menu path no longer requires `RelayCommand`, `ICommand`, `ObservableCollection`, or `NodeTemplateViewModel`
- menu building can be driven from runtime/descriptor data rather than VM host object shape
- compatibility `MenuItemDescriptor` / augmentor flows still work through adapters
- focused service/menu regression coverage passes
</acceptance_criteria>

<verification>
- dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorSessionTests|FullyQualifiedName~GraphEditorServiceSeamsTests|FullyQualifiedName~GraphContextMenuPresenterTests" -v minimal
</verification>

<output>
After completion, create `.planning/phases/15-capability-and-descriptor-contract-normalization/15-02-SUMMARY.md`
</output>
