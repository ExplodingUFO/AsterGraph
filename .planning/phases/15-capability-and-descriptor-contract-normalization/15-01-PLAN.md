---
phase: 15-capability-and-descriptor-contract-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AsterGraph.Editor/Runtime/
  - src/AsterGraph.Editor/Diagnostics/
  - src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
  - src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorDiagnosticsContractsTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
requirements:
  - CAP-01
autonomous: true
---

<objective>
Expose explicit immutable capability and optional-service discovery on the canonical runtime path so hosts no longer need to infer support from facade/object shape.
</objective>

<scope>
- Introduce one or more runtime descriptor snapshots that make command/service/feature support explicit for host discovery.
- Keep `GraphEditorCapabilitySnapshot` as the lightweight compatibility boolean view while projecting richer discovery data from the same canonical runtime source.
- Ensure retained `GraphEditorViewModel.Session` and factory-created sessions report the same descriptor state.
</scope>

<read_first>
- src/AsterGraph.Editor/Runtime/IGraphEditorSession.cs
- src/AsterGraph.Editor/Runtime/IGraphEditorQueries.cs
- src/AsterGraph.Editor/Runtime/GraphEditorCapabilitySnapshot.cs
- src/AsterGraph.Editor/Diagnostics/GraphEditorInspectionSnapshot.cs
- src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
- src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
- tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
- tests/AsterGraph.Editor.Tests/GraphEditorDiagnosticsContractsTests.cs
</read_first>

<action>
- Add a canonical runtime read for capability/service descriptors under `IGraphEditorQueries`, backed by the kernel/session path rather than VM-only shape.
- Model explicit discovery for optional host-facing services/features already represented in composition and runtime state, such as workspace/fragment support, diagnostics, localization, presentation, menu augmentation, and instrumentation support.
- Reuse the new descriptor snapshot in diagnostics/inspection surfaces where host tooling needs a richer integrated view.
- Add focused contract and parity tests covering both `CreateSession(...)` and retained `Create(...).Session` paths.
</action>

<acceptance_criteria>
- `src/AsterGraph.Editor/Runtime/IGraphEditorQueries.cs` exposes an immutable capability/service discovery read beyond the existing boolean capability snapshot
- hosts can determine optional-service support without inspecting `GraphEditorViewModel`, nullable services, or `ICommand`/VM object shape
- retained and factory session paths return equivalent descriptor discovery results
- focused contract tests for the new descriptor surface pass
</acceptance_criteria>

<verification>
- dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorSessionTests|FullyQualifiedName~GraphEditorDiagnosticsContractsTests|FullyQualifiedName~GraphEditorMigrationCompatibilityTests" -v minimal
</verification>

<output>
After completion, create `.planning/phases/15-capability-and-descriptor-contract-normalization/15-01-SUMMARY.md`
</output>
