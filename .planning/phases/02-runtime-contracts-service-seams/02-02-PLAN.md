---
phase: 02-runtime-contracts-service-seams
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/AsterGraph.Editor/Services/IGraphWorkspaceService.cs
  - src/AsterGraph.Editor/Services/IGraphFragmentWorkspaceService.cs
  - src/AsterGraph.Editor/Services/IGraphFragmentLibraryService.cs
  - src/AsterGraph.Editor/Services/IGraphClipboardPayloadSerializer.cs
  - src/AsterGraph.Editor/Services/GraphEditorStorageDefaults.cs
  - src/AsterGraph.Editor/Diagnostics/IGraphEditorDiagnosticsSink.cs
  - src/AsterGraph.Editor/Diagnostics/GraphEditorDiagnostic.cs
  - src/AsterGraph.Editor/Services/GraphWorkspaceService.cs
  - src/AsterGraph.Editor/Services/GraphFragmentWorkspaceService.cs
  - src/AsterGraph.Editor/Services/GraphFragmentLibraryService.cs
  - src/AsterGraph.Editor/Services/GraphClipboardPayloadSerializer.cs
  - src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
  - src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
  - src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
  - src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs
autonomous: true
requirements:
  - SERV-01
  - SERV-02
must_haves:
  truths:
    - "A host can replace persistence, fragment/template, clipboard payload, and diagnostics behavior through public `AsterGraph.Editor` contracts instead of patching concrete classes."
    - "Default runtime services no longer write to `LocalApplicationData/AsterGraphDemo/...` when hosts accept defaults."
    - "Recoverable storage and clipboard failures flow through both machine-readable diagnostics and the compatibility `StatusMessage` path."
  artifacts:
    - path: "src/AsterGraph.Editor/Services/IGraphWorkspaceService.cs"
      provides: "Replaceable workspace persistence contract."
    - path: "src/AsterGraph.Editor/Services/IGraphFragmentWorkspaceService.cs"
      provides: "Replaceable fragment workspace contract."
    - path: "src/AsterGraph.Editor/Services/IGraphFragmentLibraryService.cs"
      provides: "Replaceable fragment template library contract."
    - path: "src/AsterGraph.Editor/Diagnostics/IGraphEditorDiagnosticsSink.cs"
      provides: "Machine-readable diagnostics sink contract."
    - path: "src/AsterGraph.Editor/Services/GraphEditorStorageDefaults.cs"
      provides: "Shared non-demo default storage resolver."
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs"
      provides: "Regression coverage for service replacement and generic storage defaults."
  key_links:
    - from: "src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs"
      to: "new service interfaces"
      via: "Factory/service injection path"
      pattern: "IGraphWorkspaceService|IGraphFragmentWorkspaceService|IGraphFragmentLibraryService|IGraphClipboardPayloadSerializer|IGraphEditorDiagnosticsSink"
    - from: "src/AsterGraph.Editor/Runtime/GraphEditorSession.cs"
      to: "src/AsterGraph.Editor/Diagnostics/IGraphEditorDiagnosticsSink.cs"
      via: "Recoverable failure publication"
      pattern: "workspace.save.failed|workspace.load.failed|fragment.import.failed|clipboard.read.failed"
    - from: "src/AsterGraph.Editor/Services/GraphWorkspaceService.cs"
      to: "src/AsterGraph.Editor/Services/GraphEditorStorageDefaults.cs"
      via: "Shared default-path resolution"
      pattern: "AsterGraphDemo"
---

<objective>
Extract the first replaceable runtime service seams and remove the demo-branded default storage assumptions from the publishable editor package.

Purpose: Satisfy SERV-01 and SERV-02 while preserving the already-mature localization, presentation, context-menu augmentor, and compatibility seams from earlier work.
Output: Public service interfaces, generic default storage resolution, diagnostics sink contracts, interface-based factory/session composition, and targeted regression coverage for service replacement.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-runtime-contracts-service-seams/02-CONTEXT.md
@.planning/phases/02-runtime-contracts-service-seams/02-RESEARCH.md
@.planning/phases/02-runtime-contracts-service-seams/02-VALIDATION.md
@src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
@src/AsterGraph.Editor/Services/GraphWorkspaceService.cs
@src/AsterGraph.Editor/Services/GraphFragmentWorkspaceService.cs
@src/AsterGraph.Editor/Services/GraphFragmentLibraryService.cs
@src/AsterGraph.Editor/Services/GraphClipboardPayloadSerializer.cs
@src/AsterGraph.Editor/Services/IGraphTextClipboardBridge.cs
@src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
@src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
@tests/AsterGraph.Editor.Tests/EditorClipboardAndFragmentCompatibilityTests.cs

<interfaces>
From `src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs`:
```csharp
public sealed record AsterGraphEditorOptions
{
    public GraphWorkspaceService? WorkspaceService { get; init; }
    public GraphFragmentWorkspaceService? FragmentWorkspaceService { get; init; }
    public GraphFragmentLibraryService? FragmentLibraryService { get; init; }
    public IGraphContextMenuAugmentor? ContextMenuAugmentor { get; init; }
    public INodePresentationProvider? NodePresentationProvider { get; init; }
    public IGraphLocalizationProvider? LocalizationProvider { get; init; }
}
```

From `src/AsterGraph.Editor/Services/GraphWorkspaceService.cs` and related files:
```csharp
public GraphWorkspaceService(string? workspacePath = null)
public static string GetDefaultWorkspacePath()
public void Save(GraphDocument document)
public GraphDocument Load()
public bool Exists()

public GraphFragmentWorkspaceService(string? fragmentPath = null)
public static string GetDefaultFragmentPath()
public bool Exists(string? path = null)

public GraphFragmentLibraryService(string? libraryPath = null)
public static string GetDefaultLibraryPath()
public IReadOnlyList<FragmentTemplateInfo> EnumerateTemplates()
public void DeleteTemplate(string path)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish replaceable service and diagnostics contracts with generic default storage resolution</name>
  <read_first>
    .planning/phases/02-runtime-contracts-service-seams/02-CONTEXT.md
    .planning/phases/02-runtime-contracts-service-seams/02-RESEARCH.md
    src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
    src/AsterGraph.Editor/Services/GraphWorkspaceService.cs
    src/AsterGraph.Editor/Services/GraphFragmentWorkspaceService.cs
    src/AsterGraph.Editor/Services/GraphFragmentLibraryService.cs
    src/AsterGraph.Editor/Services/GraphClipboardPayloadSerializer.cs
  </read_first>
  <files>src/AsterGraph.Editor/Services/IGraphWorkspaceService.cs, src/AsterGraph.Editor/Services/IGraphFragmentWorkspaceService.cs, src/AsterGraph.Editor/Services/IGraphFragmentLibraryService.cs, src/AsterGraph.Editor/Services/IGraphClipboardPayloadSerializer.cs, src/AsterGraph.Editor/Services/GraphEditorStorageDefaults.cs, src/AsterGraph.Editor/Diagnostics/IGraphEditorDiagnosticsSink.cs, src/AsterGraph.Editor/Diagnostics/GraphEditorDiagnostic.cs, src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs</files>
  <action>Per D-06 and D-07, formalize the first replaceable service seams around workspace persistence, fragment workspace persistence, fragment template libraries, clipboard payload serialization, and diagnostics, while leaving the existing localization, presentation, context-menu augmentor, and compatibility seams intact. Update `AsterGraphEditorOptions` to accept the new interfaces instead of concrete service types and add the host-facing inputs needed for generic storage defaults: support an explicit storage root override and a diagnostics sink. Per D-08, create a shared `GraphEditorStorageDefaults` resolver that uses `AsterGraphEditorOptions.StorageRootPath` when supplied, otherwise resolves to `LocalApplicationData/AsterGraph/{entry-assembly-or-process-name}`; do not keep or reintroduce any `AsterGraphDemo` fallback. Seed diagnostics with a small stable contract, not a full subsystem: `GraphEditorDiagnostic` should carry `Code`, `Operation`, `Message`, `Severity`, and optional `Exception`, and the sink should be one fire-and-forget method rather than a logging framework.</action>
  <verify>
    <automated>dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj -v minimal</automated>
  </verify>
  <acceptance_criteria>The publishable editor package exposes public interfaces for the targeted service seams, `AsterGraphEditorOptions` can accept replacements through those interfaces, and the default storage resolver no longer names `AsterGraphDemo` anywhere in the path logic.</acceptance_criteria>
  <done>The Phase 2 service seams and generic default-path contract are part of the public `AsterGraph.Editor` surface.</done>
</task>

<task type="auto">
  <name>Task 2: Wire interface-based defaults through the runtime session and prove host replacement behavior</name>
  <read_first>
    src/AsterGraph.Editor/Services/IGraphWorkspaceService.cs
    src/AsterGraph.Editor/Services/IGraphFragmentWorkspaceService.cs
    src/AsterGraph.Editor/Services/IGraphFragmentLibraryService.cs
    src/AsterGraph.Editor/Services/IGraphClipboardPayloadSerializer.cs
    src/AsterGraph.Editor/Diagnostics/IGraphEditorDiagnosticsSink.cs
    src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
    tests/AsterGraph.Editor.Tests/EditorClipboardAndFragmentCompatibilityTests.cs
  </read_first>
  <files>src/AsterGraph.Editor/Services/GraphWorkspaceService.cs, src/AsterGraph.Editor/Services/GraphFragmentWorkspaceService.cs, src/AsterGraph.Editor/Services/GraphFragmentLibraryService.cs, src/AsterGraph.Editor/Services/GraphClipboardPayloadSerializer.cs, src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs, src/AsterGraph.Editor/Runtime/GraphEditorSession.cs, src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs, tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs</files>
  <action>Per D-01, D-02, D-06, and D-08, make the existing default services implement the new interfaces and route factory/session/view-model composition through interface types rather than concrete classes. Keep the compatibility facade alive, but stop constructing demo-branded defaults inside `GraphEditorViewModel`; use the shared storage resolver instead. Add recoverable diagnostics publication to the runtime session for the existing catch-and-status paths that should not crash the host, using stable codes at minimum for `workspace.save.failed`, `workspace.load.failed`, `fragment.export.failed`, `fragment.import.failed`, `clipboard.read.failed`, `clipboard.write.failed`, and `contextmenu.augment.failed`, while still updating `StatusMessage` for compatibility. In `GraphEditorServiceSeamsTests.cs`, cover host-supplied doubles for the new service interfaces, confirm existing localization/presentation/compatibility seams still flow through options unchanged per D-07, and assert that default workspace/fragment/template paths no longer contain `AsterGraphDemo`.</action>
  <verify>
    <automated>dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorServiceSeamsTests" -v minimal</automated>
  </verify>
  <acceptance_criteria>Hosts can swap the targeted services through `AsterGraphEditorOptions`, recoverable failures emit machine-readable diagnostics without dropping compatibility status text, and the default service paths are generic enough for package consumers instead of demo hosts.</acceptance_criteria>
  <done>SERV-01 and SERV-02 are satisfied through tested, public service seams and non-demo default implementations.</done>
</task>

</tasks>

<verification>
- `dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorServiceSeamsTests" -v minimal`
- `dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj -v minimal`
</verification>

<success_criteria>
- Hosts can replace the targeted persistence, template, clipboard payload, and diagnostics services through public editor options/interfaces.
- Default runtime services resolve generic storage roots instead of demo-branded paths.
- Recoverable failure information reaches both runtime diagnostics contracts and compatibility status text.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-contracts-service-seams/02-02-SUMMARY.md`
</output>
