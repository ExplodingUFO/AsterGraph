---
phase: 02-runtime-contracts-service-seams
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
  - src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
  - src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorTransactionTests.cs
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
must_haves:
  truths:
    - "A host can create or obtain a real `IGraphEditorSession` from `AsterGraph.Editor` without touching Avalonia controls."
    - "The session can execute representative editor commands, query runtime state, and publish typed runtime events through the contracts defined in Plan 02-01."
    - "Hosts can batch related mutations through a lightweight scope and receive coherent notifications without a new bus architecture."
  artifacts:
    - path: "src/AsterGraph.Editor/Runtime/GraphEditorSession.cs"
      provides: "Concrete runtime session implementation."
    - path: "src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs"
      provides: "Public `CreateSession(...)` runtime entry point."
    - path: "src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs"
      provides: "Compatibility facade exposing the shared runtime session."
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs"
      provides: "Behavior coverage for runtime commands, queries, and events."
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorTransactionTests.cs"
      provides: "Behavior coverage for lightweight batching."
  key_links:
    - from: "src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs"
      to: "src/AsterGraph.Editor/Runtime/GraphEditorSession.cs"
      via: "Public `CreateSession(...)` composition path"
      pattern: "CreateSession"
    - from: "src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs"
      to: "src/AsterGraph.Editor/Runtime/IGraphEditorSession.cs"
      via: "Compatibility `Session` property"
      pattern: "Session"
    - from: "tests/AsterGraph.Editor.Tests/GraphEditorTransactionTests.cs"
      to: "src/AsterGraph.Editor/Runtime/GraphEditorSession.cs"
      via: "Mutation batching verification"
      pattern: "BeginMutation|Dispose"
---

<objective>
Implement the framework-neutral runtime session and bridge it through the existing host entry points without breaking the compatibility facade.

Purpose: Turn the Wave 0 runtime contracts into a real host API while keeping `GraphEditorViewModel` as the migration-safe shell per D-01 and D-02.
Output: A concrete `GraphEditorSession`, a factory-level session entry point, a `GraphEditorViewModel.Session` bridge, and passing runtime session/batching tests.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-runtime-contracts-service-seams/02-CONTEXT.md
@.planning/phases/02-runtime-contracts-service-seams/02-RESEARCH.md
@.planning/phases/02-runtime-contracts-service-seams/02-VALIDATION.md
@.planning/phases/02-runtime-contracts-service-seams/02-01-PLAN.md
@src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
@src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
@tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
@tests/AsterGraph.Editor.Tests/GraphEditorTransactionTests.cs

<interfaces>
Expected from `02-01`:
```csharp
public interface IGraphEditorSession
{
    IGraphEditorCommands Commands { get; }
    IGraphEditorQueries Queries { get; }
    IGraphEditorEvents Events { get; }
    IGraphEditorMutationScope BeginMutation(string? label = null);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the runtime session root and compatibility bridge</name>
  <read_first>
    .planning/phases/02-runtime-contracts-service-seams/02-01-PLAN.md
    src/AsterGraph.Editor/Runtime/IGraphEditorSession.cs
    src/AsterGraph.Editor/Runtime/IGraphEditorCommands.cs
    src/AsterGraph.Editor/Runtime/IGraphEditorQueries.cs
    src/AsterGraph.Editor/Runtime/IGraphEditorEvents.cs
    src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
    src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
    tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
  </read_first>
  <files>src/AsterGraph.Editor/Runtime/GraphEditorSession.cs, src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs, src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs, tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs</files>
  <action>Per D-01 through D-05, implement `GraphEditorSession` by forwarding into the existing editor behavior instead of cloning it. Add `AsterGraphEditorFactory.CreateSession(AsterGraphEditorOptions options)` and expose the same runtime object from `GraphEditorViewModel.Session` so both new and migrating hosts converge on one session contract. Commands must cover the host-visible edit flows already exposed by the facade, queries must return document snapshot, selection snapshot, viewport snapshot, node positions, compatible targets, and active capabilities, and events must surface the typed document, selection, viewport, fragment, command, and recoverable-failure notifications required by API-03.</action>
  <verify>
    <automated>dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorSessionTests" -v minimal</automated>
  </verify>
  <acceptance_criteria>Hosts can create a runtime session through the factory or retrieve it from the compatibility facade, then execute commands, query state, and subscribe to typed runtime events without depending on Avalonia internals.</acceptance_criteria>
  <done>The public runtime session exists and is reachable through both the new factory path and the retained `GraphEditorViewModel` path.</done>
</task>

<task type="auto">
  <name>Task 2: Implement lightweight batching behavior and convert Wave 0 tests into real coverage</name>
  <read_first>
    src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
    src/AsterGraph.Editor/Runtime/IGraphEditorMutationScope.cs
    tests/AsterGraph.Editor.Tests/GraphEditorTransactionTests.cs
    tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
  </read_first>
  <files>src/AsterGraph.Editor/Runtime/GraphEditorSession.cs, tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs, tests/AsterGraph.Editor.Tests/GraphEditorTransactionTests.cs</files>
  <action>Per D-09 and D-10, implement batching as a pragmatic mutation scope that coalesces runtime notifications until the scope closes, without introducing a command bus, mediator, or heavy event layer. Expand the Wave 0 session and transaction tests from surface assertions into behavior checks that prove command execution, query results, command-executed notifications, recoverable-failure publication, and batched notification behavior work through the public runtime surface.</action>
  <verify>
    <automated>dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorSessionTests|FullyQualifiedName~GraphEditorTransactionTests" -v minimal</automated>
  </verify>
  <acceptance_criteria>The targeted runtime tests pass with real behavior coverage for commands, queries, events, and batching through `IGraphEditorSession`.</acceptance_criteria>
  <done>API-01 through API-04 are implemented through a concrete, tested runtime session surface.</done>
</task>

</tasks>

<verification>
- `dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorSessionTests|FullyQualifiedName~GraphEditorTransactionTests" -v minimal`
- `dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj -v minimal`
</verification>

<success_criteria>
- Hosts can obtain a real runtime session through `AsterGraph.Editor`.
- `GraphEditorViewModel` remains the compatibility facade while delegating runtime access through `Session`.
- Targeted tests prove session commands, queries, events, and batching work through the new public contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-contracts-service-seams/02-02-SUMMARY.md`
</output>
