---
phase: 02-runtime-contracts-service-seams
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AsterGraph.Editor/Services/IGraphWorkspaceService.cs
  - src/AsterGraph.Editor/Services/IGraphFragmentWorkspaceService.cs
  - src/AsterGraph.Editor/Services/IGraphFragmentLibraryService.cs
  - src/AsterGraph.Editor/Services/IGraphClipboardPayloadSerializer.cs
  - src/AsterGraph.Editor/Services/GraphEditorStorageDefaults.cs
  - src/AsterGraph.Editor/Diagnostics/IGraphEditorDiagnosticsSink.cs
  - src/AsterGraph.Editor/Diagnostics/GraphEditorDiagnostic.cs
  - src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs
autonomous: true
requirements:
  - SERV-01
  - SERV-02
must_haves:
  truths:
    - "A host can discover public interfaces for the targeted Phase 2 services instead of depending on concrete editor classes."
    - "The default storage contract uses an explicit `StorageRootPath` override or a package-neutral default, never host-identity inference."
    - "Wave 0 regression coverage exists for service replacement and default storage behavior before runtime/service wiring begins."
  artifacts:
    - path: "src/AsterGraph.Editor/Services/IGraphWorkspaceService.cs"
      provides: "Replaceable workspace persistence contract."
    - path: "src/AsterGraph.Editor/Services/IGraphFragmentWorkspaceService.cs"
      provides: "Replaceable fragment persistence contract."
    - path: "src/AsterGraph.Editor/Services/IGraphFragmentLibraryService.cs"
      provides: "Replaceable template library contract."
    - path: "src/AsterGraph.Editor/Services/IGraphClipboardPayloadSerializer.cs"
      provides: "Replaceable clipboard payload contract."
    - path: "src/AsterGraph.Editor/Services/GraphEditorStorageDefaults.cs"
      provides: "Shared explicit/default storage-root resolver."
    - path: "src/AsterGraph.Editor/Diagnostics/IGraphEditorDiagnosticsSink.cs"
      provides: "Machine-readable diagnostics sink contract."
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs"
      provides: "Wave 0 service seam regression file."
  key_links:
    - from: "src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs"
      to: "new service interfaces"
      via: "Host-owned composition inputs"
      pattern: "IGraphWorkspaceService|IGraphFragmentWorkspaceService|IGraphFragmentLibraryService|IGraphClipboardPayloadSerializer|IGraphEditorDiagnosticsSink|StorageRootPath"
    - from: "src/AsterGraph.Editor/Services/GraphEditorStorageDefaults.cs"
      to: "default services"
      via: "Shared path resolution"
      pattern: "StorageRootPath|LocalApplicationData|AsterGraph"
    - from: "tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs"
      to: "service and storage contracts"
      via: "Wave 0 surface assertions"
      pattern: "StorageRootPath|GraphEditorStorageDefaults|IGraphEditorDiagnosticsSink"
---

<objective>
Publish the first replaceable service contracts and lock down the non-demo storage-default policy before the runtime/service wiring plan starts.

Purpose: Satisfy the contract side of SERV-01 and SERV-02 with explicit service interfaces, diagnostics contracts, and Wave 0 test scaffolding.
Output: Public service and diagnostics interfaces, explicit storage-root inputs on editor options, a package-neutral default-path resolver, and a compile-safe service-seam test file.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-runtime-contracts-service-seams/02-CONTEXT.md
@.planning/phases/02-runtime-contracts-service-seams/02-RESEARCH.md
@.planning/phases/02-runtime-contracts-service-seams/02-VALIDATION.md
@src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
@src/AsterGraph.Editor/Services/GraphWorkspaceService.cs
@src/AsterGraph.Editor/Services/GraphFragmentWorkspaceService.cs
@src/AsterGraph.Editor/Services/GraphFragmentLibraryService.cs
@src/AsterGraph.Editor/Services/GraphClipboardPayloadSerializer.cs
@src/AsterGraph.Editor/Services/IGraphTextClipboardBridge.cs
@tests/AsterGraph.Editor.Tests/EditorClipboardAndFragmentCompatibilityTests.cs

<interfaces>
From `src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs`:
```csharp
public sealed record AsterGraphEditorOptions
{
    public GraphWorkspaceService? WorkspaceService { get; init; }
    public GraphFragmentWorkspaceService? FragmentWorkspaceService { get; init; }
    public GraphFragmentLibraryService? FragmentLibraryService { get; init; }
    public IGraphContextMenuAugmentor? ContextMenuAugmentor { get; init; }
    public INodePresentationProvider? NodePresentationProvider { get; init; }
    public IGraphLocalizationProvider? LocalizationProvider { get; init; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the replaceable service and diagnostics contracts</name>
  <read_first>
    .planning/phases/02-runtime-contracts-service-seams/02-CONTEXT.md
    .planning/phases/02-runtime-contracts-service-seams/02-RESEARCH.md
    src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
    src/AsterGraph.Editor/Services/GraphWorkspaceService.cs
    src/AsterGraph.Editor/Services/GraphFragmentWorkspaceService.cs
    src/AsterGraph.Editor/Services/GraphFragmentLibraryService.cs
    src/AsterGraph.Editor/Services/GraphClipboardPayloadSerializer.cs
  </read_first>
  <files>src/AsterGraph.Editor/Services/IGraphWorkspaceService.cs, src/AsterGraph.Editor/Services/IGraphFragmentWorkspaceService.cs, src/AsterGraph.Editor/Services/IGraphFragmentLibraryService.cs, src/AsterGraph.Editor/Services/IGraphClipboardPayloadSerializer.cs, src/AsterGraph.Editor/Diagnostics/IGraphEditorDiagnosticsSink.cs, src/AsterGraph.Editor/Diagnostics/GraphEditorDiagnostic.cs, src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs</files>
  <action>Per D-06 and D-07, publish the first replaceable service seams around workspace persistence, fragment persistence, template libraries, clipboard payload serialization, and diagnostics while leaving the mature localization, presentation, and context-menu seams intact. Update `AsterGraphEditorOptions` to accept the new interfaces instead of concrete service types and to expose `StorageRootPath` plus a diagnostics sink as explicit host-owned inputs. Keep the diagnostics contract deliberately small: `GraphEditorDiagnostic` should carry stable code, operation, message, severity, and optional exception data, and the sink should be one fire-and-forget publication method rather than a logging framework.</action>
  <verify>
    <automated>dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj -v minimal</automated>
  </verify>
  <acceptance_criteria>The publishable editor package exposes public service/diagnostics interfaces and options-level inputs that let hosts replace the targeted defaults through documented contracts.</acceptance_criteria>
  <done>The contract half of SERV-01 is in place and ready for concrete-service wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Define the explicit storage-default policy and Wave 0 service-seam tests</name>
  <read_first>
    src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
    src/AsterGraph.Editor/Services/GraphWorkspaceService.cs
    src/AsterGraph.Editor/Services/GraphFragmentWorkspaceService.cs
    src/AsterGraph.Editor/Services/GraphFragmentLibraryService.cs
    tests/AsterGraph.Editor.Tests/EditorClipboardAndFragmentCompatibilityTests.cs
  </read_first>
  <files>src/AsterGraph.Editor/Services/GraphEditorStorageDefaults.cs, tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs</files>
  <action>Per D-08 and SERV-02, create `GraphEditorStorageDefaults` so the default services use `AsterGraphEditorOptions.StorageRootPath` when provided and otherwise fall back to one package-neutral root such as `LocalApplicationData/AsterGraph`. Do not inspect entry assembly names, process names, application titles, or any other host identity signal. In `GraphEditorServiceSeamsTests.cs`, create the Wave 0 regression file that names the expected host-replacement and storage-default behaviors this phase will implement, while keeping not-yet-wired runtime/service assertions compile-safe.</action>
  <verify>
    <automated>dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorServiceSeamsTests" -v minimal</automated>
  </verify>
  <acceptance_criteria>The default-path contract is explicit, package-neutral, and free of demo branding or host-identity inference, and the service-seam regression file exists as a concrete test target for downstream plans.</acceptance_criteria>
  <done>Wave 0 now includes the service-seam regression file and the corrected non-demo storage-default contract.</done>
</task>

</tasks>

<verification>
- `dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorServiceSeamsTests" -v minimal`
- `dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj -v minimal`
</verification>

<success_criteria>
- Hosts can see public contracts for the targeted persistence, clipboard payload, and diagnostics seams.
- Default storage behavior is anchored on explicit `StorageRootPath` or one package-neutral default root.
- The service-seam test file exists before concrete runtime/service wiring begins.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-contracts-service-seams/02-03-SUMMARY.md`
</output>
