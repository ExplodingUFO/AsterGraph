---
phase: 02-runtime-contracts-service-seams
plan: 05
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-04
files_modified:
  - tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
  - tools/AsterGraph.HostSample/Program.cs
  - tools/AsterGraph.PackageSmoke/Program.cs
  - docs/host-integration.md
  - README.md
  - src/AsterGraph.Editor/README.md
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - SERV-01
  - SERV-02
must_haves:
  truths:
    - "A host can see one coherent Phase 2 integration story: create or obtain a runtime session, execute commands, query state, subscribe to events, and replace services without Avalonia internals."
    - "The compatibility path remains documented and verified so existing `GraphEditorViewModel` and `GraphEditorView` hosts are not forced into a one-shot rewrite."
    - "Sample and smoke consumers prove the new runtime/session and service seams from outside the main library projects."
  artifacts:
    - path: "tools/AsterGraph.HostSample/Program.cs"
      provides: "Reference host composition for runtime session and service replacement."
    - path: "tools/AsterGraph.PackageSmoke/Program.cs"
      provides: "Consumer smoke markers for session and service seam usage."
    - path: "docs/host-integration.md"
      provides: "Canonical host guidance for the Phase 2 runtime/session and service contracts."
    - path: "src/AsterGraph.Editor/README.md"
      provides: "Package-level runtime/session and service seam documentation."
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs"
      provides: "Regression coverage for compatibility-facade parity alongside the new runtime session."
  key_links:
    - from: "tools/AsterGraph.HostSample/Program.cs"
      to: "AsterGraphEditorFactory.CreateSession(...)"
      via: "Reference host usage"
      pattern: "CreateSession|CompatibilityService"
    - from: "tools/AsterGraph.PackageSmoke/Program.cs"
      to: "runtime session + service overrides"
      via: "Package-consumer smoke markers"
      pattern: "SESSION_|SERVICE_|CompatibilityService"
    - from: "docs/host-integration.md"
      to: "src/AsterGraph.Editor/README.md"
      via: "Shared consumer guidance"
      pattern: "session|commands|queries|events|services"
---

<objective>
Close Phase 2 by proving the new runtime/session and service seams from real host-consumer entry points and documenting the stable migration story.

Purpose: Convert the extracted Phase 2 runtime and service contracts into an external-consumer workflow that matches the package story from Phase 1.
Output: Updated host/sample and package-smoke programs, regression coverage for initialization and migration against the new runtime surface, and aligned consumer documentation.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-runtime-contracts-service-seams/02-CONTEXT.md
@.planning/phases/02-runtime-contracts-service-seams/02-RESEARCH.md
@.planning/phases/02-runtime-contracts-service-seams/02-VALIDATION.md
@.planning/phases/01-consumption-compatibility-guardrails/01-04-SUMMARY.md
@.planning/phases/02-runtime-contracts-service-seams/02-02-PLAN.md
@.planning/phases/02-runtime-contracts-service-seams/02-04-PLAN.md
@docs/host-integration.md
@README.md
@src/AsterGraph.Editor/README.md
@tools/AsterGraph.HostSample/Program.cs
@tools/AsterGraph.PackageSmoke/Program.cs
@tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs
@tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs

<interfaces>
Expected from `02-02` output:
```csharp
public static IGraphEditorSession CreateSession(AsterGraphEditorOptions options)
public IGraphEditorSession Session { get; }
```

Expected from `02-03` / `02-04` output:
```csharp
public interface IGraphWorkspaceService { ... }
public interface IGraphFragmentWorkspaceService { ... }
public interface IGraphFragmentLibraryService { ... }
public interface IGraphClipboardPayloadSerializer { ... }
public interface IGraphEditorDiagnosticsSink
{
    void Publish(GraphEditorDiagnostic diagnostic);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exercise the runtime session and service seams from host-facing consumers</name>
  <read_first>
    tools/AsterGraph.HostSample/Program.cs
    tools/AsterGraph.PackageSmoke/Program.cs
    tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs
    tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
    .planning/phases/01-consumption-compatibility-guardrails/01-04-SUMMARY.md
  </read_first>
  <files>tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs, tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs, tools/AsterGraph.HostSample/Program.cs, tools/AsterGraph.PackageSmoke/Program.cs</files>
  <action>Per D-01, D-03, D-04, D-06, and D-08, update the reference host and smoke consumers to use the new Phase 2 runtime/session path directly. The host sample must obtain a session through `AsterGraphEditorFactory.CreateSession(...)`, execute representative commands and queries without touching Avalonia internals, subscribe to runtime command/failure events, and demonstrate at least one host-supplied service replacement plus diagnostics sink usage while still supplying a host-owned `CompatibilityService`. The package smoke program must print unambiguous markers proving session creation, command/query/event access, service replacement, retained `CompatibilityService` replacement, and retained compatibility-facade paths; keep the legacy/factory markers from Phase 1 and add new `SESSION_*` / `SERVICE_*` markers rather than replacing them. Expand initialization and migration tests so they assert `CreateSession(...)`, `GraphEditorViewModel.Session`, constructor/view compatibility parity, `CompatibilityService` continuity, and service-seam continuity for migrating hosts.</action>
  <verify>
    <automated>dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorInitializationTests|FullyQualifiedName~GraphEditorMigrationCompatibilityTests" -v minimal && dotnet run --project tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj && dotnet run --project tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj</automated>
  </verify>
  <acceptance_criteria>The external host sample and smoke program both prove the new runtime/session and service seams from consumer-facing code, including compatibility-service replacement continuity, and the compatibility regression suites confirm that existing `GraphEditorViewModel`/`GraphEditorView` hosts can adopt the new APIs incrementally.</acceptance_criteria>
  <done>Reference consumers and migration tests exercise the full Phase 2 public surface, including retained compatibility-service replacement, instead of only the Phase 1 factory path.</done>
</task>

<task type="auto">
  <name>Task 2: Align the consumer docs and close with the final verification ring</name>
  <read_first>
    docs/host-integration.md
    README.md
    src/AsterGraph.Editor/README.md
    tools/AsterGraph.HostSample/Program.cs
    tools/AsterGraph.PackageSmoke/Program.cs
    .planning/phases/01-consumption-compatibility-guardrails/01-04-SUMMARY.md
  </read_first>
  <files>docs/host-integration.md, README.md, src/AsterGraph.Editor/README.md</files>
  <action>Document one consistent Phase 2 host story across the root README, host integration guide, and `AsterGraph.Editor` package README. Per D-01 and D-04, explain that hosts now have a public runtime session rooted in `AsterGraph.Editor` with explicit commands, queries, events, and mutation batching, while `GraphEditorViewModel` and `GraphEditorView` remain supported compatibility facades for migration. Per D-06 through D-08, document the replaceable service interfaces, explicit `StorageRootPath` override, package-neutral default storage behavior, and diagnostics sink expectations, and call out that localization, presentation, and context-menu augmentation seams continue to work unchanged. Reserve the full solution test suite for plan-level verification; use this task to align the docs with the host/sample behavior and confirm the consumer story through one targeted smoke command.</action>
  <verify>
    <automated>dotnet run --project tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj</automated>
  </verify>
  <acceptance_criteria>The docs, host sample, and smoke consumer all describe the same runtime/session and service-seam workflow, and the final verification ring can run without relying on the full suite as a task-level gate.</acceptance_criteria>
  <done>Phase 2 is externally consumable, documented, and ready for the final verification ring.</done>
</task>

</tasks>

<verification>
- `dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorSessionTests|FullyQualifiedName~GraphEditorTransactionTests|FullyQualifiedName~GraphEditorServiceSeamsTests|FullyQualifiedName~GraphEditorInitializationTests|FullyQualifiedName~GraphEditorMigrationCompatibilityTests" -v minimal`
- `dotnet run --project tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj`
- `dotnet run --project tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj`
- `dotnet test avalonia-node-map.sln -v minimal`
</verification>

<success_criteria>
- Consumer-facing docs, sample code, and smoke coverage all use the new runtime/session and service seams consistently.
- Compatibility-facade hosts are still supported and explicitly documented.
- The final verification ring proves the publishable editor package still works through both the new host APIs and the retained compatibility facade.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-contracts-service-seams/02-05-SUMMARY.md`
</output>
