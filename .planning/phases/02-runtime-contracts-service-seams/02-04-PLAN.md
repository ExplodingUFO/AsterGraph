---
phase: 02-runtime-contracts-service-seams
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/AsterGraph.Editor/Services/GraphWorkspaceService.cs
  - src/AsterGraph.Editor/Services/GraphFragmentWorkspaceService.cs
  - src/AsterGraph.Editor/Services/GraphFragmentLibraryService.cs
  - src/AsterGraph.Editor/Services/GraphClipboardPayloadSerializer.cs
  - src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
  - src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
  - src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs
autonomous: true
requirements:
  - SERV-01
  - SERV-02
must_haves:
  truths:
    - "A host can replace the targeted persistence, template, clipboard payload, and diagnostics services through `AsterGraph.Editor` options."
    - "The default runtime services use the shared package-neutral storage defaults instead of demo-branded or host-identity-derived paths."
    - "Recoverable runtime failures reach both machine-readable diagnostics and the compatibility `StatusMessage` path."
  artifacts:
    - path: "src/AsterGraph.Editor/Services/GraphWorkspaceService.cs"
      provides: "Interface-backed default workspace service."
    - path: "src/AsterGraph.Editor/Services/GraphFragmentWorkspaceService.cs"
      provides: "Interface-backed default fragment service."
    - path: "src/AsterGraph.Editor/Services/GraphFragmentLibraryService.cs"
      provides: "Interface-backed default template library service."
    - path: "src/AsterGraph.Editor/Services/GraphClipboardPayloadSerializer.cs"
      provides: "Interface-backed default clipboard payload serializer."
    - path: "src/AsterGraph.Editor/Runtime/GraphEditorSession.cs"
      provides: "Runtime diagnostics publication through the session."
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs"
      provides: "Behavior coverage for service replacement and default-path rules."
  key_links:
    - from: "src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs"
      to: "new service interfaces"
      via: "Interface-based composition path"
      pattern: "IGraphWorkspaceService|IGraphFragmentWorkspaceService|IGraphFragmentLibraryService|IGraphClipboardPayloadSerializer|IGraphEditorDiagnosticsSink"
    - from: "src/AsterGraph.Editor/Runtime/GraphEditorSession.cs"
      to: "src/AsterGraph.Editor/Diagnostics/IGraphEditorDiagnosticsSink.cs"
      via: "Recoverable failure publication"
      pattern: "workspace.save.failed|workspace.load.failed|fragment.export.failed|fragment.import.failed|clipboard.read.failed|clipboard.write.failed|contextmenu.augment.failed"
    - from: "tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs"
      to: "src/AsterGraph.Editor/Services/GraphEditorStorageDefaults.cs"
      via: "Non-demo default-path assertions"
      pattern: "StorageRootPath|AsterGraph"
---

<objective>
Wire the new service contracts through the runtime/session stack and prove that default implementations remain package-neutral and host-replaceable.

Purpose: Finish SERV-01 and SERV-02 by connecting the contract work from Plan 02-03 into the concrete runtime and compatibility layers.
Output: Interface-backed default services, interface-based composition through the runtime/session path, machine-readable diagnostics publication, and passing service-seam behavior tests.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-runtime-contracts-service-seams/02-CONTEXT.md
@.planning/phases/02-runtime-contracts-service-seams/02-RESEARCH.md
@.planning/phases/02-runtime-contracts-service-seams/02-VALIDATION.md
@.planning/phases/02-runtime-contracts-service-seams/02-02-PLAN.md
@.planning/phases/02-runtime-contracts-service-seams/02-03-PLAN.md
@src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
@src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
@tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs

<interfaces>
Expected from `02-03`:
```csharp
public interface IGraphWorkspaceService { ... }
public interface IGraphFragmentWorkspaceService { ... }
public interface IGraphFragmentLibraryService { ... }
public interface IGraphClipboardPayloadSerializer { ... }
public interface IGraphEditorDiagnosticsSink
{
    void Publish(GraphEditorDiagnostic diagnostic);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the default services and composition path to the new interfaces</name>
  <read_first>
    .planning/phases/02-runtime-contracts-service-seams/02-03-PLAN.md
    src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
    src/AsterGraph.Editor/Services/GraphWorkspaceService.cs
    src/AsterGraph.Editor/Services/GraphFragmentWorkspaceService.cs
    src/AsterGraph.Editor/Services/GraphFragmentLibraryService.cs
    src/AsterGraph.Editor/Services/GraphClipboardPayloadSerializer.cs
  </read_first>
  <files>src/AsterGraph.Editor/Services/GraphWorkspaceService.cs, src/AsterGraph.Editor/Services/GraphFragmentWorkspaceService.cs, src/AsterGraph.Editor/Services/GraphFragmentLibraryService.cs, src/AsterGraph.Editor/Services/GraphClipboardPayloadSerializer.cs, src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs</files>
  <action>Per D-06 through D-08, make the existing default persistence, template, and clipboard payload services implement the new public interfaces and wire the factory through interface types instead of concrete classes. Use `GraphEditorStorageDefaults` for all default path resolution. Do not recreate `AsterGraphDemo`, do not infer host identity, and do not leave any direct concrete-type requirement in the public options path.</action>
  <verify>
    <automated>dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj -v minimal</automated>
  </verify>
  <acceptance_criteria>Hosts can pass replacements through `AsterGraphEditorOptions`, and the default services resolve package-neutral storage paths through the shared defaults helper.</acceptance_criteria>
  <done>The concrete default services and factory composition path are interface-driven instead of concrete-type-driven.</done>
</task>

<task type="auto">
  <name>Task 2: Publish recoverable diagnostics through the runtime session and prove host replacement behavior</name>
  <read_first>
    src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
    src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
    src/AsterGraph.Editor/Diagnostics/IGraphEditorDiagnosticsSink.cs
    tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs
    tests/AsterGraph.Editor.Tests/EditorClipboardAndFragmentCompatibilityTests.cs
  </read_first>
  <files>src/AsterGraph.Editor/Runtime/GraphEditorSession.cs, src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs, tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs</files>
  <action>Per D-01, D-02, and D-06 through D-08, keep the compatibility facade alive while routing recoverable failure publication through the new diagnostics sink and runtime session. Use stable codes at minimum for `workspace.save.failed`, `workspace.load.failed`, `fragment.export.failed`, `fragment.import.failed`, `clipboard.read.failed`, `clipboard.write.failed`, and `contextmenu.augment.failed`, while preserving `StatusMessage` updates for compatibility. Expand `GraphEditorServiceSeamsTests.cs` from Wave 0 scaffolding into behavior coverage that proves service replacement works, legacy seams like localization, presentation, and context-menu augmentation still flow through unchanged per D-07, and default paths no longer depend on demo branding or host identity.</action>
  <verify>
    <automated>dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorServiceSeamsTests" -v minimal</automated>
  </verify>
  <acceptance_criteria>The service-seam regression suite passes, recoverable failures emit machine-readable diagnostics plus compatibility status text, and the default path rules remain package-neutral.</acceptance_criteria>
  <done>SERV-01 and SERV-02 are fully implemented through tested, public service seams and runtime diagnostics publication.</done>
</task>

</tasks>

<verification>
- `dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorServiceSeamsTests" -v minimal`
- `dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj -v minimal`
</verification>

<success_criteria>
- Hosts can replace the targeted runtime services through public editor options/interfaces.
- Default runtime services rely on explicit or package-neutral storage roots only.
- Recoverable failures reach both diagnostics contracts and compatibility status text.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-contracts-service-seams/02-04-SUMMARY.md`
</output>
