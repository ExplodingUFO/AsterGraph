---
phase: 22-plugin-composition-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
  - src/AsterGraph.Editor/Plugins/IGraphEditorPlugin.cs
  - src/AsterGraph.Editor/Plugins/GraphEditorPluginDescriptor.cs
  - src/AsterGraph.Editor/Plugins/GraphEditorPluginRegistration.cs
  - src/AsterGraph.Editor/Plugins/GraphEditorPluginBuilder.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorPluginContractsTests.cs
requirements:
  - PLUG-01
autonomous: true
---

<objective>
Define the first stable public plugin contract and registration surface so hosts can declare plugins through the canonical factory/options path without depending on Avalonia types, retained MVVM objects, or ad-hoc reflection code.
</objective>

<scope>
- Add public plugin contract types in `AsterGraph.Editor`.
- Extend `AsterGraphEditorOptions` with explicit plugin registration input(s).
- Keep the new public surface additive and runtime-first.
</scope>

<read_first>
- .planning/phases/22-plugin-composition-contracts/22-CONTEXT.md
- .planning/phases/22-plugin-composition-contracts/22-RESEARCH.md
- src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
- src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
- src/AsterGraph.Editor/Runtime/IGraphEditorSession.cs
- src/AsterGraph.Editor/Runtime/IGraphEditorQueries.cs
- tests/AsterGraph.Editor.Tests/GraphEditorInitializationTests.cs
- tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
</read_first>

<action>
- Introduce a new public `AsterGraph.Editor.Plugins` surface that includes the minimum stable contract needed for later phases: a plugin interface, plugin descriptor DTO, plugin registration model, and a builder/contribution shape for additive runtime-facing seams.
- Ensure the public plugin surface is free of Avalonia control types and does not require `GraphEditorViewModel` references. Plugins must target canonical runtime/contracts only.
- Extend `AsterGraphEditorOptions` with explicit plugin registration input that can represent one or more plugins without requiring host-side reflection against internal loader code.
- Keep existing service/provider options untouched and additive. The new plugin input should coexist with the current host seam properties instead of replacing them.
- Add focused contract tests that lock the new public shape and fail if the plugin surface drifts toward Avalonia, MVVM, or compatibility-only APIs.
</action>

<acceptance_criteria>
- `AsterGraphEditorOptions` exposes an explicit public plugin registration input
- `src/AsterGraph.Editor/Plugins/` contains the stable public contract surface required for later plugin phases
- the public plugin contract is runtime-first and free of Avalonia or `GraphEditorViewModel` dependencies
- focused contract tests fail if plugin registration requires internal object knowledge or incompatible UI types
</acceptance_criteria>

<verification>
- dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorPluginContractsTests|FullyQualifiedName~GraphEditorInitializationTests" -v minimal
- dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj --nologo -v minimal
</verification>

<output>
After completion, create `.planning/phases/22-plugin-composition-contracts/22-01-SUMMARY.md`
</output>
