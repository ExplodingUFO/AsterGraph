---
phase: 07-runtime-host-boundary-completion
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs
  - tools/AsterGraph.HostSample/Program.cs
  - tools/AsterGraph.PackageSmoke/Program.cs
  - README.md
  - docs/host-integration.md
  - src/AsterGraph.Editor/README.md
autonomous: true
requirements:
  - HOST-01
  - HOST-02
must_haves:
  truths:
    - "The phase proof ring shows that a host can stay on `AsterGraphEditorFactory.CreateSession(...)` for selection, movement, connection, viewport, and DTO-based compatible-target workflows without dropping to direct `GraphEditorViewModel` calls."
    - "Migration parity proves the retained legacy editor facade, the factory-created editor facade, and the canonical factory-created session all preserve the same Phase 7 runtime semantics where they overlap."
    - "HostSample and PackageSmoke emit explicit Phase 7 runtime-boundary evidence instead of leaving the new host story implicit."
  artifacts:
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs"
      provides: "Migration parity proof for legacy editor, factory editor, and factory session runtime paths."
      contains: "CreateSession|SetSelection|SetNodePositions|GetCompatiblePortTargets"
    - path: "tools/AsterGraph.HostSample/Program.cs"
      provides: "Human-readable runtime-first host walkthrough for Phase 7."
      contains: "CreateSession|PendingConnectionChanged|GetCompatiblePortTargets"
    - path: "tools/AsterGraph.PackageSmoke/Program.cs"
      provides: "Machine-checkable Phase 7 host-boundary smoke markers."
      contains: "SESSION_FACTORY_OK|RUNTIME_SELECTION_OK|RUNTIME_CONNECTION_OK|RUNTIME_DTO_QUERY_OK"
    - path: "docs/host-integration.md"
      provides: "Updated host guidance for runtime-first custom UI usage."
      contains: "CreateSession|GraphEditorViewModel.Session|compatibility-only"
  key_links:
    - from: "tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs"
      to: "tools/AsterGraph.HostSample/Program.cs"
      via: "same runtime-first flow proven in tests and sample output"
      pattern: "CreateSession|SetSelection|SetNodePositions|StartConnection|CompleteConnection"
    - from: "tools/AsterGraph.PackageSmoke/Program.cs"
      to: "src/AsterGraph.Editor/Runtime/IGraphEditorQueries.cs"
      via: "machine-checkable proof of DTO query adoption"
      pattern: "GetCompatiblePortTargets"
    - from: "README.md"
      to: "docs/host-integration.md"
      via: "aligned migration guidance for canonical session-first usage"
      pattern: "CreateSession|GraphEditorViewModel.Session"
---

<objective>
Close Phase 7 with the external proof ring: migration parity, HostSample, PackageSmoke, and runtime-first host guidance that all demonstrate the new core session boundary.

Purpose: prove the additive runtime hardening is real for consumers, not just implemented locally inside `AsterGraph.Editor`.
Output: updated migration/service-seam tests, runtime-first sample and smoke output, and aligned docs that prefer the new session-first workflow while keeping compatibility facades documented.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-runtime-host-boundary-completion/07-CONTEXT.md
@.planning/phases/07-runtime-host-boundary-completion/07-RESEARCH.md
@.planning/phases/02-runtime-contracts-service-seams/02-05-SUMMARY.md
@tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
@tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs
@tools/AsterGraph.HostSample/Program.cs
@tools/AsterGraph.PackageSmoke/Program.cs
@README.md
@docs/host-integration.md
@src/AsterGraph.Editor/README.md

<interfaces>
Current host proof baseline:
```csharp
var runtimeSession = AsterGraphEditorFactory.CreateSession(new AsterGraphEditorOptions { ... });
var compatibleTargets = runtimeSession.Queries.GetCompatibleTargets("sample-source-001", "result");
using (runtimeSession.BeginMutation("host-sample-batch"))
{
    runtimeSession.Commands.AddNode(new NodeDefinitionId("host.sample.sink"), new GraphPoint(680, 200));
    runtimeSession.Commands.PanBy(12, 18);
}
```

Current package smoke proof baseline:
```csharp
Console.WriteLine($"SESSION_FACTORY_OK:{session.Queries.CreateDocumentSnapshot().Nodes.Count}:{string.Join(\",\", commandIds)}");
Console.WriteLine($"SESSION_EVENTS_OK:{documentChanges}:{viewportChanges}:{failureCode ?? \"<none>\"}");
Console.WriteLine($"COMPATIBILITY_SERVICE_OK:{compatibleTargets.Count}:{compatibilityService.EvaluateCalls}");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend migration and service-seam proof to cover the full Phase 7 runtime workflow</name>
  <files>tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs, tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs</files>
  <action>Per D-11 and D-12, make the Phase 7 proof ring explicit in the focused test layer before touching consumer-facing samples. Expand `GraphEditorMigrationCompatibilityTests.cs` so legacy editor, factory editor, and factory session paths all prove the new overlap semantics: selection mutation by ID, node position mutation, pending connection start/complete/cancel, connection deletion, viewport-centering helpers, and DTO-based compatible-target queries. Keep the retained `GraphEditorViewModel` path alive as a compatibility facade, but make the canonical assertions flow through `CreateSession(...)` and `GraphEditorViewModel.Session`. Update `GraphEditorServiceSeamsTests.cs` so host-supplied compatibility services are exercised through the new DTO query path rather than the old MVVM-typed runtime query. Validate through a temporary focused harness at `%TEMP%\\astergraph-phase7-proof-validation\\AsterGraph.Phase7.Proof.Validation.csproj`.</action>
  <verify>
    <automated>dotnet test %TEMP%\astergraph-phase7-proof-validation\AsterGraph.Phase7.Proof.Validation.csproj -v minimal</automated>
  </verify>
  <acceptance_criteria>Focused parity tests prove the canonical session-first host story and confirm that host-supplied services still participate through the new DTO-oriented runtime path.</acceptance_criteria>
  <done>Phase 7 has a migration-proof test ring for runtime-first hosts, not just isolated editor behavior coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Rework HostSample, PackageSmoke, and docs to make the Phase 7 host boundary visible</name>
  <files>tools/AsterGraph.HostSample/Program.cs, tools/AsterGraph.PackageSmoke/Program.cs, README.md, docs/host-integration.md, src/AsterGraph.Editor/README.md</files>
  <action>Update the consumer-facing proof so the new runtime boundary is obvious and reproducible. In `HostSample`, demonstrate a full session-first custom-host flow using the new runtime commands, pending-connection event/query path, DTO-based compatible-target query, and retained `GraphEditorViewModel.Session` parity. In `PackageSmoke`, add explicit machine-checkable Phase 7 markers such as `RUNTIME_SELECTION_OK`, `RUNTIME_CONNECTION_OK`, `RUNTIME_VIEWPORT_OK`, and `RUNTIME_DTO_QUERY_OK`; keep existing markers intact. Update the root README, `docs/host-integration.md`, and `src/AsterGraph.Editor/README.md` so they all describe the same story: `CreateSession(...)` is now sufficient for core runtime interaction ownership, `GraphEditorViewModel.Session` remains the staged-migration compatibility bridge, and any MVVM-typed compatibility query is legacy-only for menu/editor-internal callers. Do not redesign menu, inspector, or presentation seams here per D-02.</action>
  <verify>
    <automated>dotnet run --project tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj</automated>
  </verify>
  <acceptance_criteria>Sample output, smoke markers, and docs all tell the same Phase 7 runtime-first host story and visibly prove the new host boundary without hiding the compatibility path.</acceptance_criteria>
  <done>The Phase 7 proof ring is externally visible through tests, HostSample, PackageSmoke, and aligned host guidance.</done>
</task>

</tasks>

<verification>
- `dotnet test %TEMP%\astergraph-phase7-proof-validation\AsterGraph.Phase7.Proof.Validation.csproj -v minimal`
- `dotnet run --project tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj`
- `dotnet run --project tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj`
- `rg -n "CreateSession|GraphEditorViewModel.Session|SetSelection|SetNodePositions|GetPendingConnectionSnapshot|GetCompatiblePortTargets|compatibility-only" README.md docs/host-integration.md src/AsterGraph.Editor/README.md`
</verification>

<success_criteria>
- Migration tests prove the retained legacy path and the canonical session path share the same Phase 7 runtime semantics where intended.
- HostSample and PackageSmoke visibly demonstrate the new session-first workflow and DTO-based compatibility queries.
- Consumer docs describe the new runtime-first host story without expanding scope into Phase 8 or Phase 9 seam redesign.
</success_criteria>

<output>
After completion, create `.planning/phases/07-runtime-host-boundary-completion/07-03-SUMMARY.md`
</output>

