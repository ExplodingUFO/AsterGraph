# Plan 42-02

## Objective

Move the plugin proof suites onto the shared resolver so clean Release builds and hosted runners use the same payload path.

## Tasks

1. Replace duplicated `GetSamplePluginAssemblyPath()` implementations with the shared helper.
2. Keep the current plugin coverage intact while removing the hidden Debug-only assumption.
3. Re-run focused plugin suites and fix any remaining parity issues caused by the new path handling.
