# Plan 42-01

## Objective

Add a focused resolver test that fails under the old hard-coded `Debug/net9.0` convention and proves the new path logic tracks the active test output.

## Tasks

1. Add a shared test helper for locating `AsterGraph.TestPlugins` artifacts from the current test run.
2. Add focused tests that assert the resolved artifact path exists and uses the current configuration/framework.
3. Run the focused test first and capture the failing state before implementation.
