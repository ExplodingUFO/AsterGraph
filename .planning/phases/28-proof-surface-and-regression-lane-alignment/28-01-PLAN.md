---
phase: 28-proof-surface-and-regression-lane-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - avalonia-node-map.sln
  - .planning/codebase/STACK.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/TESTING.md
requirements:
  - PROOF-01
autonomous: true
---

<objective>
Align the tracked proof-tool entry points with the live tree by making `ScaleSmoke` part of the tracked verification surface and removing stale `HostSample` claims from current planning/codebase maps.
</objective>

<scope>
- Add `AsterGraph.ScaleSmoke` to the solution under the existing `tools` folder.
- Remove stale `HostSample` references from current planning/codebase source-of-truth documents.
- Keep the current docs/automation claim that `PackageSmoke` and `ScaleSmoke` are the live proof tools.
</scope>

<must_haves>
- Fresh-worktree automation can restore and build the `net8.0` proof-tool lane without failing on `ScaleSmoke` assets.
- Current planning/codebase maps stop describing `HostSample` as a live project.
- The tracked solution and tracked automation describe the same proof-tool surface.
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Add `ScaleSmoke` to the tracked solution surface</name>
    <files>
      avalonia-node-map.sln
    </files>
    <read_first>
      avalonia-node-map.sln
      tools/AsterGraph.ScaleSmoke/AsterGraph.ScaleSmoke.csproj
      tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj
      eng/ci.ps1
    </read_first>
    <action>
      Update `avalonia-node-map.sln` so the `tools` solution folder includes `tools/AsterGraph.ScaleSmoke/AsterGraph.ScaleSmoke.csproj` as a tracked project alongside `AsterGraph.PackageSmoke`. Preserve the existing solution-folder layout and add the necessary `Project(...)` entry, project GUID wiring, and `NestedProjects` mapping so solution restore/build includes `ScaleSmoke` in fresh worktrees.
    </action>
    <acceptance_criteria>
      `avalonia-node-map.sln` contains `AsterGraph.ScaleSmoke`
      `avalonia-node-map.sln` contains `tools\\AsterGraph.ScaleSmoke\\AsterGraph.ScaleSmoke.csproj`
      `pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane all -Framework net8.0 -Configuration Release` exits 0
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Task 2: Remove stale `HostSample` claims from current planning/codebase maps</name>
    <files>
      .planning/codebase/STACK.md
      .planning/codebase/STRUCTURE.md
      .planning/codebase/TESTING.md
    </files>
    <read_first>
      .planning/codebase/STACK.md
      .planning/codebase/STRUCTURE.md
      .planning/codebase/TESTING.md
      .planning/phases/28-proof-surface-and-regression-lane-alignment/28-CONTEXT.md
    </read_first>
    <action>
      Update the current planning/codebase reference docs so they no longer describe `tools/AsterGraph.HostSample` as a live project. Replace those references with the real current proof surface: `AsterGraph.PackageSmoke`, `AsterGraph.ScaleSmoke`, the sample-only `AsterGraph.Demo`, and the current test projects. Also refresh any stale roadmap-position text in those files if needed so they reflect the current v1.5 state rather than older milestone progress.
    </action>
    <acceptance_criteria>
      `.planning/codebase/STACK.md` does not contain `tools/AsterGraph.HostSample`
      `.planning/codebase/STRUCTURE.md` does not contain `tools/AsterGraph.HostSample`
      `.planning/codebase/TESTING.md` does not contain `tools/AsterGraph.HostSample`
      `.planning/codebase/STRUCTURE.md` contains `tools/AsterGraph.ScaleSmoke`
      `.planning/codebase/TESTING.md` contains `tools/AsterGraph.ScaleSmoke`
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane all -Framework net8.0 -Configuration Release
</verification>

<output>
After completion, create `.planning/phases/28-proof-surface-and-regression-lane-alignment/28-01-SUMMARY.md`
</output>
