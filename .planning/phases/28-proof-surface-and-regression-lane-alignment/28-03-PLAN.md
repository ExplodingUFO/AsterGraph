---
phase: 28-proof-surface-and-regression-lane-alignment
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - README.md
  - docs/quick-start.md
  - docs/host-integration.md
requirements:
  - PROOF-01
autonomous: true
---

<objective>
Refresh public proof and integration docs so they describe the same live proof-tool surface and the same core-vs-demo regression split now encoded in the tree and automation.
</objective>

<scope>
- Update README proof commands and verification guidance.
- Update quick-start and host-integration docs so they point to the current proof tools and the new separated regression lanes.
- Keep the docs aligned with the sample-only status of `AsterGraph.Demo`.
</scope>

<must_haves>
- Public docs no longer imply stale proof tools.
- Public docs describe both live proof tools and the new core-vs-demo regression distinction.
- Public docs stay consistent with the tracked CI/script entry point from Phase 27.
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Synchronize public docs with the live proof surface and regression lanes</name>
    <files>
      README.md
      docs/quick-start.md
      docs/host-integration.md
    </files>
    <read_first>
      README.md
      docs/quick-start.md
      docs/host-integration.md
      eng/ci.ps1
      tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj
      tests/AsterGraph.Demo.Tests/AsterGraph.Demo.Tests.csproj
      tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj
      tools/AsterGraph.ScaleSmoke/AsterGraph.ScaleSmoke.csproj
    </read_first>
    <action>
      Update `README.md`, `docs/quick-start.md`, and `docs/host-integration.md` so they describe the live proof surface as `PackageSmoke`, `ScaleSmoke`, the sample-only `AsterGraph.Demo`, the core SDK regression lane (`AsterGraph.Editor.Tests` plus `AsterGraph.Serialization.Tests`), and the demo/sample regression lane (`AsterGraph.Demo.Tests`). Where the docs list validation commands, make them match the Phase 27 script-first posture by pointing readers to `eng/ci.ps1` and the now-separated regression commands rather than implying one undifferentiated test lane.
    </action>
    <acceptance_criteria>
      `README.md` contains `AsterGraph.Demo.Tests`
      `README.md` contains `eng/ci.ps1`
      `docs/quick-start.md` contains `PackageSmoke`
      `docs/quick-start.md` contains `ScaleSmoke`
      `docs/host-integration.md` contains `AsterGraph.Demo.Tests`
      `docs/host-integration.md` contains `eng/ci.ps1`
      `pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane all -Framework all -Configuration Release` exits 0
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane all -Framework all -Configuration Release
</verification>

<output>
After completion, create `.planning/phases/28-proof-surface-and-regression-lane-alignment/28-03-SUMMARY.md`
</output>
