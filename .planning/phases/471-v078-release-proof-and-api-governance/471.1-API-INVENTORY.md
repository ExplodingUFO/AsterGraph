# Phase 471.1 Public API Inventory

Bead: `avalonia-node-map-v78.6.1`

Status: complete

Scope: public API inventory and baseline classification only. No runtime code, examples, CI workflow, or bead state changed.

## Baseline Check

Compared v0.78 work after Phase 466 against the v0.77 completion point (`749bb5d`). The only publishable-package public API drift found by the current API validator is:

| Symbol | Package | Classification | Reason |
| --- | --- | --- | --- |
| `ViewportVisibleSceneProjection.ToInvalidationBudgetMarker(ViewportVisibleSceneProjection next)` | `AsterGraph.Editor` | Stable canonical, viewport projection proof | Machine-readable visible-scene invalidation evidence for Phase 467. It extends the existing viewport projection proof route and does not create a renderer, query language, background index, fallback layer, or adapter-specific runtime contract. |

## Inventory Updates

- `eng/public-api-baseline.txt` was regenerated with the existing API gate for `Release` `net9.0`.
- `docs/en/public-api-inventory.md` and `docs/zh-CN/public-api-inventory.md` classify the new marker method under `Viewport projection proof`.
- Existing package scope remains `AsterGraph.Abstractions`, `AsterGraph.Core`, `AsterGraph.Editor`, and `AsterGraph.Avalonia`.

## Verification

Required focused command:

```powershell
.\eng\validate-public-api-surface.ps1 -Configuration Release -Framework net9.0
```

Expected markers:

- `PUBLIC_API_SURFACE_OK:4122:net9.0`
- `PUBLIC_API_SCOPE_OK:AsterGraph.Abstractions,AsterGraph.Core,AsterGraph.Editor,AsterGraph.Avalonia`
- `PUBLIC_API_GUIDANCE_OK:True`
- `PUBLIC_API_DIFF_GATE_OK:True`
- `PUBLIC_API_USAGE_GUIDANCE_OK:True`
- `PUBLIC_API_STABILITY_SCOPE_OK:True`

## Integration Notes

- Parent Phase 471 can treat v0.78 API drift as classified after this artifact and the API gate pass.
- Docs/examples release gate owners should not need to reclassify this method unless they change public API wording again.
- CI-sensitive .NET 8/9/10 proof remains a separate Phase 471 responsibility.
