# Phase 471.2 Docs And Examples Release Gate

Bead: `avalonia-node-map-v78.6.2`

## Scope

Verify release-facing docs, examples, templates, and cookbook content for Phase 471 without touching API baseline or CI workflow files.

This pass covers the REL-02 documentation requirement from `.planning/REQUIREMENTS.md`: public docs and examples explain supported v0.78 contracts without naming external inspiration projects.

## Source Anchors

- `.planning/REQUIREMENTS.md` keeps v0.78 non-goals explicit: no generated runnable Demo/cookbook code execution, marketplace/sandbox claims, WPF parity, adapter expansion, or external inspiration names.
- `.planning/phases/470-cookbook-component-showcase/470-HANDOFF.md` assigns the docs/examples gate to Phase 471 and says not to add fallback layers, compatibility shims, generated runnable cookbook code, Demo package-boundary claims, or external inspiration project names.
- `docs/en/demo-cookbook.md` and `docs/zh-CN/demo-cookbook.md` map v0.78 rendering/viewport, customization, and spatial authoring routes back to source/test/proof anchors while stating that Demo remains proof/sample only.

## Verification Commands

```powershell
$externalNameHits = rg -n "<external-inspiration-name-pattern-set>" README.md README.zh-CN.md docs templates tools src -g "*.md" -g "*.cs" -g "*.axaml" -g "*.json"
if ($LASTEXITCODE -eq 1) { "PASS: no prohibited external inspiration names" } elseif ($LASTEXITCODE -eq 0) { $externalNameHits; throw "external inspiration names found" } else { exit $LASTEXITCODE }

$claimHits = rg -n "(?i)(generated (runnable|production|adapter|runtime)|runnable demo code|demo code.*(runnable|generated|adapter|runtime)|production-ready|battle-tested|drop-in|guaranteed|guarantee(s)? (runtime|adapter|demo|production))" README.md README.zh-CN.md docs templates tools src -g "*.md" -g "*.cs" -g "*.axaml" -g "*.json" | rg -v "(?i)(not|no |does not|do not|without|is not|不是|不新增|不代表|不引入|没有)"
if ($LASTEXITCODE -eq 1) { "PASS: no unsupported positive generated runnable Demo-code/adapter/runtime claims" } elseif ($LASTEXITCODE -eq 0) { $claimHits; throw "unsupported positive claim found" } else { exit $LASTEXITCODE }

rg -n "(?i)(not .*execution engine|no .*execution engine|does not .*execution engine|not .*marketplace|no .*marketplace|does not .*marketplace|not .*sandbox|no .*sandbox|does not .*sandbox|not .*WPF parity|no .*WPF parity|does not .*WPF parity|not .*GA|no .*GA|does not .*GA|not .*runtime route|no .*runtime route|does not .*runtime route|not .*second runtime model|no .*second runtime model|does not .*second runtime model)" README.md README.zh-CN.md docs templates tools src -g "*.md" -g "*.cs" -g "*.axaml" -g "*.json" | Measure-Object -Line

git diff --check
```

## Results

- External inspiration name scan passed. The only initial broad-scan noise came from `CommunityToolkit.Mvvm` package imports and generic words such as "concrete"; the narrowed prohibited-name scan returned no matches.
- Unsupported generated runnable Demo-code/adapter/runtime claim scan passed. No positive claim matched the guarded phrases.
- Boundary-language scan found release-facing negative claims that explicitly reject unsupported promises, including no execution engine, marketplace, sandbox, WPF parity, GA claim, new runtime route, or second runtime model. These are expected guardrails, not support expansion.
- `git diff --check` passed.

## Release Gate Decision

Docs/examples gate is clean for `avalonia-node-map-v78.6.2`.

No docs wording changes were needed outside this proof artifact. No API baseline, CI workflow, code, template, or cookbook source files were changed by this pass.

## Residual Risk

This is a text scan and source-backed docs review. It does not replace the separate Phase 471 API baseline classification, CI-sensitive .NET 8/9/10 proof, or milestone closeout synthesis beads.
