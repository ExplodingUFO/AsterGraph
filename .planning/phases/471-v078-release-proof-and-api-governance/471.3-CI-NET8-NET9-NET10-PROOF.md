# 471.3 CI .NET 8/9/10 Proof

Bead: `avalonia-node-map-v78.6.3`

Scope: CI-sensitive .NET 8/9/10 release proof for Phase 471, with emphasis on .NET 10. This pass did not change CI configuration because the existing workflow and release script already exercise the intended framework coverage.

## CI Coverage

- `.github/workflows/ci.yml` has a `framework-matrix` job on `windows-latest` for `net8.0`, `net9.0`, and `net10.0`, passing each value to `.\eng\ci.ps1 -Lane all -Framework ${{ matrix.framework }} -Configuration Release`.
- `.github/workflows/ci.yml` also has Linux and macOS validation jobs that run `./eng/ci.ps1 -Lane all -Framework all -Configuration Release`.
- `.github/workflows/ci.yml` gates `release-validation` on `hygiene-proof`, `framework-matrix`, `linux-validation`, `macos-validation`, and `contract-proof`.
- `.github/workflows/release.yml` runs `.\eng\ci.ps1 -Lane release -Framework all -Configuration Release` for prerelease validation and repeats Linux `-Framework all` validation.
- `global.json` pins the SDK band to `10.0.201` with `rollForward: latestFeature`; local verification resolved SDK `10.0.203`.

## Project Target Coverage

- Publishable libraries target all three frameworks:
  - `src/AsterGraph.Abstractions/AsterGraph.Abstractions.csproj`: `net8.0;net9.0;net10.0`
  - `src/AsterGraph.Core/AsterGraph.Core.csproj`: `net8.0;net9.0;net10.0`
  - `src/AsterGraph.Editor/AsterGraph.Editor.csproj`: `net8.0;net9.0;net10.0`
  - `src/AsterGraph.Avalonia/AsterGraph.Avalonia.csproj`: `net8.0;net9.0;net10.0`
- `eng/ci.ps1` maps test projects to `net8.0` and `net9.0`; the `net10.0` lane builds the publishable libraries and runs `Invoke-HostSample -TargetFramework net10.0`.
- `tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj` switches to `net8.0;net10.0` when `/p:EnableNet10ConsumerProof=true`, which is how the CI script proves the .NET 10 consumer route.
- `tests/AsterGraph.Demo.Tests/CrossPlatformHostPackagingCiTests.cs` asserts the workflow matrix, OS validation lanes, release dependency ordering, and net10 packaged HostSample proof marker wiring.

## Local Verification

Environment:

- `dotnet --list-sdks`: `9.0.308`, `10.0.203`
- `dotnet --info`: SDK `10.0.203`; runtimes include `Microsoft.NETCore.App` `8.0.22`, `8.0.26`, `9.0.11`, `9.0.15`, and `10.0.7`.

Commands:

- `dotnet build src\AsterGraph.Avalonia\AsterGraph.Avalonia.csproj -c Release -m:1 --nologo`
  - Passed for `net8.0`, `net9.0`, and `net10.0`.
  - Result: 0 warnings, 0 errors.
- `dotnet test tests\AsterGraph.Demo.Tests\AsterGraph.Demo.Tests.csproj -c Release --filter FullyQualifiedName~CrossPlatformHostPackagingCiTests -m:1 --logger "console;verbosity=minimal" --nologo`
  - Passed 1/1 test on `net9.0`.
  - Confirms the CI/workflow contract test still recognizes the framework matrix and net10 proof wiring.
- `.\eng\ci.ps1 -Lane all -Framework net10.0 -Configuration Release`
  - Passed.
  - Built publishable libraries for `net10.0`.
  - Ran `tools/AsterGraph.HostSample` on `net10.0` with `/p:EnableNet10ConsumerProof=true`.
  - HostSample output included `HOST_SAMPLE_OK:True`; the script appends `HOST_SAMPLE_NET10_OK:True` and `HOST_SAMPLE_NET10_PROJECT_OK:True` to `artifacts/proof/hostsample-net10-project.txt`.
  - Also revalidated the Windows WPF slice and 8 WPF tests.

## Decision

No CI configuration change is needed for bead `avalonia-node-map-v78.6.3`. The existing CI and release scripts already prove the intended .NET 8/9/10 release coverage, and local focused verification passed with the strongest emphasis on the .NET 10 lane.

## Residual Risks

- This is local Windows verification plus workflow/script inspection. Actual GitHub-hosted Linux/macOS runners and release artifacts still need normal CI execution.
- The local machine has .NET 10 SDK `10.0.203`, while `global.json` requests `10.0.201` with latest-feature roll-forward. This is expected under the configured roll-forward policy, but exact hosted-runner SDK resolution remains a CI concern.
