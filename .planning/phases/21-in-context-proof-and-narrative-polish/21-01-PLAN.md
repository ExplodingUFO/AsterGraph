---
phase: 21-in-context-proof-and-narrative-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AsterGraph.Demo/Views/MainWindow.axaml
  - src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
  - tests/AsterGraph.Editor.Tests/DemoMainWindowTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorDemoShellTests.cs
requirements:
  - PROOF-01
autonomous: true
---

<objective>
Replace the remaining generic shell copy with compact in-context ownership cues so users can immediately tell the demo is a host-controlled showcase running on one shared live session.
</objective>

<scope>
- Tighten the top-level `展示` and `证明` menu copy.
- Replace generic intro/drawer headings with proof-oriented labels.
- Add concise ownership cues for host-owned seams, shared runtime state, and the active host group.
</scope>

<read_first>
- .planning/phases/21-in-context-proof-and-narrative-polish/21-CONTEXT.md
- .planning/phases/21-in-context-proof-and-narrative-polish/21-RESEARCH.md
- .planning/phases/21-in-context-proof-and-narrative-polish/21-UI-SPEC.md
- .planning/phases/20-host-menu-control-consolidation/20-VERIFICATION.md
- src/AsterGraph.Demo/Views/MainWindow.axaml
- src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
- tests/AsterGraph.Editor.Tests/DemoMainWindowTests.cs
- tests/AsterGraph.Editor.Tests/GraphEditorDemoShellTests.cs
</read_first>

<action>
- Update `src/AsterGraph.Demo/Views/MainWindow.axaml` so the `展示` and `证明` submenu entries use compact proof-oriented copy. Replace the exact strings `打开展示面板` and `打开证明面板` with concise wording such as `打开展示摘要` and `查看证明要点`.
- Replace the drawer overline `宿主展示面板` with a more accurate proof/control-surface label, and replace the intro-strip title `实时节点图` with a live-session title that reflects the SDK showcase story.
- Add explicit proof cues near the graph intro strip for host-owned seams, shared runtime state, and the currently active host group. These cues must be visible without opening a second explanation rail.
- Update `src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs` to expose dedicated ownership/active-group properties needed by the new badges and labels. Do not rely solely on `MainEditorSummary` or broad generic summary strings for this first-read proof.
- Preserve the current menu groups, drawer structure, and single constructor-time `new GraphEditorViewModel(` path.
- Update the headless shell tests to assert the new high-signal labels or named proof cues without overfitting every full sentence.
</action>

<acceptance_criteria>
- `src/AsterGraph.Demo/Views/MainWindow.axaml` no longer contains the exact strings `打开展示面板`, `打开证明面板`, or `宿主展示面板`
- `src/AsterGraph.Demo/Views/MainWindow.axaml` contains proof-oriented showcase/proof actions with the exact labels `打开展示摘要` and `查看证明要点`
- `src/AsterGraph.Demo/Views/MainWindow.axaml` contains visible intro-strip proof cues for host ownership, shared runtime state, and active group context
- `src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs` exposes dedicated proof/ownership properties and still contains only the existing constructor-time `new GraphEditorViewModel(` initialization
- `dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~DemoMainWindowTests|FullyQualifiedName~GraphEditorDemoShellTests" -v minimal` exits 0
- `dotnet build src/AsterGraph.Demo/AsterGraph.Demo.csproj --nologo -v minimal` exits 0
</acceptance_criteria>

<verification>
- dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~DemoMainWindowTests|FullyQualifiedName~GraphEditorDemoShellTests" -v minimal
- dotnet build src/AsterGraph.Demo/AsterGraph.Demo.csproj --nologo -v minimal
</verification>

<output>
After completion, create `.planning/phases/21-in-context-proof-and-narrative-polish/21-01-SUMMARY.md`
</output>
