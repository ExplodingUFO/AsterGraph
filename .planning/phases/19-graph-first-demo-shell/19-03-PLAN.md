---
phase: 19-graph-first-demo-shell
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj
  - tests/AsterGraph.Editor.Tests/GraphEditorDemoShellTests.cs
  - src/AsterGraph.Demo/Views/MainWindow.axaml
  - src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
requirements:
  - SHOW-02
autonomous: true
---

<objective>
Lock the Phase 19 graph-first shell baseline with targeted regression coverage and concrete shell-state assertions so the new layout cannot drift back into a side-panel-first console.
</objective>

<scope>
- Add a headless regression harness for the demo shell if one does not already exist.
- Assert graph-first shell structure and single-session continuity through tests or other machine-checkable shell-state checks.
- Keep proof narrow: this plan protects the shell baseline, not the later Phase 21 narrative polish.
</scope>

<read_first>
- .planning/phases/19-graph-first-demo-shell/19-CONTEXT.md
- .planning/phases/19-graph-first-demo-shell/19-UI-SPEC.md
- .planning/phases/19-graph-first-demo-shell/19-01-PLAN.md
- .planning/phases/19-graph-first-demo-shell/19-02-PLAN.md
- tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj
- src/AsterGraph.Demo/Views/MainWindow.axaml
- src/AsterGraph.Demo/Views/MainWindow.axaml.cs
- src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
</read_first>

<action>
- Add a demo-shell regression file such as `tests/AsterGraph.Editor.Tests/GraphEditorDemoShellTests.cs` and reference `AsterGraph.Demo` from `tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj` if required so the test assembly can instantiate the demo window in headless Avalonia.
- Write targeted shell tests that instantiate `AsterGraph.Demo.Views.MainWindow` and verify the graph-first baseline through named parts or visual-tree queries. At minimum, assert that `PART_HostMenu` exists, `MainGraphEditorView` exists, there is only one main graph editor surface, and the compact secondary shell surface starts in the intended bounded state.
- Add one assertion that the shell continues to bind to the same `MainWindowViewModel.Editor` instance while the host shell surface opens or changes active group state.
- Make any small naming or state-shape adjustments in `MainWindow.axaml` or `MainWindowViewModel.cs` required to make those checks robust and machine-verifiable.
</action>

<acceptance_criteria>
- `tests/AsterGraph.Editor.Tests/GraphEditorDemoShellTests.cs` exists and contains headless demo-shell assertions
- `tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj` references `src/AsterGraph.Demo/AsterGraph.Demo.csproj` if the new tests require the demo assembly
- the new regression coverage fails if `PART_HostMenu` or `MainGraphEditorView` disappears from the demo shell
- the new regression coverage fails if the demo shell introduces a second equal-weight graph editor surface or rebuilds the editor when host shell state changes
- `dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorDemoShellTests" -v minimal` exits 0
- `dotnet build src/AsterGraph.Demo/AsterGraph.Demo.csproj --nologo -v minimal` exits 0
</acceptance_criteria>

<verification>
- dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorDemoShellTests" -v minimal
- dotnet build src/AsterGraph.Demo/AsterGraph.Demo.csproj --nologo -v minimal
</verification>

<output>
After completion, create `.planning/phases/19-graph-first-demo-shell/19-03-SUMMARY.md`
</output>
