---
phase: 19-graph-first-demo-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/AsterGraph.Demo/Views/MainWindow.axaml
  - src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
  - src/AsterGraph.Demo/ViewModels/
requirements:
  - SHOW-01
  - SHOW-02
autonomous: true
---

<objective>
Introduce the compact secondary shell surface and host-menu state model that keep the graph primary while allowing on-demand showcase content on the same live session.
</objective>

<scope>
- Add shell state for the host menu and compact secondary surface.
- Express compact showcase panels without scene switching or editor/session reconstruction.
- Keep this plan focused on graph-first shell behavior, not the full control consolidation owned by Phase 20.
</scope>

<read_first>
- .planning/phases/19-graph-first-demo-shell/19-CONTEXT.md
- .planning/phases/19-graph-first-demo-shell/19-RESEARCH.md
- .planning/phases/19-graph-first-demo-shell/19-UI-SPEC.md
- .planning/phases/19-graph-first-demo-shell/19-01-PLAN.md
- src/AsterGraph.Demo/Views/MainWindow.axaml
- src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
- src/AsterGraph.Demo/ViewModels/ViewModelBase.cs
- src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml
</read_first>

<action>
- Add explicit shell state to `MainWindowViewModel` for the graph-first showcase shell. This must include a compact secondary-surface open/closed state and a selected host-menu group state. Use concrete state names such as `IsHostPaneOpen` and `SelectedHostMenuGroup`, or equally explicit alternatives.
- Implement the compact secondary surface in `MainWindow.axaml` as a bounded on-demand panel, preferably a right-side `SplitView` pane or an equivalent right-side shell container. Keep the pane width within the design-contract range and make sure the graph remains visible when the pane opens.
- Wire the host menu scaffold from Plan 01 into this shell state so that opening a menu group changes the active compact panel without creating a second `GraphEditorViewModel` or replacing `Editor`.
- Migrate only the minimum shell summaries needed to satisfy the graph-first requirement into the compact secondary surface: a short showcase summary, current active menu group, and one or two subordinate proof rows. Leave the broader control migration for Phase 20.
- Keep the existing `Editor` initialization in `MainWindowViewModel` single-owner and constructor-local. Do not add code paths that instantiate another `GraphEditorViewModel` during menu or pane interactions.
</action>

<acceptance_criteria>
- `src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs` contains explicit host shell state for the compact secondary surface and active host menu group
- `src/AsterGraph.Demo/Views/MainWindow.axaml` contains a bounded right-side compact panel or `SplitView`-based pane instead of restoring permanent left/right narrative rails
- `src/AsterGraph.Demo/Views/MainWindow.axaml` still contains exactly one `controls:GraphEditorView x:Name="MainGraphEditorView"`
- `src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs` contains only the existing single constructor-time `new GraphEditorViewModel(` initialization and no additional runtime editor creation path
- opening or switching the compact secondary surface is driven by host shell state, not by scene switching collections or alternate editor instances
- `dotnet build src/AsterGraph.Demo/AsterGraph.Demo.csproj --nologo -v minimal` exits 0
</acceptance_criteria>

<verification>
- dotnet build src/AsterGraph.Demo/AsterGraph.Demo.csproj --nologo -v minimal
</verification>

<output>
After completion, create `.planning/phases/19-graph-first-demo-shell/19-02-SUMMARY.md`
</output>
