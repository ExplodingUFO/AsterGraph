---
phase: 19-graph-first-demo-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AsterGraph.Demo/Views/MainWindow.axaml
  - src/AsterGraph.Demo/Views/MainWindow.axaml.cs
  - src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
requirements:
  - SHOW-01
autonomous: true
---

<objective>
Replace the current three-column capability-console shell with a graph-first demo shell whose first read is a host-level menu plus one live `GraphEditorView`.
</objective>

<scope>
- Remove the permanent left/right first-screen composition that currently dominates the demo.
- Introduce a top-level host menu as the first visible shell control surface.
- Keep one central `GraphEditorView` as the dominant live artifact of the window.
</scope>

<read_first>
- .planning/phases/19-graph-first-demo-shell/19-CONTEXT.md
- .planning/phases/19-graph-first-demo-shell/19-RESEARCH.md
- .planning/phases/19-graph-first-demo-shell/19-UI-SPEC.md
- docs/plans/2026-04-08-demo-showcase-design.md
- src/AsterGraph.Demo/Views/MainWindow.axaml
- src/AsterGraph.Demo/Views/MainWindow.axaml.cs
- src/AsterGraph.Demo/ViewModels/MainWindowViewModel.cs
- src/AsterGraph.Demo/App.axaml
- src/AsterGraph.Avalonia/Controls/GraphEditorView.axaml
</read_first>

<action>
- Rewrite `src/AsterGraph.Demo/Views/MainWindow.axaml` so the root shell is top-first rather than a permanent `280,*,360` three-column grid. Use a `DockPanel` or equivalent structure with a top `Menu` named `PART_HostMenu` and a main content region below it.
- Add top-level menu groups `展示`, `视图`, `行为`, `运行时`, and `证明` to `PART_HostMenu`. In this plan the menu only needs to establish the shell scaffold and first-read hierarchy; full control migration is deferred to later plans.
- Keep exactly one `controls:GraphEditorView x:Name="MainGraphEditorView"` bound to `Editor="{Binding Editor}"` as the dominant content artifact. Preserve the existing demo editor instance; do not introduce a second live editor surface.
- Replace the current large left intro/navigation rail and large right explanation rail with a graph-first composition where any remaining supporting summaries are compact and subordinate to the graph region.
- Update `MainWindow.axaml.cs` only if needed to support named-part initialization or shell wiring. Avoid adding new runtime ownership or editor creation there.
</action>

<acceptance_criteria>
- `src/AsterGraph.Demo/Views/MainWindow.axaml` contains `<Menu x:Name="PART_HostMenu"` and top-level `MenuItem` headers for `展示`, `视图`, `行为`, `运行时`, and `证明`
- `src/AsterGraph.Demo/Views/MainWindow.axaml` no longer contains `ColumnDefinitions="280,*,360"`
- `src/AsterGraph.Demo/Views/MainWindow.axaml` contains exactly one `controls:GraphEditorView x:Name="MainGraphEditorView"` bound to `Editor="{Binding Editor}"`
- the first-screen shell no longer depends on permanent left and right `ScrollViewer` rails dominating the layout
- `dotnet build src/AsterGraph.Demo/AsterGraph.Demo.csproj --nologo -v minimal` exits 0
</acceptance_criteria>

<verification>
- dotnet build src/AsterGraph.Demo/AsterGraph.Demo.csproj --nologo -v minimal
</verification>

<output>
After completion, create `.planning/phases/19-graph-first-demo-shell/19-01-SUMMARY.md`
</output>
