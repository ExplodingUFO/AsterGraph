---
phase: 05-diagnostics-integration-inspection
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - src/AsterGraph.Editor/AsterGraph.Editor.csproj
  - src/AsterGraph.Editor/Diagnostics/GraphEditorInstrumentationOptions.cs
  - src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
  - src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
  - src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorDiagnosticsInstrumentationTests.cs
autonomous: true
requirements: [DIAG-03]
must_haves:
  truths:
    - "Hosts can opt into logging and tracing through standard .NET abstractions instead of an AsterGraph-only telemetry framework."
    - "Instrumentation is emitted from editor runtime operations and host seam boundaries, never from Avalonia controls."
    - "Disabling instrumentation preserves existing diagnostics sink behavior and does not change core editor semantics."
  artifacts:
    - path: "src/AsterGraph.Editor/Diagnostics/GraphEditorInstrumentationOptions.cs"
      provides: "Opt-in logging and tracing contract."
      contains: "ILoggerFactory|ActivitySource"
    - path: "src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs"
      provides: "Canonical composition of instrumentation options into sessions."
      contains: "Instrumentation"
    - path: "src/AsterGraph.Editor/Runtime/GraphEditorSession.cs"
      provides: "Runtime log and activity emission around meaningful operations."
      contains: "ActivitySource|ILogger"
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorDiagnosticsInstrumentationTests.cs"
      provides: "Focused validation for logger/tracing opt-in behavior."
      contains: "Activity"
  key_links:
    - from: "src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs"
      to: "src/AsterGraph.Editor/Runtime/GraphEditorSession.cs"
      via: "factory-wired instrumentation options"
      pattern: "Instrumentation"
    - from: "src/AsterGraph.Editor/Runtime/GraphEditorSession.cs"
      to: "src/AsterGraph.Editor/Diagnostics/GraphEditorDiagnostic.cs"
      via: "diagnostic-to-log/trace correlation"
      pattern: "Code|Operation|Severity"
---

<objective>
Wire the new instrumentation contract into the editor runtime so hosts can attach standard .NET logging and tracing sinks to AsterGraph without changing the diagnostics API shape.

Purpose: satisfy DIAG-03 with opt-in, vendor-neutral instrumentation rooted in the existing session/runtime architecture.
Output: logging and tracing composition through editor options/factory paths, runtime emission around meaningful boundaries, and focused instrumentation coverage.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-diagnostics-integration-inspection/05-CONTEXT.md
@.planning/phases/05-diagnostics-integration-inspection/05-RESEARCH.md
@.planning/phases/05-diagnostics-integration-inspection/05-01-PLAN.md
@.planning/phases/05-diagnostics-integration-inspection/05-02-PLAN.md
@src/AsterGraph.Editor/AsterGraph.Editor.csproj
@src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs
@src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs
@src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
@.planning/research/ARCHITECTURE.md

<interfaces>
Direction locked by research:
```csharp
public sealed record GraphEditorInstrumentationOptions
{
    public ILoggerFactory? LoggerFactory { get; init; }
    public ActivitySource? ActivitySource { get; init; }
}
```

Instrumentation is opt-in and should stay disabled when options are omitted.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the public instrumentation wiring through the editor options and factory path</name>
  <files>src/AsterGraph.Editor/AsterGraph.Editor.csproj, src/AsterGraph.Editor/Diagnostics/GraphEditorInstrumentationOptions.cs, src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs, src/AsterGraph.Editor/Hosting/AsterGraphEditorFactory.cs</files>
  <action>Per D-07 through D-09, finish the concrete composition path for instrumentation. Add any required logging abstraction package reference to `AsterGraph.Editor.csproj`, finalize `GraphEditorInstrumentationOptions`, and thread it through `AsterGraphEditorOptions` plus `AsterGraphEditorFactory` so both `Create(...)` and `CreateSession(...)` can construct instrumented sessions. Keep the configuration minimal and host-owned; do not introduce a custom telemetry service abstraction or anything Avalonia-specific.</action>
  <verify>
    <automated>dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj -v minimal</automated>
  </verify>
  <acceptance_criteria>Hosts can enable logs, traces, or both through the canonical editor options/factory path, and omitting instrumentation keeps the existing default runtime behavior unchanged.</acceptance_criteria>
  <done>The canonical composition path now knows how to create instrumented sessions without changing host adoption patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Emit runtime logs and activities only at meaningful integration boundaries</name>
  <files>src/AsterGraph.Editor/Runtime/GraphEditorSession.cs, tests/AsterGraph.Editor.Tests/GraphEditorDiagnosticsInstrumentationTests.cs</files>
  <action>Per D-07 and D-08, emit logger entries and `ActivitySource` spans around support-relevant editor runtime operations only: workspace save/load, fragment export/import, host seam failures or fallbacks, and any key diagnostics publication path introduced in `05-02`. Correlate logs and activities with stable operation names and existing diagnostics codes when possible so hosts can line up inspection snapshots, diagnostics history, and telemetry. Do not instrument `AsterGraph.Avalonia`, per-pointer actions, or frame-level rendering. Validate through a focused temp harness such as `%TEMP%\\astergraph-phase5-instrumentation-validation\\AsterGraph.Phase5.Instrumentation.Validation.csproj`.</action>
  <verify>
    <automated>dotnet test %TEMP%\astergraph-phase5-instrumentation-validation\AsterGraph.Phase5.Instrumentation.Validation.csproj -v minimal</automated>
  </verify>
  <acceptance_criteria>The focused regression ring proves that logs and activities are emitted only when instrumentation is enabled, they stay rooted in the editor runtime, and they correlate to meaningful diagnostics/operation boundaries.</acceptance_criteria>
  <done>DIAG-03 is implemented through standard .NET instrumentation instead of a custom AsterGraph-only telemetry system.</done>
</task>

</tasks>

<verification>
- `dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj -v minimal`
- `dotnet test %TEMP%\astergraph-phase5-instrumentation-validation\AsterGraph.Phase5.Instrumentation.Validation.csproj -v minimal`
</verification>

<success_criteria>
- Logging and tracing are opt-in, vendor-neutral, and reachable from the canonical editor composition path.
- Instrumentation stays in `AsterGraph.Editor` runtime operations and host seam boundaries.
- Diagnostics sink behavior and compatibility UX remain intact when instrumentation is disabled.
</success_criteria>

<output>
After completion, create `.planning/phases/05-diagnostics-integration-inspection/05-03-SUMMARY.md`
</output>
