---
phase: 05-diagnostics-integration-inspection
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
  - tools/AsterGraph.HostSample/Program.cs
  - tools/AsterGraph.PackageSmoke/Program.cs
  - README.md
  - docs/host-integration.md
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03]
must_haves:
  truths:
    - "The compatibility story proves that legacy `GraphEditorViewModel` hosts and canonical factory/session hosts can both reach the new diagnostics and inspection surfaces."
    - "Host sample output and package smoke markers prove inspection snapshots, recent diagnostics, and opt-in instrumentation without requiring Avalonia-specific diagnostics parsing."
    - "Consumer docs describe diagnostics and inspection as editor-layer contracts and keep `StatusMessage` framed as a compatibility UX surface."
  artifacts:
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs"
      provides: "Compatibility regression coverage for the new diagnostics and inspection surfaces."
      contains: "Diagnostics"
    - path: "tools/AsterGraph.HostSample/Program.cs"
      provides: "Human-readable proof of inspection, recent diagnostics, and instrumentation usage."
      contains: "diagnostic|inspection|Activity"
    - path: "tools/AsterGraph.PackageSmoke/Program.cs"
      provides: "Machine-checkable diagnostics and instrumentation markers."
      contains: "DIAG_"
    - path: "docs/host-integration.md"
      provides: "Consumer guidance for diagnostics, inspection, and opt-in telemetry."
      contains: "inspection"
  key_links:
    - from: "tools/AsterGraph.HostSample/Program.cs"
      to: "src/AsterGraph.Editor/Hosting/AsterGraphEditorOptions.cs"
      via: "instrumentation and diagnostics configuration"
      pattern: "Diagnostics|Instrumentation"
    - from: "tools/AsterGraph.PackageSmoke/Program.cs"
      to: "src/AsterGraph.Editor/Runtime/IGraphEditorSession.cs"
      via: "machine-checkable diagnostics markers"
      pattern: "DIAG_"
---

<objective>
Close Phase 5 with a consumer-facing proof ring: compatibility regressions, host sample output, package smoke markers, focused final validation, and updated documentation.

Purpose: prove the diagnostics and inspection story end-to-end for both canonical and retained host paths, and document the supported adoption model.
Output: final Phase 5 proof ring, updated consumer guidance, and machine-checkable diagnostics markers.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-diagnostics-integration-inspection/05-CONTEXT.md
@.planning/phases/05-diagnostics-integration-inspection/05-RESEARCH.md
@.planning/phases/05-diagnostics-integration-inspection/05-02-PLAN.md
@.planning/phases/05-diagnostics-integration-inspection/05-03-PLAN.md
@tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
@tools/AsterGraph.HostSample/Program.cs
@tools/AsterGraph.PackageSmoke/Program.cs
@README.md
@docs/host-integration.md

<interfaces>
Expected proof points after `05-02` and `05-03`:
```csharp
session.Diagnostics.CaptureInspectionSnapshot();
session.Diagnostics.GetRecentDiagnostics();
new AsterGraphEditorOptions
{
    DiagnosticsSink = ...,
    Instrumentation = ...,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove diagnostics and inspection across compatibility, sample, and smoke paths</name>
  <files>tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs, tools/AsterGraph.HostSample/Program.cs, tools/AsterGraph.PackageSmoke/Program.cs</files>
  <action>Extend the compatibility proof ring so both legacy `GraphEditorViewModel` construction and canonical factory/session creation can reach the new diagnostics surface, capture inspection snapshots, and observe recent diagnostics. Rework the host sample to print human-readable evidence for inspection state, recent diagnostic history, and optional logger/tracer wiring. Rework package smoke to emit machine-checkable `DIAG_*` markers for inspection snapshots, diagnostics history, diagnostics sink continuity, and instrumentation enablement while preserving earlier phase markers. Keep the proof rooted in `AsterGraph.Editor`; do not require an Avalonia diagnostics UI. Use focused temp harnesses instead of touching the workspace-local `tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj` or the unresolved `GraphEditorViewTests.cs` noise file.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "dotnet run --project tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj; dotnet run --project tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj"</automated>
  </verify>
  <acceptance_criteria>The host sample shows readable diagnostics and inspection output, package smoke emits stable `DIAG_*` markers, and compatibility coverage proves legacy plus factory/session hosts can both adopt the new surface.</acceptance_criteria>
  <done>The new observability story is proven across human-readable and machine-checkable consumer entry points.</done>
</task>

<task type="auto">
  <name>Task 2: Document the diagnostics story and close the final focused validation ring</name>
  <files>README.md, docs/host-integration.md</files>
  <action>Update the root docs so they tell one coherent Phase 5 story: diagnostics and inspection live in `AsterGraph.Editor`, `StatusMessage` remains a compatibility UX surface, and logging/tracing are optional host-standard extensions rather than mandatory telemetry. Include canonical examples for factory/session adoption and note the retained compatibility path through `GraphEditorViewModel.Session`. Build a final temp validation harness under `%TEMP%\\astergraph-phase5-validation\\AsterGraph.Phase5.Validation.csproj` that references only the focused Phase 5 regression files and explicitly avoids the workspace-local test noise. Do not touch `src/AsterGraph.Editor/README.md` while the current local modifications remain unresolved; keep the consumer-facing closeout in root docs plus sample/smoke proof instead.</action>
  <verify>
    <automated>dotnet test %TEMP%\astergraph-phase5-validation\AsterGraph.Phase5.Validation.csproj -v minimal</automated>
  </verify>
  <acceptance_criteria>The final docs describe the same diagnostics and inspection adoption model proven by the sample and smoke output, and the final focused harness passes without depending on noisy workspace-local test files.</acceptance_criteria>
  <done>Phase 5 closes with aligned docs and a proof ring that is safe to run in this workspace.</done>
</task>

</tasks>

<verification>
- `dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj -v minimal`
- `dotnet test %TEMP%\astergraph-phase5-validation\AsterGraph.Phase5.Validation.csproj -v minimal`
- `dotnet run --project tools/AsterGraph.HostSample/AsterGraph.HostSample.csproj`
- `dotnet run --project tools/AsterGraph.PackageSmoke/AsterGraph.PackageSmoke.csproj`
- `rg -n "diagnostic|inspection|logger|trace|ActivitySource|DIAG_" README.md docs/host-integration.md tools/AsterGraph.HostSample/Program.cs tools/AsterGraph.PackageSmoke/Program.cs`
</verification>

<success_criteria>
- The full diagnostics and inspection story is proven for canonical and retained host paths.
- Sample and smoke outputs give both human-readable and machine-checkable evidence.
- Consumer docs explain the supported adoption model without relying on UI-only status text or Avalonia-specific diagnostics surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/05-diagnostics-integration-inspection/05-04-SUMMARY.md`
</output>
