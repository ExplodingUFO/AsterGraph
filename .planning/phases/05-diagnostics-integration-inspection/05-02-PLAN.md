---
phase: 05-diagnostics-integration-inspection
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
  - src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
  - src/AsterGraph.Editor/Diagnostics/GraphEditorDiagnostic.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorDiagnosticsInspectionTests.cs
autonomous: true
requirements: [DIAG-01, DIAG-02]
must_haves:
  truths:
    - "The concrete runtime session can capture one immutable inspection snapshot that aggregates current document, selection, viewport, pending connection, capability, status, node-position, and recent-diagnostics context."
    - "Hosts receive meaningful machine-readable operation diagnostics and warnings/errors without scraping `StatusMessage`."
    - "The retained `GraphEditorViewModel.Session` path reaches the same diagnostics surface as factory-created sessions."
  artifacts:
    - path: "src/AsterGraph.Editor/Runtime/GraphEditorSession.cs"
      provides: "Concrete diagnostics implementation, inspection snapshot capture, and bounded diagnostics history."
      contains: "CaptureInspectionSnapshot|GetRecentDiagnostics"
    - path: "src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs"
      provides: "Compatibility-path state inputs and runtime publication hooks."
      contains: "StatusMessage|HasPendingConnection|PublishRecoverableFailure"
    - path: "tests/AsterGraph.Editor.Tests/GraphEditorDiagnosticsInspectionTests.cs"
      provides: "Focused behavior coverage for inspection aggregation and diagnostics publication."
      contains: "InspectionSnapshot"
  key_links:
    - from: "src/AsterGraph.Editor/Runtime/GraphEditorSession.cs"
      to: "src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs"
      via: "inspection snapshot composition over existing editor state"
      pattern: "CreateDocumentSnapshot|GetNodePositions|StatusMessage|HasPendingConnection"
    - from: "src/AsterGraph.Editor/Runtime/GraphEditorSession.cs"
      to: "src/AsterGraph.Editor/Diagnostics/GraphEditorDiagnostic.cs"
      via: "recent diagnostics history and operation publication"
      pattern: "Publish|Severity|Operation|Code"
---

<objective>
Implement the concrete diagnostics and inspection behavior on top of the new Phase 5 contracts: aggregate immutable inspection snapshots, maintain bounded recent-diagnostics history, and publish richer machine-readable operation diagnostics.

Purpose: satisfy the runtime half of DIAG-01 and DIAG-02 while preserving the existing compatibility story.
Output: working session diagnostics implementation, compatibility-path bridging, and focused inspection/diagnostics regression coverage.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-diagnostics-integration-inspection/05-CONTEXT.md
@.planning/phases/05-diagnostics-integration-inspection/05-RESEARCH.md
@.planning/phases/05-diagnostics-integration-inspection/05-01-PLAN.md
@src/AsterGraph.Editor/Runtime/GraphEditorSession.cs
@src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs
@src/AsterGraph.Editor/Diagnostics/GraphEditorDiagnostic.cs
@src/AsterGraph.Editor/Diagnostics/IGraphEditorDiagnosticsSink.cs
@src/AsterGraph.Editor/Events/GraphEditorRecoverableFailureEventArgs.cs
@tests/AsterGraph.Editor.Tests/GraphEditorServiceSeamsTests.cs
@tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs

<interfaces>
Expected from `05-01`:
```csharp
public interface IGraphEditorDiagnostics
{
    GraphEditorInspectionSnapshot CaptureInspectionSnapshot();
    IReadOnlyList<GraphEditorDiagnostic> GetRecentDiagnostics(int maxCount = 20);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement aggregate inspection snapshot capture and bounded recent-diagnostics history</name>
  <files>src/AsterGraph.Editor/Runtime/GraphEditorSession.cs, src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs</files>
  <action>Per D-04 through D-06 and D-09, implement the new session diagnostics surface in `GraphEditorSession` by composing over the existing editor state instead of creating a second editor model. Build one immutable inspection snapshot from `CreateDocumentSnapshot`, selection, viewport, capability, and node-position queries, plus narrowly scoped pending-connection and current-status records. Maintain a bounded in-memory recent-diagnostics history so hosts can ask what happened most recently without scraping logs or UI text. Keep the retained `GraphEditorViewModel.Session` path working as the migration bridge; do not require hosts to construct a new object graph to access inspection.</action>
  <verify>
    <automated>dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj -v minimal</automated>
  </verify>
  <acceptance_criteria>A host can call the new diagnostics surface on a runtime session and receive a complete immutable inspection snapshot plus bounded recent diagnostics without Avalonia dependencies.</acceptance_criteria>
  <done>The Phase 5 inspection surface is concretely implemented over the existing runtime/session stack and is reachable from both canonical and compatibility host paths.</done>
</task>

<task type="auto">
  <name>Task 2: Publish richer machine-readable operation diagnostics and prove the support story</name>
  <files>src/AsterGraph.Editor/Runtime/GraphEditorSession.cs, src/AsterGraph.Editor/ViewModels/GraphEditorViewModel.cs, src/AsterGraph.Editor/Diagnostics/GraphEditorDiagnostic.cs, tests/AsterGraph.Editor.Tests/GraphEditorDiagnosticsInspectionTests.cs</files>
  <action>Per D-01, D-02, D-05, and D-08, broaden diagnostics publication beyond the current recoverable-failure-only baseline, but keep the signal intentionally support-oriented. At minimum, cover meaningful runtime operations and warnings/errors around workspace save/load, fragment export/import, clipboard or host-seam failures already surfaced through the editor runtime, and any other integration-relevant fallback that already exists in `GraphEditorViewModel`. Reuse stable `Code`, `Operation`, `Severity`, and message semantics; only extend `GraphEditorDiagnostic` if a narrow extra field is truly necessary. Do not emit per-pointer or per-frame noise. Add focused behavior coverage in a new test file and validate through a temp harness such as `%TEMP%\\astergraph-phase5-inspection-validation\\AsterGraph.Phase5.Inspection.Validation.csproj`.</action>
  <verify>
    <automated>dotnet test %TEMP%\astergraph-phase5-inspection-validation\AsterGraph.Phase5.Inspection.Validation.csproj -v minimal</automated>
  </verify>
  <acceptance_criteria>The focused regression ring proves that operation diagnostics are machine-readable, recent diagnostics are queryable, and `StatusMessage` remains a parallel compatibility UX surface rather than the sole diagnostics contract.</acceptance_criteria>
  <done>DIAG-01 and DIAG-02 now have a concrete runtime behavior path backed by focused tests instead of only contract scaffolding.</done>
</task>

</tasks>

<verification>
- `dotnet build src/AsterGraph.Editor/AsterGraph.Editor.csproj -v minimal`
- `dotnet test %TEMP%\astergraph-phase5-inspection-validation\AsterGraph.Phase5.Inspection.Validation.csproj -v minimal`
</verification>

<success_criteria>
- Session diagnostics can answer both “what state is the editor in now?” and “what recently went wrong?”.
- Hosts receive support-relevant machine-readable operation diagnostics without scraping UI text.
- The retained compatibility facade gains the same inspection and diagnostics behavior through `GraphEditorViewModel.Session`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-diagnostics-integration-inspection/05-02-SUMMARY.md`
</output>
