---
phase: 31-history-and-save-semantic-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - tests/AsterGraph.Editor.Tests/GraphEditorTransactionTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorHistoryInteractionTests.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorSaveBoundaryTests.cs
  - tests/AsterGraph.Editor.Tests/NodeCanvasNodeDragCoordinatorTests.cs
  - tests/AsterGraph.Editor.Tests/NodeCanvasPointerInteractionCoordinatorTests.cs
  - eng/ci.ps1
requirements:
  - STATE-02
autonomous: true
---

<objective>
Split retained history/save regressions into smaller focused suites and point the maintenance lane at them so contributors can localize history interaction, drag, and save-boundary failures without depending on one broad transaction file.
</objective>

<scope>
- Move retained history interaction and save-boundary assertions into dedicated focused test suites.
- Keep runtime batching and session-transaction coverage in `GraphEditorTransactionTests.cs`.
- Update canvas drag coordinator coverage where needed so the retained interaction boundary remains explicit.
- Replace the broad transaction-suite maintenance filter entry with the new focused suites.
</scope>

<must_haves>
- Dedicated focused suites exist for retained history interaction and save-boundary behavior.
- `GraphEditorTransactionTests.cs` keeps runtime mutation batching and kernel/session transaction coverage instead of carrying the whole retained history story.
- The maintenance lane points at the new focused suites instead of the broad transaction file for retained history/save semantics.
- Pointer/drag coordinator regressions still cover the retained interaction boundary that feeds history completion.
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Extract retained history interaction and save-boundary regressions into dedicated suites</name>
    <files>
      tests/AsterGraph.Editor.Tests/GraphEditorTransactionTests.cs
      tests/AsterGraph.Editor.Tests/GraphEditorHistoryInteractionTests.cs
      tests/AsterGraph.Editor.Tests/GraphEditorSaveBoundaryTests.cs
    </files>
    <read_first>
      tests/AsterGraph.Editor.Tests/GraphEditorTransactionTests.cs
      tests/AsterGraph.Editor.Tests/GraphEditorHistorySemanticTests.cs
      .planning/phases/31-history-and-save-semantic-closure/31-RESEARCH.md
    </read_first>
    <action>
      Extract retained history interaction, no-op drag, selection round-trip, and save-boundary behavior into smaller dedicated test files. Leave `GraphEditorTransactionTests.cs` focused on runtime batching and session-transaction behavior.
    </action>
    <acceptance_criteria>
      dedicated retained history interaction and save-boundary suites exist
      `GraphEditorTransactionTests.cs` no longer carries the retained history/save burden
      the focused suites are small enough that failures identify one semantic area directly
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Task 2: Keep canvas drag semantics explicit and update the maintenance lane to the new suites</name>
    <files>
      tests/AsterGraph.Editor.Tests/NodeCanvasNodeDragCoordinatorTests.cs
      tests/AsterGraph.Editor.Tests/NodeCanvasPointerInteractionCoordinatorTests.cs
      eng/ci.ps1
    </files>
    <read_first>
      tests/AsterGraph.Editor.Tests/NodeCanvasNodeDragCoordinatorTests.cs
      tests/AsterGraph.Editor.Tests/NodeCanvasPointerInteractionCoordinatorTests.cs
      eng/ci.ps1
      .planning/phases/31-history-and-save-semantic-closure/31-RESEARCH.md
    </read_first>
    <action>
      Add or refine canvas drag coordinator tests so the retained interaction boundary remains explicit, then replace the maintenance lane's broad history/save bundle with the new focused suites while keeping the lane fast and hotspot-oriented.
    </action>
    <acceptance_criteria>
      node-drag or pointer-release tests explicitly cover the history interaction boundary
      `eng/ci.ps1 -Lane maintenance` points at dedicated history/save suites rather than the broad transaction file for this surface
      the maintenance lane still exits 0
    </acceptance_criteria>
  </task>
</tasks>

<threat_model>
- Risk: splitting tests produces duplicate assertions or leaves gaps between old and new suites.
- Mitigation: move retained history/save cases deliberately, then keep runtime batching in `GraphEditorTransactionTests.cs` as the remaining transaction-focused scope.
- Risk: the maintenance lane becomes slower or noisier after the split.
- Mitigation: replace, do not add, the broad transaction-suite entry for retained history/save coverage.
- Risk: drag semantics become implicit again because coordinator coverage only checks visual movement.
- Mitigation: add explicit coordinator assertions around the retained history interaction boundary.
</threat_model>

<verification>
- dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj --filter "FullyQualifiedName~GraphEditorHistoryInteractionTests|FullyQualifiedName~GraphEditorSaveBoundaryTests|FullyQualifiedName~NodeCanvasNodeDragCoordinatorTests|FullyQualifiedName~NodeCanvasPointerInteractionCoordinatorTests" -v minimal
- pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane maintenance -Framework all -Configuration Release
</verification>

<output>
After completion, create `.planning/phases/31-history-and-save-semantic-closure/31-02-SUMMARY.md`
</output>
