---
phase: 30-milestone-history-and-refactor-gate-closeout
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - eng/ci.ps1
requirements:
  - GUARD-01
autonomous: true
---

<objective>
Add one checked-in maintenance/refactor lane to `eng/ci.ps1` so hotspot-sensitive refactor work has a stable proof command that is narrower than the release gate and does not depend on manually assembled test filters.
</objective>

<scope>
- Extend `eng/ci.ps1` with a `maintenance` lane rooted in the current script-first validation posture.
- Curate the lane around hotspot-sensitive test suites plus `ScaleSmoke`.
- Keep `release` as the full publish-facing gate; do not fold package validation or `PackageSmoke` into the maintenance lane unless execution proves they are required for the targeted surface.
</scope>

<must_haves>
- `eng/ci.ps1` exposes a `maintenance` lane through the same checked-in script interface used by current lanes.
- The lane runs the hotspot-sensitive regression surface around facade/runtime parity, history/save behavior, migration compatibility, projection/refactor coverage, and `NodeCanvas` pointer coordination.
- The lane also runs `tools/AsterGraph.ScaleSmoke` so hotspot changes still see one maintained runtime/readiness proof path.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane maintenance -Framework all -Configuration Release` exits 0.
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Add a script-first `maintenance` lane to `eng/ci.ps1`</name>
    <files>
      eng/ci.ps1
    </files>
    <read_first>
      eng/ci.ps1
      .planning/phases/30-milestone-history-and-refactor-gate-closeout/30-RESEARCH.md
      docs/plans/2026-04-13-god-code-split-foundation.md
    </read_first>
    <action>
      Extend the lane switch and execution flow in `eng/ci.ps1` so contributors can invoke a dedicated `maintenance` lane through the same repo-local script path as the existing `all` and `release` lanes.
    </action>
    <acceptance_criteria>
      `eng/ci.ps1` accepts `-Lane maintenance`
      the new lane does not duplicate release-only pack/package-validation work
      the new lane remains available through the existing script entrypoint instead of a second script or YAML-only path
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Task 2: Curate the hotspot-sensitive maintenance proof surface</name>
    <files>
      eng/ci.ps1
    </files>
    <read_first>
      eng/ci.ps1
      tests/AsterGraph.Editor.Tests/GraphEditorMutationCompatibilityTests.cs
      tests/AsterGraph.Editor.Tests/GraphEditorFacadeMutationParityTests.cs
      tests/AsterGraph.Editor.Tests/GraphEditorSessionTests.cs
      tests/AsterGraph.Editor.Tests/GraphEditorTransactionTests.cs
      tests/AsterGraph.Editor.Tests/GraphEditorMigrationCompatibilityTests.cs
      tests/AsterGraph.Editor.Tests/GraphEditorFacadeRefactorTests.cs
      tests/AsterGraph.Editor.Tests/GraphEditorViewModelProjectionTests.cs
      tests/AsterGraph.Editor.Tests/NodeCanvasPointerInteractionCoordinatorTests.cs
      tools/AsterGraph.ScaleSmoke/Program.cs
    </read_first>
    <action>
      Make the `maintenance` lane run the focused hotspot suites and `ScaleSmoke`, using checked-in filter logic rather than expecting contributors to remember or hand-build a filter string during refactor work.
    </action>
    <acceptance_criteria>
      `maintenance` runs the hotspot-sensitive editor suites
      `maintenance` runs `AsterGraph.ScaleSmoke`
      `maintenance` is materially faster and narrower than `release`
      `pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane maintenance -Framework all -Configuration Release` exits 0
    </acceptance_criteria>
  </task>
</tasks>

<threat_model>
- Risk: the new lane becomes a second validation story that drifts from the repo-local script boundary.
- Mitigation: implement it inside `eng/ci.ps1` only, and keep later doc updates pointing at that same command.
- Risk: the lane is too narrow and gives false confidence during hotspot refactors.
- Mitigation: anchor it on the existing parity/history/migration/projection/canvas suites plus `ScaleSmoke`, and keep `release` documented as the full publish gate.
- Risk: script changes widen filesystem cleanup or artifact deletion behavior.
- Mitigation: reuse the current `Reset-Directory` and repo-root safety checks; do not add new recursive deletes outside established artifact roots.
</threat_model>

<verification>
- pwsh -NoProfile -ExecutionPolicy Bypass -File .\eng\ci.ps1 -Lane maintenance -Framework all -Configuration Release
</verification>

<output>
After completion, create `.planning/phases/30-milestone-history-and-refactor-gate-closeout/30-02-SUMMARY.md`
</output>
