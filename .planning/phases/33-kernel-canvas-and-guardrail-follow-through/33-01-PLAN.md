---
phase: 33-kernel-canvas-and-guardrail-follow-through
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AsterGraph.Editor/Kernel/GraphEditorKernel.cs
  - src/AsterGraph.Editor/Kernel/Internal/CommandRouting/GraphEditorKernelCommandRouter.cs
  - src/AsterGraph.Editor/Kernel/Internal/CommandRouting/GraphEditorKernelCommandRouterHost.cs
  - tests/AsterGraph.Editor.Tests/GraphEditorKernelCommandRouterTests.cs
  - eng/ci.ps1
requirements:
  - FACADE-03
  - FACADE-04
autonomous: true
---

<objective>
Extract the remaining command-router host ownership out of `GraphEditorKernel.cs` so the kernel keeps command behavior but stops directly carrying the router-host contract inline.
</objective>

<scope>
- Introduce a dedicated internal router host adapter for `GraphEditorKernel`.
- Remove direct `IGraphEditorKernelCommandRouterHost` implementation from the kernel type.
- Keep command descriptor shape and canonical command execution behavior unchanged.
</scope>

<must_haves>
- `GraphEditorKernel` no longer implements `IGraphEditorKernelCommandRouterHost` directly.
- canonical command descriptors and `TryExecuteCommand(...)` behavior remain stable.
- the maintenance lane stays aligned to the moved kernel hotspot if it was previously uncovered.
</must_haves>

<tasks>
  <task type="auto">
    <name>Task 1: Extract dedicated command-router host ownership from GraphEditorKernel</name>
    <files>
      src/AsterGraph.Editor/Kernel/GraphEditorKernel.cs
      src/AsterGraph.Editor/Kernel/Internal/CommandRouting/GraphEditorKernelCommandRouter.cs
      src/AsterGraph.Editor/Kernel/Internal/CommandRouting/GraphEditorKernelCommandRouterHost.cs
    </files>
    <read_first>
      src/AsterGraph.Editor/Kernel/GraphEditorKernel.cs
      src/AsterGraph.Editor/Kernel/Internal/CommandRouting/GraphEditorKernelCommandRouter.cs
      .planning/phases/33-kernel-canvas-and-guardrail-follow-through/33-RESEARCH.md
    </read_first>
    <action>
      Add a dedicated internal router host adapter and rewire the kernel to use it so command-routing ownership matches the host-adapter pattern already used by node, connection, and workspace collaborators.
    </action>
    <acceptance_criteria>
      kernel type no longer implements the router-host interface directly
      command descriptors remain stable
      canonical command execution stays green
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Task 2: Guard the moved kernel seam and keep maintenance representative</name>
    <files>
      tests/AsterGraph.Editor.Tests/GraphEditorKernelCommandRouterTests.cs
      eng/ci.ps1
    </files>
    <read_first>
      tests/AsterGraph.Editor.Tests/GraphEditorKernelCommandRouterTests.cs
      eng/ci.ps1
      .planning/phases/33-kernel-canvas-and-guardrail-follow-through/33-VALIDATION.md
    </read_first>
    <action>
      Add or refine focused tests that prove the new host adapter exists and the kernel no longer owns the router-host contract directly, then include the suite in maintenance if the moved hotspot would otherwise be unguarded.
    </action>
    <acceptance_criteria>
      focused kernel seam tests fail on ownership regression
      maintenance stays narrow
      moved kernel hotspot seam is covered by a checked-in gate
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- dotnet test tests/AsterGraph.Editor.Tests/AsterGraph.Editor.Tests.csproj -c Release --filter "FullyQualifiedName~GraphEditorKernelCommandRouterTests|FullyQualifiedName~GraphEditorServiceSeamsTests|FullyQualifiedName~GraphEditorMigrationCompatibilityTests" -v minimal
</verification>

<output>
After completion, create `.planning/phases/33-kernel-canvas-and-guardrail-follow-through/33-01-SUMMARY.md`
</output>
