# AsterGraph Avalonia Host

This project is a minimal native Avalonia desktop host for AsterGraph.

Run it with:

```powershell
dotnet run
```

The starter keeps the integration host-owned: replace the sample `GraphDocument`, `NodeCatalog`, and window chrome with your product code while keeping the `AsterGraphHostBuilder` composition path.
