namespace AsterGraph.Core.Models;

/// <summary>
/// Declares the persisted card expansion mode for one node surface.
/// </summary>
public enum GraphNodeExpansionState
{
    Collapsed = 0,
    Expanded = 1,
}
