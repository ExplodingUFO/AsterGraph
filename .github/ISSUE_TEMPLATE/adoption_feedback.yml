name: Adoption feedback
description: Report how a real or trial adoption attempt went, where friction appeared, and what should come next.
title: "[Adoption] "
labels:
  - feedback
body:
  - type: input
    id: version
    attributes:
      label: AsterGraph version
      description: NuGet package version or public tag you evaluated
      placeholder: 0.11.0-beta / v0.11.0-beta
    validations:
      required: true
  - type: dropdown
    id: route
    attributes:
      label: Route or artifact tried
      options:
        - HelloWorld
        - AsterGraph.Starter.Avalonia
        - HelloWorld.Avalonia
        - ConsumerSample.Avalonia
        - HostSample
        - PackageSmoke
        - ScaleSmoke
        - Demo
        - Other
    validations:
      required: true
  - type: textarea
    id: friction
    attributes:
      label: Main friction
      description: What slowed you down or felt unclear?
    validations:
      required: true
  - type: textarea
    id: proof_markers
    attributes:
      label: Evidence proof markers
      description: Include exact proof marker lines from your artifact set
      placeholder: HELLOWORLD_WPF_OK:True
    validations:
      required: true
  - type: textarea
    id: support_bundle
    attributes:
      label: Support bundle attachment
      description: Attachment note for the intake record. Use `SUPPORT_BUNDLE_PATH:...` when the route produced a bundle, or `NO_SUPPORT_BUNDLE:route-cannot-produce-one` when it did not.
      placeholder: SUPPORT_BUNDLE_PATH:... or NO_SUPPORT_BUNDLE:route-cannot-produce-one
